# 工艺路线模块前端重构方案

## 1. 任务理解

本次只停留在文档阶段，不修改项目实际代码。

目标是只针对 `工艺路线模块` 做一次更贴近业务、也更顺手的前端重构规划，其他模块一律不动。你这次提出的重点可以收敛成两层：

1. 工艺路线主页面重构  
   从“左侧路线卡片 + 右侧详情拼页”改成“工艺产品列表页”，让用户先选对象，再进入详情。

2. 工艺路线详情页重构  
   把当前信息堆叠感比较强的详情页拆得更清楚，让概览、工序、资料、版本这些区块职责更明确。

同时有两个明确边界：

- 筛选栏整体逻辑不动，保留现有筛选思路和时间范围组件。
- 只修改工艺路线模块相关页面、类型、mock 数据，不改其他模块页面与代码。

---

## 2. 所属 PLM 业务链路

本次属于：

- 基础资料模块
- Process 核心对象前端表达
- 工艺路线中心页面
- 工艺路线详情页面

它服务的真实业务动作主要是：

- 先找到某个产品/型号下的工艺路线
- 再进入该路线查看具体工序、确认门禁、资料挂接、版本变更和联动影响
- 支持工程、采购、品质围绕同一条工艺路线协作

---

## 3. 涉及前端页面

本次方案只涉及以下前端文件：

- `plm-web/src/views/process/ProcessCenterView.vue`
- `plm-web/src/types/process.ts`
- `plm-web/src/mock/process.ts`
- `plm-web/src/api/modules/process.ts`

如果后续正式实施时想把主列表和详情拆开，也可以考虑新增：

- `plm-web/src/views/process/components/ProcessRouteListTable.vue`
- `plm-web/src/views/process/components/ProcessRouteDetailView.vue`
- `plm-web/src/views/process/components/ProcessAttachmentTable.vue`
- `plm-web/src/views/process/components/ProcessChangeTimeline.vue`

但从当前工程规模看，第一轮仍然可以先只改 `ProcessCenterView.vue`，避免扩散范围。

---

## 4. 当前页面问题判断

结合你给的截图、现有代码和之前的工艺路线文档，当前页面主要有下面这些问题。

### 4.1 主页面入口层级不够直接

当前 `ProcessCenterView.vue` 是：

- 顶部说明区
- 指标卡片区
- 筛选区
- 左侧路线卡片列表
- 右侧详情区

这套结构的问题不是“做不出来”，而是入口判断路径偏绕：

- 用户先看到很多摘要卡片
- 再看到路线卡片
- 再进入右侧详情

对业务来说，用户最核心的问题其实是：

- 我要找哪一个产品的工艺路线？

所以主页面更适合先是“工艺产品列表”，而不是先看一堆运营摘要。

### 4.2 页面上方红框区域信息价值偏低

你圈出来的红框区域包括：

- 页面头部右上角按钮
- 工艺路线总数 / 待锁定路线 / 外协工序 / 差异工序等数据板

这些内容并不是完全没价值，但当前优先级不高，而且会挤压用户真正想看的主体列表区域。

因此按你的要求，本轮建议直接移除：

- 页面顶部操作按钮区
- 主页面指标卡片区

保留筛选栏，直接进入工艺产品列表。

### 4.3 当前主页面更像“路线选中页”，不像“产品工艺入口页”

现在左侧是路线卡片：

- 路线名
- 编码
- 工序数
- 成本
- 风险

但业务上，工程人员很多时候是先想到“产品”，再想到“这条产品下面的工艺路线”。

所以主页面应改成“工艺产品列表”，字段更偏对象级，而不是摘要卡片级。

### 4.4 详情页顶部数据板块占位太重

详情页顶部现在还有一组数据卡片：

- 工序数量
- 工艺成本
- 差异工序
- 当前门禁

这些内容本身应该保留，但不适合继续用四张大卡片横铺。因为：

- 它们不是独立业务模块
- 本质是当前路线的概览摘要
- 会把用户注意力从真正的详情区块打散

更好的方式是：

- 把这些信息收进“概览”区
- 以更紧凑、更对齐的摘要行/信息栅格显示

### 4.5 “查看板块”本质是导航，但层级表达不够自然

现在的 `查看板块` 是一整块独立区域，里面再切：

- 路线概览
- 工序明细
- 确认门禁
- 资料挂接
- 版本变更
- 联动影响

这个思路本身是对的，但目前页面上它有点像一个额外模块，而不是路线详情本身的自然导航。

建议保留切换方式，但增强层级关系：

- 页面名称下方加面包屑式路径字段
- tab 区作为详情主导航

这样用户会更容易理解“自己现在看的是这条路线的哪一个板块”。

---

## 5. 目标页面结构

## 5.1 主页面结构

工艺路线主页面建议调整为：

1. 页面标题
2. 筛选栏（保留现状逻辑）
3. 工艺产品列表

去掉：

- 顶部操作按钮区
- 指标卡片区

### 5.1.1 主页面列表字段

主页面列表按你的要求建议固定为：

- 编号
- 所属产品
- 名字
- 状态
- 进程
- 版本
- 详情

这里的理解建议是：

- `编号`：工艺路线编号，如 `PROC-SC30-A`
- `所属产品`：产品编码 + 产品名称
- `名字`：工艺路线名称
- `状态`：draft / confirmed / locked / released 的业务状态展示
- `进程`：当前门禁 / 当前推进阶段，例如“差异测试验证”“正式发布”
- `版本`：版本号，如 `A` / `B`
- `详情`：进入该路线详情页或切换到详情区的按钮

### 5.1.2 主页面展示方式

建议不要继续用左侧卡片流，而改成标准表格或准表格列表。

推荐形态：

- 一行一个工艺路线对象
- 行高统一
- 文字左对齐
- 状态与进程用标签/状态块展示
- `详情` 用链接按钮或小号主按钮

这样会比卡片更工整，也更适合扫描。

---

## 5.2 详情页结构

进入某条工艺路线后，详情页建议结构如下：

1. 名称区
2. 名称下方的面包屑式路径字段
3. 概览区
4. 详情导航区
5. 各分区内容

### 5.2.1 名称下方面包屑字段

把你说的“查看板块那一个字段以面包屑的形式放在名称下面”，我建议理解为：

在名称下方显示一条更轻量的路径信息，例如：

`工艺路线中心 / 超队 3.0 / 标准工艺路线 / 版本 A`

或：

`PROC-SC30-A / 超队 3.0 / 标准工艺路线 / 版本 A`

作用有两个：

1. 明确当前详情属于哪个对象
2. 让页面导航感更自然，不再像“孤立详情块”

---

## 6. 各区块改造方案

## 6.1 概览区

### 现状

当前是四张数据板卡片：

- 工序数量
- 工艺成本
- 差异工序
- 当前门禁

### 调整建议

去掉这四张卡片，改成概览信息区。

建议放入：

- 工序数量
- 工艺成本
- 差异工序数
- 当前门禁
- 路线类型
- 模板来源
- 负责人
- 继承来源
- 当前状态

### 推荐表现方式

不要再大卡片堆叠，改成：

- 两列或三列信息栅格
- 每项是“标签 + 值”
- 当前状态和当前门禁可用更醒目的标签

这样页面会更整齐，也不会把顶部撑得太高。

---

## 6.2 工序明细区

### 现状

当前工序明细表字段有：

- 顺序
- 工序编码（前端推导）
- 工序名称
- 工序类型
- 执行位置
- 供应商
- 核心参数摘要
- 质量要求
- 单工序成本
- 标记

### 调整建议

保留原有主体字段，同时加两项：

1. 确认人标签
2. 搜索组件

### 6.2.1 确认人标签

建议在每条工序记录里增加：

- 确认人
- 或确认角色标签

表现方式建议：

- 在表格中单独加一列 `确认人`
- 或在 `标记` 列里增加标签组

更推荐单独列，因为更清晰。

建议字段值：

- `工程确认：刘浩`
- `采购确认：李采`
- `品质确认：王质`

如果不想做太宽，可以简化为一个 `确认人` 标签列，内容显示主确认责任人。

### 6.2.2 搜索组件

在工序明细区顶部增加本区专用搜索，不影响页面主筛选栏。

建议搜索范围：

- 工序编码
- 工序名称
- 执行位置
- 供应商

交互建议：

- 放在工序明细表上方右侧
- 小号搜索框，宽度约 220px 到 260px
- 输入即筛选当前工序列表

这样用户在长工序列表里能更快定位工序。

---

## 6.3 资料部分

### 现状

当前资料挂接区是摘要表格，字段主要是：

- 工序
- SOP
- SIP
- 参数表
- 检验标准
- 状态
- 最近更新

### 调整建议

在最左边加两个操作入口：

- 预览
- 添加

建议新增为前两列：

- 操作预览
- 操作添加

或者合并成一个 `操作` 列，里面有两个按钮：

- `预览`
- `添加`

如果你强调“最左边加个预览和添加”，那就建议直接把 `操作` 列放最左。

### 推荐表格顺序

- 操作
- 工序
- SOP
- SIP
- 参数表
- 检验标准
- 状态
- 最近更新

### 预期效果

用户不需要先去别的地方找入口，能直接从当前路线详情给某工序补资料或预览资料。

---

## 6.4 版本变更区

### 现状

当前是变更卡片列表，只展示：

- 版本
- 变更类型
- 变更原因
- 影响工序
- 成本变化
- 交期变化

### 调整建议

在每条路线详情下的版本变更区新增两个动作：

- 敲定
- 变更申请

### 6.4.1 敲定键

含义建议：

- 当前版本变更内容确认完成
- 可以进入锁定/发布前状态

按钮位置建议：

- 版本变更区标题栏右侧
- 或每个变更记录卡片右上角

更推荐标题栏右侧，表示这是对当前版本变更集合的动作。

### 6.4.2 变更申请

含义建议：

- 发起新的工艺版本变更

按钮位置建议：

- 与 `敲定` 同级
- 次级按钮或描边按钮

这样会形成：

- `变更申请`
- `敲定`

两个动作一主一辅，符合业务节奏。

---

## 7. 页面交互重组建议

## 7.1 主页面从分栏改成列表优先

当前页面是：

- 左侧路线列表
- 右侧详情

本轮建议改成：

- 默认显示工艺产品列表
- 点击某一行后进入详情视图

实现上可以有两种方案：

### 方案 A：同页切换

- 主列表和详情仍在同一页面
- 点击 `详情` 后，把列表区收起或切到详情模式

优点：

- 复用当前 `/processes`

### 方案 B：伪双页结构

- `/processes` 显示列表
- `/processes?routeId=xxx` 显示详情态

优点：

- 路径更清晰
- 仍不需要新增真正的新路由文件

本轮前端重构我更推荐 **方案 B**，因为它最稳，也最符合你当前代码结构。

---

## 7.2 详情导航保持，但视觉层级更轻

`路线概览 / 工序明细 / 确认门禁 / 资料挂接 / 版本变更 / 联动影响`

这组导航仍建议保留，但改法如下：

1. 不再叫“查看板块”
2. 放在概览区下面，作为路线详情主导航
3. 保持整行对齐
4. 高亮当前项

这会比现在更自然。

---

## 8. 需要修改的代码文件

## 8.1 `plm-web/src/views/process/ProcessCenterView.vue`

这是本次改造的主文件，也是主要改动点。

需要调整的内容：

1. 去掉页面顶部 actions 区域
2. 去掉主页面 metrics 数据板区
3. 保留筛选栏与时间范围筛选
4. 把左侧路线卡片列表改成工艺产品表格列表
5. 把详情顶部数据卡片改成概览信息区
6. 名称下方增加面包屑式路径字段
7. 工序明细区增加局部搜索组件
8. 工序明细表增加确认人字段
9. 资料挂接表最左增加 `预览 / 添加`
10. 版本变更区增加 `敲定 / 变更申请`

### 建议改动结构

当前：

- `metric-grid`
- `SearchBar`
- `split-grid process-grid`
- 左侧 `route-list-panel`
- 右侧 `detail-panel`

建议改为：

- 页面标题
- `SearchBar`
- 工艺产品列表区
- 条件进入详情区

详情区再分：

- 详情头部
- 面包屑路径
- 概览区
- 导航 tabs
- 内容区

---

## 8.2 `plm-web/src/types/process.ts`

需要补充或调整的字段建议：

### `ProcessRouteListItem`

建议确保主列表字段完整表达：

```ts
export interface ProcessRouteListItem {
  routeId: number
  routeCode: string
  productId: number
  productCode: string
  productName: string
  routeName: string
  status: CommonStatus
  currentGate: string
  versionNo: string
  targetPath: string
}
```

虽然现在已经有大部分字段，但文档层面建议明确主列表只围绕这些字段展示。

### `ProcessOperationRecord`

建议新增：

```ts
confirmerName?: string
confirmerRole?: string
```

### `ProcessAttachmentSummary`

如果资料区要支持更直观操作，建议为后续前端按钮预留：

```ts
previewPath?: string
canAdd?: boolean
```

### `ProcessChangeRecord`

如果版本区要承接按钮动作，建议为交互预留：

```ts
canFinalize?: boolean
canApplyChange?: boolean
```

---

## 8.3 `plm-web/src/mock/process.ts`

这里需要配合页面重构补数据。

### 主列表 mock

要确保每条路线有下面这些主列表字段：

- routeCode
- productName
- routeName
- status
- currentGate
- versionNo

### 工序明细 mock

要给每条工序补：

- confirmerName
- confirmerRole

例如：

```ts
{
  operationId: 21,
  operationCode: 'PROC-SC30-A-010',
  operationName: '注塑成型',
  confirmerName: '刘浩',
  confirmerRole: '工程确认'
}
```

### 资料区 mock

建议在每条资料记录上补充：

```ts
previewPath: '/files?operationId=21'
canAdd: true
```

### 版本变更 mock

建议在详情级别加动作控制数据：

```ts
canFinalize: true
canApplyChange: true
```

---

## 8.4 `plm-web/src/api/modules/process.ts`

当前如果只是 mock API，这里通常不需要大改逻辑，但需要保证返回结构跟页面新字段一致。

如果后续把详情态和列表态拆开，也可以考虑接口层表达为：

- 获取工艺路线列表
- 获取单条工艺路线详情

但本轮文档阶段先不要求真的拆。

---

## 9. 页面效果预期

## 9.1 主页面效果

改完后主页面应更像“工艺路线清单页”，而不是“摘要 + 卡片拼接页”。

用户会先看到：

- 标题
- 筛选栏
- 工艺路线列表表格

这样页面第一眼更清楚，也更工整。

## 9.2 详情页面效果

改完后详情页顶部更轻、更整齐：

- 名称
- 面包屑路径
- 概览信息栅格
- 板块导航

不再有四张横向数据板把内容压下去。

## 9.3 工序明细效果

工序明细会更适合协作：

- 能搜工序
- 能看到确认人
- 表头更完整
- 不需要来回翻找责任归属

## 9.4 资料区效果

资料区会更像真实工作入口：

- 左侧直接 `预览`
- 左侧直接 `添加`
- 不再只是被动看数量

## 9.5 版本变更效果

版本区会更像可执行模块：

- 可以发起变更申请
- 可以完成后敲定

而不是只展示历史文字。

---

## 10. 建议的代码改法示例

下面这些不是正式落库代码，只是帮助你理解后续会怎么改。

### 10.1 主页面列表结构示意

```vue
<el-table :data="routeRows" border stripe>
  <el-table-column prop="routeCode" label="编号" min-width="160" />
  <el-table-column label="所属产品" min-width="220">
    <template #default="{ row }">
      <div class="cell-stack">
        <strong>{{ row.productName }}</strong>
        <span class="subtle-text">{{ row.productCode }}</span>
      </div>
    </template>
  </el-table-column>
  <el-table-column prop="routeName" label="名字" min-width="220" />
  <el-table-column prop="status" label="状态" width="120" />
  <el-table-column prop="currentGate" label="进程" min-width="160" />
  <el-table-column prop="versionNo" label="版本" width="90" />
  <el-table-column label="详情" width="100">
    <template #default="{ row }">
      <el-button link type="primary" @click="openRouteDetail(row)">详情</el-button>
    </template>
  </el-table-column>
</el-table>
```

### 10.2 详情页名称下路径示意

```vue
<div class="detail-header">
  <h3 class="section-title">{{ activeDetail.routeName }}</h3>
  <p class="detail-breadcrumb">
    {{ activeDetail.routeCode }} / {{ activeDetail.productName }} / 版本 {{ activeDetail.versionNo }}
  </p>
</div>
```

### 10.3 工序明细搜索示意

```vue
<div class="toolbar-row">
  <div>
    <h3 class="section-title">工序明细</h3>
  </div>
  <el-input
    v-model="operationKeyword"
    clearable
    placeholder="搜索工序编码 / 名称 / 位置 / 供应商"
    class="operation-search"
  />
</div>
```

### 10.4 工序确认人列示意

```vue
<el-table-column label="确认人" min-width="140">
  <template #default="{ row }">
    <div class="cell-stack">
      <strong>{{ row.confirmerName || '--' }}</strong>
      <span class="subtle-text">{{ row.confirmerRole || '未指定' }}</span>
    </div>
  </template>
</el-table-column>
```

### 10.5 资料区最左操作列示意

```vue
<el-table-column label="操作" width="140" fixed="left">
  <template #default="{ row }">
    <div class="table-actions">
      <el-button link type="primary">预览</el-button>
      <el-button link type="success">添加</el-button>
    </div>
  </template>
</el-table-column>
```

### 10.6 版本变更按钮示意

```vue
<div class="toolbar-row">
  <div>
    <h3 class="section-title">版本变更</h3>
  </div>
  <div class="header-actions">
    <el-button plain>变更申请</el-button>
    <el-button type="primary">敲定</el-button>
  </div>
</div>
```

---

## 11. 风险点

### 11.1 主页面改成列表后，详情入口要足够顺

如果只改成表格，但详情切换不顺，用户会觉得多点了一层。所以实施时必须把 `详情` 按钮和回退路径设计清楚。

### 11.2 详情信息不能再重新堆成另一种卡片墙

虽然你要求去掉数据板，但如果概览区又堆成新的大块摘要，页面还是会显乱。实施时需要坚持紧凑信息栅格。

### 11.3 工序搜索只做局部筛选

它应该只影响工序明细区，不应影响主页面筛选栏，否则用户会混淆两个搜索作用域。

### 11.4 本轮只改工艺路线模块

和产品详情、BOM、文件中心的跳转关系可以保留现有路径，但不要顺手改其他模块页面结构。

---

## 12. 交付结论

这次工艺路线模块前端改造，核心不是“多加几个按钮”，而是把页面结构从“摘要堆叠页”改成“对象列表 + 清晰详情页”。

本轮建议的最关键变化有六个：

1. 主页面去掉顶部红框区，只保留筛选栏和工艺产品列表
2. 主页面改为工艺路线列表表格，不再以路线卡片为主
3. 详情页去掉顶部数据卡片，把信息收进概览区
4. 名称下面增加面包屑式路径字段
5. 工序明细加确认人列和局部搜索
6. 资料区加预览/添加，版本区加敲定/变更申请

这样改完后，页面会更工整、扫描效率更高，也更符合工程、采购、品质协作时的使用习惯。
