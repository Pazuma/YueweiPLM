# 2026-08-04 PLM 颜色编码中西文 Excel 同步沉淀

## 1. 本次目标

将以下两份颜色编码 Excel 对齐到 PLM 系统颜色字典：

| 文件 | 用途 |
| --- | --- |
| `D:\Users\46733\Downloads\Códigos de color-2026-08-04.xlsx` | 西语表头颜色编码源文件 |
| `D:\Users\46733\Downloads\颜色编码-2026-08-04.xlsx` | 中文表头颜色编码源文件 |
| `D:\Users\46733\Downloads\Códigos de color-2026-08-04 (2).xlsx` | 西语表头完整颜色编码源文件 |
| `D:\Users\46733\Downloads\颜色编码-2026-08-04 (1).xlsx` | 中文表头完整颜色编码源文件 |

本次不新增颜色字段，不自行翻译颜色名称。原因是两份 Excel 的颜色名称列内容一致，中文文件只把工作表、表头、状态等元数据做了中文化；当前系统表 `plm_code_item` 也只有一个名称字段 `code_name`，没有独立中文名/西语名字段。

## 2. Excel 字段结构

### 2.1 西语文件

| 位置 | 值 |
| --- | --- |
| 工作表 | `Códigos de color` |
| 表头行 | 第 6 行 |
| 表头 | `Código color`、`Nombre color`、`Estado`、`Actualizado` |
| 数据行 | 第 7 行至第 17 行 |
| 状态值 | `Enabled` |

### 2.2 中文文件

| 位置 | 值 |
| --- | --- |
| 工作表 | `颜色编码` |
| 表头行 | 第 6 行 |
| 表头 | `颜色编码`、`颜色名称`、`状态`、`更新时间` |
| 数据行 | 第 7 行至第 17 行 |
| 状态值 | `启用` |

## 3. 系统字段映射

| Excel 字段 | 中文 Excel 字段 | 系统字段 | 规则 |
| --- | --- | --- | --- |
| `Código color` | `颜色编码` | `plm_code_item.code_value` | 按文本保存，例如 `63`、`86` |
| `Nombre color` | `颜色名称` | `plm_code_item.code_name` | 保留 Excel 原值，不自动翻译 |
| `Estado` | `状态` | `plm_code_item.status` | `Enabled` / `启用` 映射为 `enabled` |
| `Actualizado` | `更新时间` | 不直接落库 | 当前导入接口使用系统提交时间更新 `updated_at` |
| 文件顺序 | 文件顺序 | `plm_code_item.sort_order` | 当前导入器按本次文件行序写入 |

## 4. 本次同步数据

| 颜色编码 | 颜色名称 | 状态 | 本次动作 |
| --- | --- | --- | --- |
| `63` | `Navy` | `enabled` | 更新 |
| `64` | `Light Pink` | `enabled` | 更新 |
| `65` | `Marvin Chang` | `enabled` | 更新 |
| `66` | `Azul Cielo` | `enabled` | 更新 |
| `67` | `resistente-acrílico` | `enabled` | 更新 |
| `68` | `resistente-TPU` | `enabled` | 更新 |
| `69` | `superficie brillante` | `enabled` | 更新 |
| `83` | `Leopardo rosa` | `enabled` | 更新 |
| `84` | `Azulejo verde` | `enabled` | 更新 |
| `85` | `Rayas negras y blancas` | `enabled` | 更新 |
| `86` | `Chicle Rosa` | `enabled` | 更新 |

## 5. 同步结果

通过当前运行系统接口完成正式提交：

| 项目 | 结果 |
| --- | --- |
| 预览接口 | `POST /api/v1/code-items/import/preview` |
| 提交接口 | `POST /api/v1/code-items/import/{token}/commit` |
| 新增数 | 0 |
| 更新数 | 11 |
| 错误数 | 0 |
| 提交数 | 11 |
| 提交时间 | `2026-08-04 10:33:58` |

系统回查确认 11 条颜色编码均为 `enabled`，其中 `67` 已修正为正确 UTF-8 名称 `resistente-acrílico`。

## 6. 中文模板兼容代码

本次同步前，运行中的后端只接受西语模板；直接上传中文文件返回：

```text
400 / 缺少工作表：Códigos de color
```

已在源码中补充中文模板兼容：

| 文件 | 变更 |
| --- | --- |
| `plm-server/src/main/java/com/yuewei/plm/module/code/service/CodeItemImportService.java` | 支持工作表 `Códigos de color` / `颜色编码`；支持西语/中文表头；支持 `Enabled`、`Disabled`、`启用`、`停用`、`禁用` 状态 |
| `plm-server/src/test/java/com/yuewei/plm/module/code/service/CodeItemImportServiceTest.java` | 增加中文工作簿导入预览测试 |

验证命令：

```powershell
cd D:\work\Yuewei\plm-server
.codex_tmp\apache-maven-3.9.9\bin\mvn.cmd -Dtest=CodeItemImportServiceTest -DforkCount=0 test
```

测试结果：`Tests run: 3, Failures: 0, Errors: 0, Skipped: 0`。

## 7. 注意事项

1. 数据库颜色字典已经同步完成，不需要再重复提交同一份数据。
2. 当前 8080 后端进程尚未加载中文模板兼容代码；需要重建或重启后端后，中文 Excel 才能直接通过导入预览接口。
3. 如果后续需要同时维护中文名与西语名，应扩展 `plm_code_item` 或新增多语言字典表，不能继续复用单一 `code_name` 字段承载多语言。

## 8. 二次同步：完整 71 条文件

用户补充提供以下两份完整文件：

| 文件 | 工作表 | 数据行 | 说明 |
| --- | --- | ---: | --- |
| `D:\Users\46733\Downloads\Códigos de color-2026-08-04 (2).xlsx` | `Códigos de color` | 71 | 西语表头，状态为 `Enabled` |
| `D:\Users\46733\Downloads\颜色编码-2026-08-04 (1).xlsx` | `颜色编码` | 71 | 中文表头，状态为 `启用` |

两份文件业务数据一致，区别只在工作表名、表头和状态语言。由于当前 8080 后端进程仍未加载中文模板兼容代码，本次通过西语完整文件执行系统导入，中文完整文件作为同源校验依据。

同步预览结果：

| 项目 | 结果 |
| --- | ---: |
| 新增数 | 0 |
| 更新数 | 11 |
| 不变数 | 60 |
| 错误数 | 0 |

正式提交结果：

| 项目 | 结果 |
| --- | ---: |
| 提交数 | 11 |
| 提交时间 | `2026-08-04 10:50:22` |

本次更新的 11 条主要是恢复完整 71 条字典下的排序：`63-69`、`83-86` 的 `sort_order` 已恢复为 `61-71`。系统回查确认当前 `code_type=color` 总数为 71，全部为启用状态；`67` 保持正确名称 `resistente-acrílico`。

## 9. 中文名称字段补充

为区分原名称和中文名称，系统新增了 `plm_code_item.code_name_zh`。

| 字段 | 含义 | 来源 |
| --- | --- | --- |
| `code_name` | 原名称 / 西语名称 | 西语模板 `Nombre color` |
| `code_name_zh` | 中文名称 | 中文模板 `颜色名称` |

当前策略：

1. 既有颜色数据先回填 `code_name_zh = code_name`，保证页面立即可见。
2. 西语 Excel 继续更新 `code_name`。
3. 中文 Excel 只更新 `code_name_zh`，不覆盖原名称。

前端展示已同步为 `编码 / 原名称 / 中文名称 / 状态 / 排序 / 更新时间`。
