# 投产工序确认前后端统一解决方案

## 1. 文档目的

针对项目 `Alterna幻甲 SM S280` 在第 10 步确认时报错：

```text
路线 Alterna幻甲 工艺路线 - SM S280 尚未确认有效投产工序
```

本方案用于统一前端操作、后端校验、投产工序落库和后续 SKU 生成之间的逻辑。

本文件只记录排查结果和拟修改方案，当前不修改业务代码。

## 2. 当前实际状态

项目数据：

| 项目 | 值 |
|---|---|
| projectId | 19240 |
| 项目名称 | Alterna幻甲 SM S280 |
| productType | model_variant |
| 当前步骤 | 10 |
| 当前节点 | MODEL_VARIANT_PROCESS_CONFIRM |
| 正式 BOM | productBomId=62 |
| 正式 BOM 路线 | productBomRouteId=104 |
| 工艺路线 | processId=404 |

工艺路线 `processId=404` 已存在 4 个有效子工序：

```text
409、410、411、412
```

但生产确认接口返回：

```json
{
  "selectedOperationCount": 0,
  "selectedColorCount": 0,
  "routeSelections": [
    {
      "productBomRouteId": 104,
      "operationProcessIds": [],
      "applicableColors": []
    }
  ]
}
```

因此，本次错误的直接原因是：

> 正式 BOM 路线已经确认，但 `plm_process_production_operation_selection` 中没有当前项目、当前 BOM 路线对应的 `confirmed` 有效工序记录。

## 3. 前后端现有逻辑核对

### 3.1 后端第 10 步门禁

文件：

```text
plm-server/src/main/java/com/yuewei/plm/module/project/service/impl/TimelineActionServiceImpl.java
```

当前逻辑：

```java
case "PRODUCT_LINE_PROCESS_CONFIRM", "MODEL_VARIANT_PROCESS_CONFIRM" ->
    productionConfirmationService.requireOperationsConfirmed(projectId);
```

产品线和新型号线都会执行投产工序门禁。

### 3.2 后端“有效投产工序”判定

文件：

```text
plm-server/src/main/java/com/yuewei/plm/module/bom/service/ProductionConfirmationService.java
```

`requireOperationsConfirmed` 的判断条件：

1. 项目存在 active 正式 BOM 路线选择。
2. 路线有效、未删除，且属于当前项目。
3. 投产工序选择记录满足：
   - `product_id = projectId`
   - `product_bom_route_id = routeId`
   - `status = confirmed`
   - `deleted_flag = 0`
4. 工序必须是当前工艺路线的子工序：
   - `operation.parentProcessId = route.processId`
   - 工序未删除。
5. 投产工序记录的路线版本与当前工艺路线版本一致；任一版本为空时兼容通过。

当前后端实际只要求每条 active 正式 BOM 路线至少有一条有效投产工序，不要求必须选择全部子工序。

### 3.3 后端确认接口

接口：

```http
POST /api/v1/projects/{projectId}/production-routes/confirm
```

后端会校验：

1. `operationProcessIds` 不能为空。
2. BOM 必须属于当前项目，状态为 `reviewing` 或 `released`。
3. BOM 路线必须 active，且属于当前 BOM 和项目。
4. 工艺路线必须与 BOM 路线的 `processId` 一致。
5. 每个工序必须是当前工艺路线的子工序。
6. 路线必须有 current 或 baseline 成本快照。
7. 至少存在一个有效适用颜色，且颜色必须属于该 BOM 路线。

确认成功后，后端在同一事务内写入：

```text
plm_product_bom_route_formal_selection
plm_process_production_operation_selection
plm_product_production_color_decision
```

工序记录状态为 `confirmed`。

### 3.4 前端现有提交逻辑

文件：

```text
plm-web/src/views/project/components/ProductionConfirmationDialog.vue
```

前端当前并不是让用户逐条选择子工序，而是自动读取：

```ts
const operationProcessIds =
  group.process.operations.map(operation => operation.processId)
```

然后把当前路线的全部子工序提交给后端。

这与后端“允许至少一条有效工序”的判定并不矛盾，但界面表达不准确：界面展示了“工序名称”，实际没有工序选择控件。

### 3.5 已发现的前端入口不一致

文件：

```text
plm-web/src/views/project/ProjectCenterView.vue
```

当前按钮只判断：

```vue
node.nodeKey === 'PRODUCT_LINE_PROCESS_CONFIRM'
```

但后端第 10 步同时使用：

```text
PRODUCT_LINE_PROCESS_CONFIRM
MODEL_VARIANT_PROCESS_CONFIRM
```

这意味着新型号线项目可能无法从当前项目详情页正常打开“敲定投产工序”对话框。`Alterna幻甲 SM S280` 正是新型号线项目，属于该不一致场景。

## 4. 解决方案

采用“保留第 10 步工序确认语义，但统一前后端入口和提交契约”的方案。

不建议仅在前端绕过错误，也不建议只删除后端门禁。原因是：

- 后续颜色确认要求颜色关联 active 正式 BOM 路线；
- 工序确认接口同时建立正式路线、工序和适用颜色的关联；
- 只取消门禁会让项目可以跳过工序确认，但会保留空的投产确认数据；
- 审计上无法区分“用户确认了工序”和“系统跳过了工序”。

### 4.1 前端统一第 10 步入口

修改文件：

```text
plm-web/src/views/project/ProjectCenterView.vue
```

拟修改逻辑：

```ts
const processConfirmNodeKeys = new Set([
  'PRODUCT_LINE_PROCESS_CONFIRM',
  'MODEL_VARIANT_PROCESS_CONFIRM'
])
```

模板统一使用：

```vue
<el-button
  v-if="processConfirmNodeKeys.has(node.nodeKey)"
  size="small"
  type="primary"
  plain
  @click="openProductionConfirmation('operations')"
>
  敲定投产工序
</el-button>
```

目标：

- 产品线和新型号线都能打开同一个投产工序确认对话框；
- 不再出现后端有门禁、前端没有操作入口的情况。

### 4.2 前端明确展示工艺路线下的子工序

修改文件：

```text
plm-web/src/views/project/components/ProductionConfirmationDialog.vue
```

拟修改逻辑：

1. 根据 `process.operations` 展示所有子工序。
2. 使用稳定的 `processId` 作为工序选择值。
3. 默认选中当前路线下已确认的工序。
4. 新确认时默认选中全部有效子工序，保持现有自动确认体验。
5. 用户可以取消不投产的子工序。
6. 如果某条路线最终没有选中工序，前端禁止提交并提示具体路线。

提交参数保持统一：

```json
{
  "routes": [
    {
      "processId": 404,
      "productBomId": 62,
      "productBomRouteId": 104,
      "operationProcessIds": [409, 410, 411, 412],
      "applicableColors": [
        {
          "codeItemId": 1,
          "colorCode": "31",
          "colorName": "透明"
        }
      ]
    }
  ],
  "remark": "确认投产工序、使用 BOM 与适用颜色"
}
```

### 4.3 后端补强确认接口的数据一致性

修改文件：

```text
plm-server/src/main/java/com/yuewei/plm/module/bom/service/ProductionConfirmationService.java
```

拟修改逻辑：

1. 对 `operationProcessIds` 去重并按工艺路线顺序保存。
2. 后端继续强制校验工序必须属于当前路线，不能信任前端传入的 ID。
3. 后端返回每条路线的：
   - 已确认工序 ID；
   - 工序名称；
   - 工序数量；
   - 适用颜色。
4. 对已经确认过的路线重新确认时，先归档旧批次，再保存新批次，保持当前批次语义。
5. 错误信息明确到路线和缺失原因，例如：

```text
路线 xxx 尚未选择投产工序
路线 xxx 缺少适用颜色
路线 xxx 缺少有效成本快照
```

### 4.4 第 10 步门禁保持与确认数据一致

修改文件：

```text
plm-server/src/main/java/com/yuewei/plm/module/project/service/impl/TimelineActionServiceImpl.java
```

保持以下逻辑：

```java
case "PRODUCT_LINE_PROCESS_CONFIRM", "MODEL_VARIANT_PROCESS_CONFIRM" ->
    productionConfirmationService.requireOperationsConfirmed(projectId);
```

只有用户在对话框中提交成功并生成 `confirmed` 工序记录后，才允许推进第 10 步。

如果业务最终决定取消门禁，应单独设计“系统跳过确认”的审计事件和后续数据补全机制，不能只删除这一行调用。

## 5. SKU 生成逻辑

颜色确认接口仍为：

```http
POST /api/v1/projects/{projectId}/production-colors/confirm
```

后端确认颜色后会调用：

```java
syncModelVariantConfirmedColorsAndSkus(project)
```

对于 `model_variant` 项目：

1. 读取当前项目已确认颜色。
2. 按颜色查找已有 SKU。
3. 没有 SKU 时创建新 SKU。
4. 已存在时更新颜色及相关编码字段。
5. 将颜色确认记录关联到创建出的 SKU。

因此，统一方案不会破坏不同颜色 SKU 自动生成。前提是：

- 第 10 步完成正式 BOM 路线和适用颜色确认；
- 后续颜色确认步骤提交有效颜色；
- 颜色仍然关联已确认的正式 BOM 路线。

## 6. 拟修改文件清单

| 文件 | 修改内容 |
|---|---|
| `plm-web/src/views/project/ProjectCenterView.vue` | 同时支持产品线和新型号线第 10 步入口 |
| `plm-web/src/views/project/components/ProductionConfirmationDialog.vue` | 明确展示并选择子工序，提交有效工序 ID |
| `plm-web/src/api/modules/bom.ts` | 统一请求类型和响应类型，补充明确的工序确认字段 |
| `plm-server/src/main/java/com/yuewei/plm/module/bom/service/ProductionConfirmationService.java` | 强化工序去重、顺序和错误信息，保持服务端最终校验 |
| `plm-server/src/main/java/com/yuewei/plm/module/project/service/impl/TimelineActionServiceImpl.java` | 保持两个项目类型的第 10 步门禁一致 |
| `plm-server/src/test/java/com/yuewei/plm/module/bom/service/ProductionConfirmationServiceTest.java` | 增加产品线、新型号线、多路线、版本不匹配和空工序测试 |
| `plm-server/src/test/java/com/yuewei/plm/module/project/service/impl/TimelineActionServiceImplTest.java` | 增加新型号线第 10 步门禁测试 |
| `plm-web/src/views/project/__tests__/ProductionConfirmationDialog.spec.ts` | 增加子工序选择、路线选择、颜色选择和提交参数测试 |

## 7. 目标效果

完成后应满足：

1. 产品线和新型号线都能从当前第 10 步打开确认窗口。
2. 页面明确显示当前工艺路线的全部子工序。
3. 用户可确认全部或部分有效投产工序。
4. 前端不会提交空工序或空适用颜色。
5. 后端不会接受不属于当前路线的工序。
6. 成功确认后，接口返回的 `operationProcessIds` 不为空。
7. 第 10 步确认不再出现“尚未确认有效投产工序”。
8. 后续确认不同颜色时，能够继续自动创建或更新对应 SKU。
9. 重复确认不会产生重复有效记录，旧批次会被归档。
10. 审计记录能够区分用户确认的 BOM 路线、工序和适用颜色。

## 8. 验收用例

### 用例一：当前项目

项目 `19240`：

- 正式 BOM 路线 `104`；
- 工艺路线 `404`；
- 子工序 `409/410/411/412`；
- 至少选择一个适用颜色；
- 提交投产工序确认；
- 第 10 步确认成功。

### 用例二：部分工序

只提交 `[409, 410]`：

- 后端应接受；
- 第 10 步应允许推进；
- 后续颜色确认不应因为未选择全部子工序而失败。

### 用例三：非法工序

提交不属于 `processId=404` 的工序：

- 后端必须拒绝；
- 不得写入正式确认记录。

### 用例四：不同颜色 SKU

确认颜色 `31`、`32`：

- 每个颜色生成或复用一个 SKU；
- 重复提交同一颜色不得创建重复 SKU。

### 用例五：路线版本变化

旧路线版本的工序确认记录不能误通过新路线版本门禁；版本为空的历史兼容记录按现有兼容规则处理。

## 9. 结论

当前错误的根本问题是：

> 后端要求确认投产工序，但新型号线前端没有提供与后端一致的第 10 步确认入口；同时前端工序界面没有真实呈现工序选择状态，导致正式 BOM 路线和实际投产工序确认记录脱节。

推荐实施“前后端统一确认契约”方案：前端补齐新型号线入口并明确子工序选择，后端继续作为最终校验方，确认成功后再进入第 10 步和后续颜色 SKU 流程。

## 10. 实施结果

本次已按本方案完成代码修改。

### 10.1 已修改文件

```text
plm-web/src/views/project/ProjectCenterView.vue
plm-web/src/views/project/components/ProductionConfirmationDialog.vue
plm-web/src/views/project/__tests__/ProductionConfirmationDialog.spec.ts
plm-server/src/main/java/com/yuewei/plm/module/bom/service/ProductionConfirmationService.java
```

### 10.2 已实现内容

1. 项目详情页第 10 步入口同时支持：
   - `PRODUCT_LINE_PROCESS_CONFIRM`
   - `MODEL_VARIANT_PROCESS_CONFIRM`
   - 后端节点名称为“敲定工序”时也能识别。
2. 投产工序对话框真实展示工艺路线下的子工序。
3. 子工序支持逐项勾选。
4. 已有确认记录时默认恢复已确认工序。
5. 没有确认记录时默认选中全部有效子工序，保持原有操作效率。
6. 前端提交前过滤不属于当前工艺路线的工序 ID。
7. 后端保存工序时按 `sequenceNo` 和 `processId` 排序，确保投产工序顺序稳定。
8. 更新前端单测，覆盖已有工序恢复和工序提交参数。
9. 项目中心 URL 带 `productId` 但列表接口没有返回该项目时，会兜底调用 `/projects/{productId}` 打开详情，避免从工作台或外部链接进入时被空列表阻断。
10. 新型号线详情流程区已补齐“敲定投产工序”按钮；产品线流程区和新型号流程区共用同一套节点识别逻辑。

第 10 步后端门禁仍然保留，未通过有效投产工序确认时不会允许推进。

### 10.3 验证结果

已通过：

```text
npm run type-check
npm run build
npm run test -- --run src/views/project/__tests__/ProductionConfirmationDialog.spec.ts
mvn -o -Dtest=ProductionConfirmationServiceTest,TimelineActionServiceImplTest test
git diff --check
```

测试结果：

```text
前端类型检查：通过
前端生产构建：通过
投产确认对话框单测：4 passed
后端相关单测：27 tests，0 failures，0 errors
```

### 10.4 Playwright 验证说明

`npx playwright install chromium` 仍存在下载超时，因此本次改用本机 Chrome：

```text
C:\Program Files\Google\Chrome\Application\chrome.exe
```

验证账号：

```text
engineer01 / plm123456
```

验证地址：

```text
http://localhost:5174/#/projects?tab=in_progress&productId=19240&section=project_flow
```

验证结果：

1. 登录成功。
2. 项目中心列表接口一开始返回空列表时，仍可通过 `productId=19240` 兜底打开“新型号详情”。
3. 第 10 步 `MODEL_VARIANT_PROCESS_CONFIRM` / “敲定工序”可见。
4. 新型号流程区出现“敲定投产工序”按钮。
5. 点击按钮后打开“敲定工序：确认使用 BOM 与工序名称”弹窗。
6. 弹窗内可见真实子工序：
   - `Alterna幻甲打印PC`
   - `Alterna幻甲TPU`
   - `Alterna幻甲PC`
   - `Alterna幻甲打印组装`
7. 未执行提交写库动作，避免在浏览器验收中改动当前业务数据。

截图留存：

```text
D:\work\Yuewei\plm-server\.codex_tmp\playwright-local-chrome-5174-model-variant-process-button-fixed.png
D:\work\Yuewei\plm-server\.codex_tmp\playwright-local-chrome-5174-model-variant-process-confirmation-dialog-fixed.png
```

### 10.5 开发提示词引用

本次实施前已读取：

```text
D:\work\资料\PLM\docs\文件沉淀\开发提示词.md
D:\work\Yuewei\docs\文件沉淀\2026-08-07-投产工序确认前后端统一解决方案.md
```

用户指定的无扩展名路径 `D:\work\资料\PLM\docs\文件沉淀\开发提示词` 不存在，实际读取的是同目录下的 `开发提示词.md`。

## 11. 工作台流程入口迁移方案

### 11.1 背景

当前“敲定投产工序”入口已经在项目详情弹窗的新型号流程区可用，但用户的主要操作路径是工作台首页的“项目进度”弹窗。实际截图显示第 10 步“敲定工序”位于工作台流程弹窗中，底部操作区仍展示“确认当前节点 / 返回上一步 / 强制推进 / 新建工艺路线 / 上传节点资料”。

这会造成操作认知不一致：业务用户在工作台看到第 10 步，却找不到主操作“敲定投产工序”，只能进入项目详情后再操作。

### 11.2 目标效果

将“敲定投产工序”按钮迁移到工作台“项目进度”弹窗的流程操作区，作为第 10 步“敲定工序”的主操作入口。

目标交互：

1. 工作台点击项目卡，打开“项目进度”弹窗。
2. 当前小节点为第 10 步 `MODEL_VARIANT_PROCESS_CONFIRM` / `PRODUCT_LINE_PROCESS_CONFIRM` / 节点名“敲定工序”时，底部显示“敲定投产工序”。
3. 点击后直接打开现有 `ProductionConfirmationDialog`。
4. 弹窗内展示使用 BOM、投产工序、适用颜色。
5. 确认后刷新工作台时间轴和项目列表。
6. 工作台不再把第 10 步误判为“新建工艺路线”。

### 11.3 修改文件

```text
plm-web/src/views/dashboard/DashboardView.vue
plm-web/src/views/project/components/ProductionConfirmationDialog.vue
plm-web/src/views/dashboard/__tests__/DashboardView.spec.ts
docs/文件沉淀/2026-08-07-投产工序确认前后端统一解决方案.md
```

说明：

- `ProductionConfirmationDialog.vue` 继续作为共用组件，不另写工作台专用确认逻辑。
- 工作台流程弹窗必须具备完整入口。
- 后端接口不需要新增，仍复用投产确认接口。

### 11.4 前端逻辑设计

在 `DashboardView.vue` 中新增投产工序确认弹窗状态：

```ts
const productionConfirmationVisible = ref(false)
const productionConfirmationMode = ref<'operations' | 'colors'>('operations')
```

复用节点识别集合：

```ts
const processConfirmationNodeKeys = new Set([
  'PRODUCT_LINE_PROCESS_CONFIRM',
  'MODEL_VARIANT_PROCESS_CONFIRM'
])
```

新增识别函数：

```ts
function isProcessConfirmationStep(step?: { nodeKey?: string; stepNo?: number; title?: string; stepName?: string; nodeName?: string } | null) {
  if (!step) return false
  const key = step.nodeKey || ''
  const name = step.title || step.stepName || step.nodeName || ''
  return processConfirmationNodeKeys.has(key)
    || key.endsWith('_PROCESS_CONFIRM_STEP')
    || name.includes('敲定投产工序')
    || name.includes('敲定工序')
}
```

将工艺路线创建和工序确认拆开：

```ts
function isProcessRouteCreateStep(step?: { nodeKey?: string; stepNo?: number; title?: string; stepName?: string; nodeName?: string } | null) {
  if (!step) return false
  const name = step.title || step.stepName || step.nodeName || ''
  return step.nodeKey === 'PRODUCT_LINE_PROCESS_ADD'
    || step.nodeKey === 'MODEL_VARIANT_PROCESS_PLAN'
    || step.stepNo === 9
    || (step.stepNo === 7 && name.includes('工艺'))
}
```

底部按钮目标：

```vue
<el-button
  v-if="isCurrentProcessConfirmationStep"
  data-test="dashboard-production-confirmation-open"
  type="primary"
  plain
  @click="openProductionConfirmationFromDashboard"
>
  敲定投产工序
</el-button>

<el-button
  v-if="isCurrentProcessRouteCreateStep"
  data-test="dashboard-process-route-create"
  type="primary"
  :icon="Plus"
  @click="openProcessRouteCreateFromDashboard"
>
  新建工艺路线
</el-button>
```

弹窗挂载目标：

```vue
<ProductionConfirmationDialog
  v-if="activeProgressProject"
  v-model="productionConfirmationVisible"
  :project-id="activeProgressProject.productId"
  :mode="productionConfirmationMode"
  @confirmed="handleProductionConfirmationConfirmedFromDashboard"
/>
```

确认后刷新：

```ts
async function handleProductionConfirmationConfirmedFromDashboard() {
  await refreshActiveProgressProject()
}
```

### 11.5 对话框显示修正

当前截图显示弹窗已打开，但“使用 BOM”和“投产工序”行只看到复选框，文字没有显示。已定位为 `ProductionConfirmationDialog.vue` 中外层 `<label>` 嵌套 Element Plus `<el-checkbox>` 的结构风险。

目标修正：

- BOM 行、工序行、颜色行使用普通 `div` 容器。
- 文本列使用稳定 grid 布局。
- 工序名称、工序编码、BOM 版本、物料数、成本都必须可见。

### 11.6 后端逻辑

无需新增后端接口。工作台入口仍调用：

```text
GET /api/v1/projects/{projectId}/production-confirmation
POST /api/v1/projects/{projectId}/production-confirmation/routes
```

后端仍负责最终校验：

- BOM 必须是当前项目可确认 BOM。
- BOM 路线必须 active。
- 投产工序必须属于当前工艺路线。
- 至少选择一道投产工序。
- 至少选择一个适用颜色。
- 第 10 步推进前仍校验已确认有效投产工序。

### 11.7 验收标准

1. 工作台项目进度弹窗中，第 10 步“敲定工序”底部可见“敲定投产工序”按钮。
2. 第 9 步“加工艺”仍显示“新建工艺路线”，第 10 步不再显示“新建工艺路线”。
3. 点击“敲定投产工序”后打开现有确认弹窗。
4. 弹窗内可见候选 BOM 名称、物料数、成本。
5. 弹窗内可见投产子工序名称和工序编码。
6. 确认后工作台当前项目时间轴刷新。
7. `npm run type-check` 通过。
8. `npm run test -- --run src/views/dashboard/__tests__/DashboardView.spec.ts src/views/project/__tests__/ProductionConfirmationDialog.spec.ts` 通过。
9. Playwright 使用本机 Chrome 验证工作台入口、弹窗显示和不提交写库场景。

### 11.8 实施注意事项

- 不复制投产确认业务逻辑，统一复用 `ProductionConfirmationDialog`。
- 不在工作台直接调用确认接口手写 payload，避免工作台和项目详情逻辑分叉。
- 不取消后端门禁，第 10 步仍由后端确认记录兜底。
- 浏览器验收只打开弹窗和检查内容，不点击最终确认按钮，避免污染当前业务数据。

### 11.9 本次实施结果

实施日期：2026-08-07。

已按本节方案完成工作台流程入口迁移，涉及文件：

```text
plm-web/src/views/dashboard/DashboardView.vue
plm-web/src/views/dashboard/__tests__/DashboardView.spec.ts
docs/文件沉淀/2026-08-07-投产工序确认前后端统一解决方案.md
```

代码修改内容：

1. 工作台 `DashboardView.vue` 引入并复用 `ProductionConfirmationDialog`，不新增工作台专用投产确认业务逻辑。
2. 新增 `productionConfirmationVisible`、`productionConfirmationMode` 状态，工作台第 10 步点击后以 `operations` 模式打开投产确认弹窗。
3. 将“新建工艺路线”和“敲定投产工序”的节点判断拆开：
   - 第 9 步 / `PRODUCT_LINE_PROCESS_ADD` / `MODEL_VARIANT_PROCESS_PLAN` 显示“新建工艺路线”。
   - 第 10 步 / `PRODUCT_LINE_PROCESS_CONFIRM` / `MODEL_VARIANT_PROCESS_CONFIRM` / 节点名“敲定工序”显示“敲定投产工序”。
4. 修正工作台小步骤 `uploadLabel`：只有工艺路线创建步骤显示“新建工艺路线”，第 10 步不再被误标为新建工艺路线。
5. 投产确认弹窗 `confirmed` 后调用 `refreshActiveProgressProject()`，刷新当前项目时间轴和工作台进行中项目列表。
6. Dashboard 单测增加 `ProductionConfirmationDialog` stub，覆盖第 9 步和第 10 步按钮互斥、弹窗打开、确认后刷新。

目标效果：

1. 用户在工作台打开“项目进度”弹窗后，第 10 步“敲定工序”底部可直接看到“敲定投产工序”按钮。
2. 点击按钮打开现有投产确认弹窗，继续使用同一套 BOM、工序、颜色确认逻辑。
3. 第 9 步仍保留“新建工艺路线”，第 10 步不再出现“新建工艺路线”误导入口。
4. 确认成功后工作台自动刷新，后端第 10 步门禁仍以已确认投产工序记录为准。

验证结果：

```text
npm run type-check
通过

npm run test -- --run src/views/dashboard/__tests__/DashboardView.spec.ts src/views/project/__tests__/ProductionConfirmationDialog.spec.ts
2 files passed，16 tests passed

git diff --check -- plm-web/src/views/dashboard/DashboardView.vue plm-web/src/views/dashboard/__tests__/DashboardView.spec.ts
未发现空白错误；仅提示工作区文件后续可能由 Git 转为 CRLF。
```

### 11.10 弹窗文字不可见修复

问题现象：

工作台可以打开“敲定工序：确认使用 BOM 与工序名称”弹窗，但“使用 BOM”和“投产工序”区域只显示复选框，BOM 版本、物料数、成本、工序名称和工序编码不可见；“适用颜色”区域文字可见。

原因：

`ProductionConfirmationDialog.vue` 中 BOM 行和工序行的文字写在 `el-checkbox` 外部，位于 `el-checkbox-group` 内。Element Plus 的复选框组样式会影响组内普通文本节点的字体表现，导致外部文本被压到不可见；颜色行的文字写在 `el-checkbox` 默认插槽内部，所以没有同样问题。

修改文件：

```text
plm-web/src/views/project/components/ProductionConfirmationDialog.vue
docs/文件沉淀/2026-08-07-投产工序确认前后端统一解决方案.md
```

修改内容：

```css
.candidate-row, .operation-row, .color-row {
  color: var(--plm-color-text-primary, #1f2937);
  font-size: 14px;
  /* 其余布局属性保持不变 */
}
```

目标效果：

1. “使用 BOM”下显示候选 BOM 版本、物料数量和成本。
2. “投产工序”下显示所有已读取的子工序名称和工序编码。
3. “适用颜色”显示不受影响。
4. 只修复显示层，不改变接口、不改变提交 payload、不改变后端门禁。

验证结果：

```text
npm run type-check
通过

npm run test -- --run src/views/project/__tests__/ProductionConfirmationDialog.spec.ts src/views/dashboard/__tests__/DashboardView.spec.ts
2 files passed，16 tests passed
```

### 11.11 Playwright 复验结果

复验日期：2026-08-07。

验证环境：

```text
前端地址：http://localhost:5173/#/dashboard
浏览器：本机 Chrome，C:\Program Files\Google\Chrome\Application\chrome.exe
账号：engineer01 / plm123456
项目：亮甲Rainbow 2.0 SM S266，projectId=19227
当前节点：第 10 步“敲定工序”
```

验证步骤：

1. Playwright 打开工作台并登录。
2. 点击进行中项目“亮甲Rainbow 2.0 SM S266”。
3. 在工作台“项目进度”弹窗中点击“敲定投产工序”。
4. 读取弹窗 `.production-confirmation` 文本和 `.candidate-row`、`.operation-row` 的 computed style。
5. 截图留存。

验证结论：

弹窗中已能读取并显示：

```text
使用 BOM
候选 BOM BOM-NBA4030000001-002
10 项物料
CNY 0

投产工序
1. Pieza PC de inyección / OP-001
2. Pieza TPU de inyección / OP-002
3. Teñido PC / OP-003
4. Ensamblaje / OP-004
5. Empaque / OP-005

适用颜色
01 · Morado
```

Playwright 读取到的关键样式：

```text
.candidate-row font-size: 14px; color: rgb(48, 49, 51); display: grid
.operation-row font-size: 14px; color: rgb(48, 49, 51); display: grid
```

截图文件：

```text
D:\work\Yuewei\outputs\production-confirmation-dialog-playwright.png
```

说明：

本次 Playwright 复验的是 `http://localhost:5173`。如果用户浏览器仍看不到文字，优先检查是否打开了另一个前端服务端口、页面缓存或旧构建产物；当前 5173 的 DOM 文本和 computed style 已验证正常。

### 11.12 复选框插槽结构修复与重启复验

复验日期：2026-08-07。

用户反馈重启后仍只显示复选框。继续排查后，最终将 BOM 行和工序行的展示文字移入 `el-checkbox` 默认插槽，和“适用颜色”保持同一种 Element Plus 结构。

修改文件：

```text
plm-web/src/views/project/components/ProductionConfirmationDialog.vue
```

修复逻辑：

1. `candidate-row` 不再把 BOM 版本、物料数、成本放在 `el-checkbox` 外部。
2. `operation-row` 不再把工序名称、工序编码放在 `el-checkbox` 外部。
3. BOM 和工序文字统一放入 `el-checkbox` 默认插槽，避免 `el-checkbox-group` 对外部普通文本产生不可见影响。
4. 补充 `.candidate-checkbox`、`.operation-checkbox`、`.candidate-content` 和 `:deep(.el-checkbox__label)` 样式，确保 label 有正常字号、颜色和宽度。

验证结果：

```text
npm run type-check
通过

npm run test -- --run src/views/project/__tests__/ProductionConfirmationDialog.spec.ts src/views/dashboard/__tests__/DashboardView.spec.ts
2 files passed，16 tests passed
```

已重启前端服务：

```text
http://localhost:5173/#/dashboard
Vite 监听进程 PID：22672
```

Playwright 使用本机 Chrome 复验 `dashboard-project-19227`：

```text
D:\work\Yuewei\outputs\production-confirmation-dialog-19227-verified.png
```

Playwright 读取到弹窗文本：

```text
使用 BOM
候选 BOM BOM-NBA4030000001-002
10 项物料
CNY 0

投产工序
1. Pieza PC de inyección / OP-001
2. Pieza TPU de inyección / OP-002
3. Teñido PC / OP-003
4. Ensamblaje / OP-004
5. Empaque / OP-005
```

Playwright 读取到关键样式：

```text
.el-checkbox__label font-size: 14px
.el-checkbox__label color: rgb(48, 49, 51)
.candidate-content / .operation-content width: 776.86px
```
