# 2026-08-05 PLM 上线前项目级代码审查与安全风险沉淀

## 1. 审查说明

- 审查时间：2026-08-05
- 审查范围：`plm-server`、`plm-web`、`plm-database/postgres`、根目录部署配置、仓库临时产物与历史数据文件。
- 执行方式：按 superpower 多视角代码审查方式执行，只读检查代码与配置，不修改业务代码；并行分为后端/安全、前端/旧数据、数据库/部署/仓库卫生三类。
- 已按要求先阅读：`D:\work\资料\PLM\docs\文件沉淀\开发提示词.md`。
- 对齐基线：手机壳制造业 PLM；核心对象限定为 Customer、Product、Order、ProductionOrder、Process、Inventory、Workstation；本期不做 ERP/MES/WMS/CRM/OA/钉钉正式闭环，只允许预留字段、映射、日志框架或接口草案；需满足权限、审批、发布、冻结、下载、操作日志和测试验收要求。
- 未执行项：本次是上线前静态代码审查，未运行全量后端测试、前端构建、数据库迁移演练或安全扫描工具。

## 2. 总体结论

结论：不建议明天直接上生产。当前至少存在 3 类上线阻断风险：认证可被环境变量关闭、钉钉公开回调/出站接口 token 为空时放行、部署配置中存在默认弱口令/默认密钥/临时公网隧道。

建议上线策略：若明天必须交付，只建议在内网受控演示或灰度环境上线；生产上线前必须先修复 P0，完成 P1 最小闭环验证，并冻结导入数据与部署配置。

## 3. P0 上线阻断项

| 类型 | 文件位置 | 问题 | 修改建议 | 修改理由 |
|---|---|---|---|---|
| 安全-认证 | `plm-server/src/main/java/com/yuewei/plm/common/config/SecurityConfig.java:57-62`、`plm-server/src/main/resources/application-prod.yml:15-16` | 生产运行只要 `APP_SECURITY_ENABLED=false`，整个 `/api/v1/**` 直接 `permitAll`；prod 仍读取 `APP_SECURITY_DEV_TOKEN`。 | 生产 profile 强制认证开启，启动时禁止 `APP_SECURITY_ENABLED=false`；删除生产 dev-token 入口；补充启动期配置校验。 | 否则 7 个核心对象、成本、导入导出、发布冻结、附件下载等接口可匿名访问或被开发 token 绕过。 |
| 安全-钉钉回调 | `plm-server/src/main/java/com/yuewei/plm/module/integration/dingtalk/controller/DingTalkApprovalCallbackController.java:31-41`、`plm-server/src/main/resources/application.yml:57-67` | 钉钉审批回调在 Security 层公开，`DINGTALK_CALLBACK_TOKEN` 默认空；token 未配置时回调校验放行，随后会进入审批分发。 | 回调必须校验签名或固定强 token；token 为空时启动失败；本期若不做正式钉钉闭环，禁用 controller 或仅保留不可写草案接口。 | 任意外部请求可伪造审批结果，触发产品线/型号正式入库，越过业务审批边界。 |
| 安全-钉钉出站 | `plm-server/src/main/java/com/yuewei/plm/module/integration/dingtalk/controller/DingTalkOutboundRelayController.java:31-43`、`plm-server/src/main/java/com/yuewei/plm/module/integration/dingtalk/service/DingTalkOutboundRelayService.java:22-27,59-88` | 出站 relay 同样公开且 token 为空时放行；`agree` 动作可按请求参数调用官方审批同意。 | 关闭生产公开 relay；必须接入来源签名、IP 白名单、幂等校验和审批实例归属校验；未配置凭证时启动失败。 | 一旦钉钉凭证可用，未认证请求可代同意正式审批，属于审批门禁绕过。 |
| 部署-敏感配置 | `docker-compose.yml:8,62-68,70` | Compose 默认数据库密码 `plm123`、默认钉钉 appKey/appSecret、默认 `dev-token`、默认 cpolar 临时公网地址。 | 删除所有生产敏感默认值；改为必填环境变量；提交 `.env.example` 只放占位说明；上线前轮换已暴露的钉钉密钥。 | Compose 很可能被直接用于验收/部署，默认值会造成弱口令、密钥泄露和数据外发。 |
| 部署-生产库暴露 | `plm-server/src/main/resources/application-prod.yml:3-4` | prod 配置曾默认连接历史公网数据库地址，默认用户为 `postgres`。 | 删除公网 IP 默认值和超级用户默认值；使用专用最小权限账号；按环境注入数据库地址；上线前轮换密码并检查访问白名单。 | 代码仓库暴露基础设施入口，本地误启 prod 也可能直连真实库造成误操作。 |

## 4. P1 上线前必须修复

| 类型 | 文件位置 | 问题 | 修改建议 | 修改理由 |
|---|---|---|---|---|
| 权限-RBAC | `plm-server/src/main/java/com/yuewei/plm/common/security/SimpleTokenAuthenticationFilter.java:55-60`、全仓未见系统性 `@PreAuthorize`/`hasRole` | 所有登录用户固定授予 `ROLE_ENGINEER`，业务接口缺少角色/对象/项目归属授权。 | 后端按接口落地 RBAC，至少对发布、冻结、审批、成本、导入导出、下载、系统配置加权限注解或服务层授权。 | 前端隐藏菜单不能作为安全边界；当前存在横向越权风险。 |
| 权限-成本 | `plm-server/src/main/java/com/yuewei/plm/module/project/controller/ProjectCostController.java:31-67`、`plm-server/src/main/java/com/yuewei/plm/module/project/service/impl/ProjectCostServiceImpl.java:49-64` | 成本汇总、明细、创建、更新、确认、作废对任意认证用户开放，并返回 `bomCost/rdCost/totalCost`。 | 成本接口加角色权限；敏感字段按权限脱敏；确认人与录入人职责分离；补审计测试。 | 成本是敏感商业信息，泄露或越权修改会直接影响报价和经营数据。 |
| 审计日志 | `plm-server/src/main/java/com/yuewei/plm/module/operationlog/controller/OperationLogController.java:39-51` | 主代码暴露 `/api/v1/operation-logs/test`，可写 `TEST_WRITE` 操作日志。 | 删除或仅 dev profile 暴露；生产操作日志只允许由可信业务服务写入。 | 允许伪造审计日志，会污染发布、冻结、审批、下载追溯链路。 |
| 发布门禁 | `plm-server/src/main/java/com/yuewei/plm/service/impl/ProductServiceImpl.java:353-384` | 发布接口允许请求参数 `riskConfirmed` 与发布动作同次提交，warning 风险可由调用方一次性确认。 | 将风险确认与发布动作拆分为二次确认凭证/审批任务；校验确认人、时间、风险快照。 | 基线要求发布前置门禁与审批可信，当前调用方可直接越过风险确认流程。 |
| 状态机/资料门禁 | `plm-server/src/main/java/com/yuewei/plm/module/project/service/impl/TimelineActionServiceImpl.java:74-109,237-260` | 阶段资料缺失被收集为 `documentWarnings`，不是强阻断。 | 明确每个节点的必填资料；缺关键资料时返回阻断错误；warning 仅用于非关键资料。 | 资料不齐仍可能推进项目节点，影响上线后的流程可信度。 |
| 附件安全 | `plm-server/src/main/java/com/yuewei/plm/module/attachment/controller/AttachmentController.java:74-111`、`plm-server/src/main/java/com/yuewei/plm/module/attachment/vo/AttachmentVO.java:35,69-70`、`plm-server/src/main/java/com/yuewei/plm/infrastructure/storage/LocalStorageClient.java:75-76` | 附件返回固定下载/预览 URL，VO 暴露 `storageKey`，本地存储 `generateDownloadUrl` 仍保留按 `storageKey` 下载旧口径。 | 上线按 MinIO/S3 临时签名 URL 或受控后端下载；VO 不返回内部存储 key；下载必须校验对象权限并记录真实用户。 | 与规范中的临时下载链接不一致，且内部对象 key 泄露会扩大越权下载风险。 |
| 前端-登录默认账号 | `plm-web/src/views/login/LoginView.vue:14-20` | 登录页默认填充并展示 `engineer01/plm123456` 测试账号。 | 上线前清空默认账号密码；移除测试账号提示；后端停用或改密种子账号。 | 明文测试账号会诱导弱口令使用，也会被安全扫描直接命中。 |
| 前端-token | `plm-web/src/stores/user.ts:8,18,40,54`、`plm-web/src/api/request.ts:21-23` | token 长期存储在 `localStorage`，请求时直接拼入 Authorization。 | 优先改 HttpOnly/SameSite Cookie；如短期来不及，至少缩短 token 过期、加 CSP、登出清理、刷新机制和 XSS 检查。 | localStorage token 被 XSS 读取后可长期冒用账号。 |
| 前端-核心接口断裂 | `plm-web/src/api/modules/foundation.ts:17,21,28,39,43,47`、`plm-web/src/api/modules/bom.ts:18`、`plm-web/src/api/modules/dashboard.ts:4` | 多个运行时 API 仍直接 `notConnected()`，核心页面会 reject。 | 上线前接真实接口或隐藏入口；失败时给明确错误、重试和禁用态。 | 基础产品、测试中心、BOM 中心、产品详情、报表、工作台属于可见核心流程，不应空跑。 |
| 前端-权限口径 | `plm-web/src/api/modules/auth.ts:42-49`、`plm-web/src/router/index.ts:387-388`、`plm-web/src/layout/Sidebar.vue:71` | 前端把非超级权限用户固定为“工程”和空权限，菜单/路由依赖前端权限过滤。 | 后端返回真实角色与权限集合；前端只做展示，后端强制接口授权。 | 会导致普通用户不可用，或为了可用被迫发全权限；安全边界不成立。 |
| 数据迁移-历史数据入库 | `plm-server/src/main/resources/db/migration/V20260730_1130__seed_inventory_from_ecount_workbook_and_archive_minor_groups.sql:4,21,8319` | Flyway 迁移中嵌入海量 ECOUNT workbook seed 数据和历史清理逻辑。 | 一次性历史导入从正式 schema 迁移中剥离；提供导入批次、校验报告、回滚脚本和生产执行审批。 | 生产迁移不可审计、不可参数化，失败或误导入会污染 Inventory 主数据。 |
| 数据迁移-绕过发布流程 | `plm-server/src/main/resources/db/migration/V20260805_1000__imported_bom_process_formal_versions.sql:3,12,33` | 迁移批量把历史 BOM/Process 补为 confirmed/frozen/released。 | 上线前业务签字确认数据集；通过应用服务或审批脚本执行；记录发布人、审批单、风险快照。 | 启发式 SQL 直接提升正式版本，绕过发布冻结门禁。 |
| 数据约束 | `plm-server/src/main/resources/db/migration/V20260730_1100__bom_color_variant_group.sql:26,31` | 删除旧唯一索引并调整 BOM 路线/明细唯一边界，未见生产重复数据预检。 | 增加迁移前重复数据检测和失败策略；保留可回滚方案。 | 核心 BOM 一致性约束变化可能导致迁移失败或脏数据合法化。 |
| 部署-仓库卫生 | `plm-server/.codex_tmp/**`、`plm-server/logs/**`、`plm-web/dist-dashboard-check/**`、`outputs/**` | Maven 包、日志、截图报告、构建产物、导入结果在仓库状态中出现。 | 清理出版本库；完善 `.gitignore`；日志和导入结果转入受控存储。 | 临时产物污染发布包，日志/导入文件可能包含令牌、请求、物料和客户数据。 |

## 5. P2/P3 可延期但需登记

| 类型 | 文件位置 | 问题 | 修改建议 | 修改理由 |
|---|---|---|---|---|
| API 暴露面 | `plm-server/src/main/java/com/yuewei/plm/common/config/SecurityConfig.java:52-55` | Swagger UI 与 `/v3/api-docs/**` 默认公开。 | 生产关闭或加管理员认证。 | 避免暴露成本、导入导出、审批等完整接口面。 |
| 健康探测 | `plm-server/src/main/java/com/yuewei/plm/common/config/SecurityConfig.java:48`、`plm-server/src/main/java/com/yuewei/plm/module/health/controller/HealthController.java:51` | `/api/v1/health/**` 全公开，包含 validation probe。 | 只公开最小只读健康检查，validation/probe 改为内部或认证接口。 | 防止外部探测校验规则和错误结构。 |
| 生产依赖默认值 | `plm-server/src/main/resources/application-prod.yml:9,18` | Redis 默认 `localhost`，本地存储根目录无明确生产校验。 | 生产必填校验；容器环境使用服务名或专有配置。 | 防止缓存/会话/附件在生产静默失败。 |
| 消息发布占位 | `plm-server/src/main/java/com/yuewei/plm/infrastructure/mq/MessagePublisher.java:8-11` | MQ 发布仅 `log.debug`。 | 明确本期不依赖 MQ；若依赖外部同步，必须接入真实消息或补偿任务。 | 防止 PLM 状态已变更但外部系统未同步。 |
| 文档可读性 | `D:\work\资料\PLM\docs\07-权限与审批流规范.md` 等 | 审查中部分规范文档读取出现乱码。 | 统一 UTF-8 编码并纳入上线文档归档。 | 权限与审批基线不可读会影响上线验收和后续交接。 |

## 6. 旧数据和旧代码清理清单

| 类型 | 文件位置 | 现象 | 建议 | 理由 |
|---|---|---|---|---|
| Mock 账号 | `plm-web/src/mock/users.ts:23-26`、`plm-web/src/mock/data.ts:1217-1220` | 仍保留 `pm/engineer/manager/admin` 与 `123456`。 | 移出运行源码；仅测试目录保留假数据；上线账号由数据库初始化和 IAM/RBAC 管理。 | 防止误引入运行时，也避免安全审计误判。 |
| Mock 业务数据 | `plm-web/src/mock/foundation.ts:1,1011-1041`、`plm-web/src/mock/report-center.ts:1,353` | 旧快照仍通过 `mockResolve` 存在。 | 对未接真实接口的模块隐藏入口或接真实 API；mock 只放测试夹具。 | 本期上线需要可验证真实数据流。 |
| 运行页面依赖 mock | `plm-web/src/views/system/SystemFieldManagementView.vue:7,54-75` | 系统字段管理页面直接 import `@/mock/systemFields`。 | 改接后端系统字段接口，或从上线菜单中移除。 | 运行页面不能依赖静态 mock 作为企业级系统配置。 |
| 禁用根对象口径 | `plm-web/src/views/system/SystemImportExportView.vue:22`、`plm-web/src/router/index.ts:91-93`、`plm-web/src/views/product/SkuView.vue:371-430` | 文案仍出现 SKU 管理、新增 SKU、删除 SKU、历史产品线/型号线/SKU 可导入 released/archived。 | 统一为 Product 下型号/颜色/版本视图；禁止使用“新增/删除 SKU”表达独立根对象动作；导入只能按 Product 主数据和状态机门禁。 | 与开发提示词“SKU 不能作为根对象”的业务口径存在冲突。 |
| 历史 ERP/ECOUNT 数据 | `plm-server/src/main/resources/db/migration/V20260727_1300__ecount_material_groups.sql:8661-8679`、`V20260730_1130__seed_inventory_from_ecount_workbook_and_archive_minor_groups.sql:21-8397`、`outputs/erp-sync-20260804/sync-report.json` | 大量历史物料、导入报告、同步报告仍和源码/迁移混在一起。 | 分离为受控数据包；上线只执行经审批的数据批次；报告进入归档库，不进源码。 | 避免历史数据误注入生产，降低敏感数据泄露面。 |
| 临时/产物目录 | `plm-server/.codex_tmp/**`、`plm-server/logs/**`、`plm-web/dist-dashboard-check/**`、`outputs/**` | 仓库状态显示大量新增/已跟踪产物。 | 清理版本库并补 `.gitignore`，发布包只含源码、必要配置模板和迁移。 | 企业级交付需要可复现、可审查、低噪声的发布边界。 |

## 7. 业务要求符合性判断

- 核心对象边界：后端主体仍围绕 Product、Process、Inventory、Order 等展开，但前端 SKU 管理、项目 archived_sku、BOM 中心关联 SKU 文案较多，业务口径需要统一为“Product 下的视图/派生信息”，避免形成 SKU 根对象。
- 外部系统边界：ERP/ECOUNT 数据迁移与钉钉正式 controller/service 已超过“只预留字段、映射、日志框架或接口草案”的原始范围。若业务已变更为本期上线钉钉闭环，必须补正式安全、幂等、签名、回滚、验收材料；否则应禁用。
- 发布/冻结/审批：已有发布、冻结、日志相关代码，但风险确认、资料门禁和钉钉审批可信来源不足，不满足生产级审批门禁。
- 附件管理：已有下载/预览日志，但固定 URL、本地存储和 `storageKey` 暴露不符合 MinIO/S3 临时签名链接要求。
- 权限：前端权限存在展示控制，后端缺少系统性 RBAC，不能满足企业级安全边界。

## 8. 明天上线还缺什么

必须完成：

1. 修复 P0：生产认证不能关闭；钉钉回调/relay 必须强校验或关闭；删除 Compose 和 prod 配置中的默认密钥、弱口令、公网库地址、cpolar 地址。
2. 轮换泄露风险凭证：钉钉 appKey/appSecret、数据库密码、dev token、测试账号密码。
3. 冻结上线范围：确认钉钉是否属于本期正式上线；若不是，所有钉钉写接口下线或配置禁用。
4. 后端最小 RBAC：发布、冻结、审批、成本、导入导出、附件下载、系统配置接口必须按角色和对象归属授权。
5. 删除或隐藏测试入口：登录页测试账号、操作日志 test 写入接口、Swagger 公开入口、validation probe。
6. 核心页面接真实接口或下线菜单：BOM 中心、基础产品、产品详情、测试中心、报表、工作台中仍 `notConnected` 的入口不能直接对用户开放。
7. 数据迁移演练：在生产备份副本执行 Flyway，验证 ECOUNT/ERP 历史数据、正式 BOM/Process 批量发布 SQL、唯一索引变更不会污染或失败。
8. 仓库发布清理：移除 `.codex_tmp`、logs、dist-dashboard-check、outputs、temp 等产物；补 `.gitignore`；生成干净发布包。
9. 验收材料：补全后端测试、前端构建、接口冒烟、权限矩阵测试、附件下载审计、发布/冻结/回退演练记录。

上线后继续跟踪：

- 成本字段脱敏与审计前后值。
- MinIO/S3 临时签名 URL 与文件病毒/类型校验。
- Redis、数据库、对象存储健康检查。
- 操作日志不可抵赖设计。
- SKU/BOM/PhoneModel 命名口径统一。

## 9. 建议修复优先级

1. 当天立即修：P0 全部安全与部署配置问题。
2. 上线前最后验收前修：P1 权限、成本、附件、核心接口断裂、历史迁移风险。
3. 可排入上线后第一周：P2 公开文档、健康探测、MQ 占位、文档编码、命名口径精修。

## 10. 审查边界说明

本报告只基于代码与仓库静态证据，不代表已完成运行时验收。由于未运行全量构建、测试和迁移，仍需在上线前补充：

- `plm-server` 全量单元/集成测试与 Spring Boot 启动检查。
- `plm-web` 类型检查、单元测试、生产构建和浏览器冒烟。
- PostgreSQL 空库迁移、准生产数据迁移、回滚演练。
- 生产环境变量审计和最小权限账号检查。
- 安全扫描：依赖漏洞、镜像漏洞、敏感信息扫描、接口权限扫描。
