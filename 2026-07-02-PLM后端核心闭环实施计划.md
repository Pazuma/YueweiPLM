# PLM 后端核心闭环实施计划

> 版本：v1.1  
> 日期：2026-07-02  
> 前置文档：《PLM 后端总体方案与项目结构设计》《PLM 三级业务流程图》  
> 目标：让本机 Spring Boot 后端连接远程 PostgreSQL，优先支撑工程部在工作台通过进行中项目时间轴推进项目、维护 BOM、维护工艺路线、上传文件并归档到文件中心。

## 1. 总目标

第一阶段实现以下真实业务闭环：

```text
登录
-> 工作台查看进行中项目
-> 打开项目时间轴
-> 查看当前节点
-> 工程部确认 / 推进节点
-> 在节点中维护 BOM
-> 在节点中维护工艺路线
-> 在节点中上传文件
-> 文件自动归档到文件中心
-> 冻结 / 发布 / 归档
-> 操作留痕
```

第一阶段验收标准不是“所有页面都有真实接口”，而是工程部两个人可以在工作台按流程推动一个真实项目。

## 2. 全局约束

```text
后端框架：Spring Boot 3
数据访问：MyBatis-Plus
数据库：远程 PostgreSQL，沿用现有表结构
运行方式：本机后端 + 远程数据库
文件存储：本机服务器目录
认证方式：简单登录 + token
权限方式：正式使用用户默认全权限
审批方式：时间轴节点确认 / 状态推进
接口前缀：/api/v1
迁移策略：不重跑 V1.0 / V1.1 全量脚本，只允许必要增量脚本
```

## 3. 阶段划分

```text
阶段 0：环境与远程库安全边界
阶段 1：简单登录与当前用户
阶段 2：操作日志基础能力
阶段 3：工作台进行中项目列表
阶段 4：项目详情与时间轴查询
阶段 5：时间轴节点推进 / 确认 / 退回
阶段 6：BOM 管理接入项目时间轴
阶段 7：工艺路线接入项目时间轴
阶段 8：时间轴节点文件上传
阶段 9：文件中心归档查询与下载
阶段 10：冻结 / 发布 / 归档
阶段 11：前端核心联调与验收
```

## 4. 阶段 0：环境与远程库安全边界

### 目标

让本机后端稳定连接远程 PostgreSQL，并避免误操作远程库。

### 主要任务

```text
1. 配置 application-dev.yml
   ├─ 远程 PostgreSQL host / port / database
   ├─ schema=plm
   ├─ 密码从环境变量读取
   ├─ 本机上传目录
   └─ 文件大小限制

2. 检查核心表
   ├─ plm_product
   ├─ plm_product_bom
   ├─ plm_product_bom_item
   ├─ plm_process
   ├─ plm_attachment
   ├─ plm_operation_log
   └─ sys_user

3. 检查附件表是否可表达时间轴归档
   ├─ product_id 或 business_id
   ├─ business_type
   ├─ file_category
   ├─ storage_type
   ├─ storage_path
   ├─ uploaded_by
   └─ timeline_node_key / stage_key 如不足则补增量脚本
```

### 验收

```text
后端能启动
能查询远程 PostgreSQL
能访问健康检查或 Product 示例接口
不会自动执行破坏性初始化脚本
```

## 5. 阶段 1：简单登录与当前用户

### 目标

让工程部两位正式用户可以登录，后端能识别当前操作者。

### 主要任务

```text
1. 新增 security 模块
   ├─ AuthController
   ├─ LoginDTO
   ├─ LoginVO
   ├─ CurrentUserVO
   ├─ SimpleTokenService
   └─ CurrentUserContext

2. 登录逻辑
   ├─ 校验账号密码
   ├─ 返回 token
   ├─ 返回用户基本信息
   └─ 返回 allPermissions=true

3. token 解析
   ├─ 从 Authorization Header 读取 Bearer token
   ├─ 解析当前用户
   ├─ 写入 request context
   └─ 未登录返回明确错误
```

### 接口

```text
POST /api/v1/auth/login
GET  /api/v1/auth/profile
POST /api/v1/auth/logout
```

### 验收

```text
正确账号密码返回 token
携带 token 能获取 profile
业务接口能读取当前用户
未登录访问业务接口返回未登录
```

## 6. 阶段 2：操作日志基础能力

### 目标

状态推进、节点确认、BOM 修改、工艺修改、文件上传、冻结发布都必须留痕。

### 主要任务

```text
1. 建立 OperationLogService
   ├─ business_type
   ├─ business_id
   ├─ action_type
   ├─ old_status
   ├─ new_status
   ├─ operator
   ├─ reason
   ├─ remark
   └─ request_id

2. 统一操作类型
   ├─ timeline_advance
   ├─ timeline_confirm
   ├─ timeline_return
   ├─ bom_create
   ├─ bom_update
   ├─ bom_freeze
   ├─ process_create
   ├─ process_update
   ├─ process_freeze
   ├─ attachment_upload
   ├─ publish
   └─ archive
```

### 验收

```text
调用任意关键动作后能查到日志
日志包含操作人、时间、动作和业务对象
失败动作不伪装成成功日志
```

## 7. 阶段 3：工作台进行中项目列表

### 目标

让工作台和项目管理的“进行中”列表可以读取真实项目数据。

### 主要任务

```text
1. 新建 project 模块
   ├─ ProjectWorkbenchController
   ├─ ProjectQueryDTO
   ├─ ProjectSummaryVO
   ├─ ProjectService
   └─ ProjectRepository

2. 数据来源
   ├─ 第一阶段以 plm_product 作为项目承载对象
   ├─ product_type 区分新产品线 / 新型号线
   ├─ status 区分进行中 / 已归档 / 已放弃
   ├─ current_step_no 表示当前时间轴步骤
   └─ active_bom_version 表示当前 BOM 主版本

3. 查询能力
   ├─ 进行中项目
   ├─ 全部项目
   ├─ 已归档项目
   ├─ 已放弃项目
   └─ 按新产品线 / 新型号线筛选
```

### 接口

```text
GET /api/v1/workbench/projects/in-progress
GET /api/v1/projects
```

### 验收

```text
前端进行中项目列表可展示真实数据
能按 product_type 筛选新产品线和新型号线
能返回 currentStepNo、currentStage、ownerUserName、activeBomVersion
```

## 8. 阶段 4：项目详情与时间轴查询

### 目标

让前端项目详情弹窗中的“当前节点 / 项目流程 / BOM管理 / 工艺路线 / 资料区”有统一数据来源。

### 主要任务

```text
1. 新建 timeline 模块
   ├─ TimelineController
   ├─ TimelineNodeVO
   ├─ TimelineDetailVO
   ├─ TimelineService
   └─ TimelineDefinitionProvider

2. 时间轴定义
   ├─ 新产品线节点
   │  ├─ 立项确认
   │  ├─ 设计确认
   │  ├─ 开模试模
   │  ├─ 样品与工艺
   │  ├─ 小批与 MX 验证
   │  └─ 投产决策
   └─ 新型号线节点
      ├─ 扩展确认
      ├─ 差异设计
      ├─ 模具判断
      ├─ 差异验证
      ├─ 小批与 MX 验证
      └─ 冻结发布

3. 时间轴返回字段
   ├─ nodeKey
   ├─ nodeName
   ├─ phaseName
   ├─ status
   ├─ receiverRole
   ├─ receiverUserName
   ├─ receivedAt
   ├─ promoterUserName
   ├─ promotedAt
   ├─ nextAction
   ├─ riskNote
   ├─ documentCount
   └─ experienceSummary
```

### 接口

```text
GET /api/v1/projects/{projectId}
GET /api/v1/projects/{projectId}/timeline
GET /api/v1/projects/{projectId}/summary
```

### 验收

```text
前端能打开项目详情
能显示当前节点
能显示完整项目流程
能显示每个节点资料数
```

## 9. 阶段 5：时间轴节点推进 / 确认 / 退回

### 目标

把第一版审批落到时间轴节点动作上，让工程部两个人可以推动项目。

### 主要任务

```text
1. 实现节点动作
   ├─ advance 推进到下一节点
   ├─ confirm 确认当前节点或门禁节点
   └─ return 退回修改

2. 状态规则
   ├─ 只有 current 节点可推进
   ├─ 门禁节点必须 confirm 后才能 advance
   ├─ advance 后当前节点变 completed
   ├─ 下一节点变 current
   └─ return 后项目回到 developing / returned

3. 留痕
   ├─ 记录原节点
   ├─ 记录新节点
   ├─ 记录动作人
   ├─ 记录原因
   └─ 写 plm_operation_log
```

### 接口

```text
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/confirm
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/advance
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/return
```

### 验收

```text
能确认当前节点
能推进到下一节点
不能跳过当前节点直接推进后续节点
门禁节点未确认时不能发布
每次动作都有操作日志
```

## 10. 阶段 6：BOM 管理接入项目时间轴

### 目标

让工程部在项目推进过程中维护 BOM，并把 BOM 版本作为节点成果物沉淀。

### 主要任务

```text
1. 新建 bom 模块
   ├─ BomController
   ├─ BomService
   ├─ ProductBom entity
   ├─ ProductBomItem entity
   ├─ BomVO
   └─ BomItemVO

2. BOM 能力
   ├─ 按 projectId/productId 查询 BOM 版本
   ├─ 创建 EBOM / MBOM / 包装 BOM
   ├─ 添加 BOM 明细
   ├─ 修改 BOM 明细
   ├─ 标记替代料
   ├─ 维护用量 / 单位 / 损耗率
   └─ 冻结 BOM

3. 时间轴关联
   ├─ 在样品与工艺、差异验证、冻结发布等节点关联 BOM
   ├─ 节点详情返回 activeBomVersion
   ├─ BOM 冻结后节点可显示已完成成果
   └─ BOM 修改写操作日志
```

### 接口

```text
GET    /api/v1/projects/{projectId}/boms
GET    /api/v1/boms/{bomId}
POST   /api/v1/projects/{projectId}/boms
PUT    /api/v1/boms/{bomId}
POST   /api/v1/boms/{bomId}/items
PUT    /api/v1/boms/{bomId}/items/{itemId}
DELETE /api/v1/boms/{bomId}/items/{itemId}
POST   /api/v1/boms/{bomId}/freeze
POST   /api/v1/projects/{projectId}/timeline/{nodeKey}/link-bom
```

### 验收

```text
能为项目创建 BOM
能添加和修改 BOM 明细
项目详情能显示 BOM 版本和明细
冻结后 BOM 不允许直接修改
BOM 冻结动作写日志
```

## 11. 阶段 7：工艺路线接入项目时间轴

### 目标

让工程部在项目推进过程中维护工艺路线、工序明细和 SOP / SIP 关联。

### 主要任务

```text
1. 新建 process 模块
   ├─ ProcessRouteController
   ├─ ProcessRouteService
   ├─ Process entity
   ├─ ProcessRouteVO
   └─ ProcessOperationVO

2. 工艺能力
   ├─ 按 projectId/productId 查询工艺路线
   ├─ 创建工艺路线
   ├─ 编辑工艺路线
   ├─ 维护工序顺序
   ├─ 维护工艺参数摘要
   ├─ 维护质量要求
   └─ 冻结工艺路线

3. 时间轴关联
   ├─ 在样品与工艺、差异验证、冻结发布等节点关联工艺路线
   ├─ SOP / SIP 通过附件与工艺路线或节点关联
   ├─ 节点详情返回 processRoutes 摘要
   └─ 工艺修改写操作日志
```

### 接口

```text
GET  /api/v1/projects/{projectId}/process-routes
GET  /api/v1/process-routes/{processId}
POST /api/v1/projects/{projectId}/process-routes
PUT  /api/v1/process-routes/{processId}
POST /api/v1/process-routes/{processId}/freeze
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/link-process
```

### 验收

```text
能为项目创建工艺路线
能维护工序顺序和工艺摘要
项目详情能显示工艺路线
能关联 SOP / SIP 文件
冻结后工艺路线不允许直接修改
工艺冻结动作写日志
```

## 12. 阶段 8：时间轴节点文件上传

### 目标

让工程部在推进项目时直接在当前时间轴节点上传文件，上传后自动沉淀到文件中心。

### 主要任务

```text
1. 新建 attachment 模块
   ├─ TimelineAttachmentController
   ├─ AttachmentService
   ├─ Attachment entity
   ├─ AttachmentVO
   └─ AttachmentUploadDTO

2. 新建本地存储实现
   ├─ LocalFileStorageClient
   ├─ 保存文件到配置目录
   ├─ 生成安全文件名
   ├─ 返回 storage_path
   └─ 支持下载读取

3. 元数据写入 plm_attachment
   ├─ project_id / product_id
   ├─ timeline_node_key
   ├─ business_type
   ├─ business_id
   ├─ file_category
   ├─ original_file_name
   ├─ storage_type=local
   ├─ storage_path
   ├─ file_size
   ├─ content_type
   ├─ version_no
   └─ uploaded_by

4. 文件分类
   ├─ drawing
   ├─ bom
   ├─ sop
   ├─ sip
   ├─ test_report
   ├─ customer_confirm
   ├─ production_doc
   └─ other
```

### 接口

```text
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/attachments
GET  /api/v1/projects/{projectId}/timeline/{nodeKey}/attachments
```

### 验收

```text
能在项目当前节点上传文件
本机上传目录有真实文件
plm_attachment 有元数据
节点资料数会增加
上传动作写操作日志
```

## 13. 阶段 9：文件中心归档查询与下载

### 目标

文件中心作为归档、检索、查看和下载入口，不作为第一阶段主上传入口。

### 主要任务

```text
1. 新建 filecenter 模块
   ├─ FileCenterController
   ├─ FileCenterService
   ├─ FileCenterQueryDTO
   ├─ FileGroupVO
   └─ FileArchiveVO

2. 查询能力
   ├─ 按进行中 / 全部项目筛选
   ├─ 按 projectId 查询
   ├─ 按 timeline_node_key 查询
   ├─ 按 file_category 查询
   ├─ 按关键字查询
   └─ 按上传时间查询

3. 下载能力
   ├─ 根据 attachmentId 查询元数据
   ├─ 校验本地文件存在
   ├─ 返回文件流
   └─ 文件不存在时返回明确错误
```

### 接口

```text
GET /api/v1/file-center/attachments
GET /api/v1/attachments/{attachmentId}
GET /api/v1/attachments/{attachmentId}/download
DELETE /api/v1/attachments/{attachmentId}
```

### 验收

```text
文件中心能看到时间轴上传的文件
能按项目和节点筛选文件
能下载文件
本地文件不存在时返回明确错误，不伪装为空结果
```

## 14. 阶段 10：冻结 / 发布 / 归档

### 目标

在项目节点、BOM、工艺路线和文件资料齐备后，支持冻结、发布和归档。

### 主要任务

```text
1. 新建 lifecycle 模块
   ├─ LifecycleService
   ├─ LifecycleActionDTO
   ├─ LifecycleActionVO
   └─ LifecycleConstants

2. 支持动作
   ├─ freeze_bom
   ├─ freeze_process
   ├─ freeze_project
   ├─ publish_project
   ├─ archive_project
   └─ abandon_project

3. 发布前校验
   ├─ 当前时间轴必须到达允许发布节点
   ├─ BOM 至少有一个可用冻结版本
   ├─ 工艺路线至少有一个可用冻结版本
   ├─ 必要文件已上传
   └─ 门禁节点已确认
```

### 接口

```text
POST /api/v1/projects/{projectId}/freeze
POST /api/v1/projects/{projectId}/publish
POST /api/v1/projects/{projectId}/archive
POST /api/v1/projects/{projectId}/abandon
```

### 验收

```text
资料不足时不能发布，并返回缺失项
资料齐备时可以发布
发布后项目进入 released
归档后文件中心仍可查看资料
冻结、发布、归档均写操作日志
```

## 15. 阶段 11：前端核心联调与验收

### 优先联调页面

```text
1. 登录页
2. 工作台首页总览
3. 项目管理 / 进行中
4. 项目详情 / 当前节点
5. 项目详情 / 项目流程
6. 项目详情 / BOM 管理
7. 项目详情 / 工艺路线
8. 项目详情 / 资料区
9. 文件中心
```

### API 替换顺序

```text
1. auth.ts
2. project.ts / product.ts 中的项目列表和详情
3. timeline.ts
4. bom.ts
5. process.ts
6. attachment.ts
7. file-center.ts
```

### 验收脚本

```text
1. 工程部用户登录
2. 进入工作台或项目管理的进行中项目
3. 打开一个新产品线项目
4. 查看当前时间轴节点
5. 确认当前节点
6. 推进到样品与工艺阶段
7. 创建或维护 BOM
8. 添加 2-3 条 BOM 明细
9. 创建或维护工艺路线
10. 上传图纸、SOP、SIP 或测试报告
11. 在文件中心查询刚上传的文件
12. 冻结 BOM
13. 冻结工艺路线
14. 发布项目
15. 查看操作日志和归档资料
```

### 完成定义

```text
真实用户可以登录
工作台能看到真实进行中项目
项目详情能显示真实时间轴
工程部可以推进节点
BOM 可以维护并冻结
工艺路线可以维护并冻结
文件可以从时间轴节点上传
文件中心可以查询和下载归档文件
项目可以发布和归档
所有关键动作都有操作日志
```

## 16. 风险与对策

### 16.1 时间轴缺少独立表

```text
风险：现有表可能没有完整 timeline 表
对策：
├─ 第一阶段用 product 当前步骤字段 + 后端节点定义承载
├─ 节点动作写 operation_log
├─ attachment 记录 timeline_node_key
└─ 后续再拆 plm_project_timeline_node 表
```

### 16.2 本地文件与远程库不一致

```text
风险：数据库在远程，文件在本机，换机器后文件不可访问
对策：
├─ storage_type 标记 local
├─ storage_path 使用配置根目录下相对路径
├─ 文件不存在时返回明确错误
└─ 正式部署时迁移到服务器目录或 MinIO
```

### 16.3 审批后续升级

```text
风险：第一版把审批简化为节点确认，后续可能要正式审批流
对策：
├─ 节点确认统一走 timeline/lifecycle 服务
├─ 操作日志记录完整
├─ 保留 approval 表和审批中心页面
└─ 后续审批任务只接管节点动作入口
```

### 16.4 BOM / 工艺被做成孤立页面

```text
风险：只做独立 BOM 和工艺页面，无法服务工程部推进项目
对策：
├─ BOM 接口必须支持 projectId/productId 查询
├─ 工艺接口必须支持 projectId/productId 查询
├─ 时间轴节点详情返回 BOM 和工艺摘要
└─ 冻结发布必须校验 BOM 和工艺成果
```

## 17. 推荐里程碑

```text
M1：环境 + 登录 + 操作日志
M2：工作台进行中项目 + 项目时间轴查询
M3：时间轴节点推进 / 确认 / 退回
M4：BOM 管理接入时间轴
M5：工艺路线接入时间轴
M6：时间轴文件上传 + 文件中心归档
M7：冻结 / 发布 / 归档
M8：前端核心联调验收
```

## 18. 最终建议

实施时不要先追求把审批中心、文件中心、BOM 页面、工艺页面都做成独立完整系统。第一阶段只抓一条线：

```text
工程部在工作台打开进行中项目
沿时间轴推动项目
在节点中沉淀 BOM、工艺路线和文件
文件归档到文件中心
最后冻结、发布、归档
```

这条线跑通后，PLM 的真实业务骨架就成立了。
