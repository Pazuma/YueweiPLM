# 32-物料库存左侧分类树与模具治具按产品细分改造方案

## 1. 文档说明

本文用于沉淀两项前端优化方案，当前阶段**只输出方案，不修改代码**：

1. 物料库存页改造为“左侧分类树 + 右侧列表”
2. 产品详情页去除“物料分类”区域，并在资料区新增“模具治具”字段

本次范围只涉及前端页面结构、前端 mock 数据结构和前端交互逻辑，不涉及：

- 后端接口改造
- 数据库表结构改造
- 真实权限联调
- ERP / WMS 正式集成

---

## 2. 开发前已阅读文档

### 2.1 开发约束文档

- `D:\Yuewei\资料\PLM\docs\README.md`
- `D:\Yuewei\资料\PLM\docs\01-开发框架总纲.md`
- `D:\Yuewei\资料\PLM\docs\02-系统架构设计.md`
- `D:\Yuewei\资料\PLM\docs\04-AI开发规范.md`
- `D:\Yuewei\资料\PLM\docs\05-数据模型与编码规范.md`
- `D:\Yuewei\资料\PLM\docs\07-权限与审批流规范.md`
- `D:\Yuewei\资料\PLM\docs\08-测试验收规范.md`
- `D:\Yuewei\资料\PLM\docs\文件沉淀\开发提示词.md`
- `D:\Yuewei\资料\PLM\docs\文件沉淀\手机壳制造业PLM系统需求规格说明书-完善版.md`

### 2.2 本次直接相关业务文档

- `D:\Yuewei\资料\PLM\docs\系统优化方案-工作台与基础资料细节.md`
- `D:\Yuewei\资料\PLM\docs\modules\07-模具治具管理.md`
- `D:\Yuewei\资料\PLM\docs\系统优化沉淀\31-产品详情节点推进与系统管理前端优化实施记录.md`

### 2.3 当前已查看代码

- `plm-web/src/views/inventory/InventoryCenterView.vue`
- `plm-web/src/views/product/ProductDetail.vue`

---

## 3. 任务理解

你这次一共补充了两类需求。

### 3.1 物料库存页

你要的不是继续保留“分类手风琴 + 下方统一表格”的浏览方式，而是把“物料库存”改成更接近业务检索的结构：

1. 在物料库存页内，增加**左侧分类树**
2. 左侧树节点直接体现物料分类类型
3. 点击左侧某个分类节点，右侧页面显示该分类对应的物料列表
4. 对“模具治具”这类特殊库存，不只停留在一级分类，而是继续往下拆
5. “模具治具”先按**产品**细分，再进入该产品下的**模具型号 / 治具型号**

### 3.2 产品详情页

你新增了一条产品详情页要求：

1. 去掉产品详情页中现有的“物料分类”区域
2. 在产品详情页“资料区”中增加一个独立字段：**模具治具**
3. 模具治具在产品详情页中作为资料引用维度展示，不再作为分类浏览区存在

本次按前端页面理解，优先实现的是：

- **物料库存页面内部的左侧树形导航**
- **产品详情页资料区字段调整**

不是系统全局主侧边栏的菜单重构。

---

## 4. 所属 PLM 业务链路

本次需求属于：

- 基础资料管理
- Inventory 核心对象前端展示优化
- 模具治具资料浏览优化
- 产品详情资料区结构优化

对应的核心对象仍然是：

- `Inventory`
- `Product`

这里不新增 `Tooling`、`Material` 等根对象，仍然遵守七个核心对象原则。

其中：

- 普通物料继续作为 `Inventory` 的不同 `inventory_type`
- 模具治具也继续作为 `Inventory` 的工装类资料
- “按产品细分模具型号”只是前端浏览层级优化，不改变核心对象归属
- “产品详情页资料区展示模具治具”只是资料引用展示优化，不改变对象模型

---

## 5. 涉及前端页面

本次主要涉及页面：

- `plm-web/src/views/inventory/InventoryCenterView.vue`
- `plm-web/src/views/product/ProductDetail.vue`

如后续需要把这一套分类关系同步到系统全局导航，再考虑补充：

- `plm-web/src/layout/Sidebar.vue`
- `plm-web/src/router/index.ts`

但按当前需求理解，**本轮先不动全局导航**，只优化：

- 物料库存页面内部结构
- 产品详情页资料区结构

---

## 6. 涉及后端模块

本轮不涉及后端改动。

如后续进入联调阶段，可能需要后端补充的只是：

- 库存分类树接口
- 模具治具按产品聚合接口
- 产品详情资料区中的模具治具摘要字段
- 节点统计数量字段

但这些都不在本轮范围内。

---

## 7. 涉及数据对象 / 状态 / API

### 7.1 核心对象

- `Inventory`
- `Product`

### 7.2 当前前端展示所需状态

本轮不新增业务状态机，只复用现有库存状态展示：

- `draft`
- `available`
- `reserved`
- `consumed`
- `closed`

### 7.3 当前前端 mock API

当前页面使用：

- `getInventoryCenterSnapshot()`

产品详情页当前使用的资料展示结构，后续也只做前端 mock 调整，不新增真实后端能力。

本轮如果后续进入代码实施，建议：

- 物料库存页仍沿用 `getInventoryCenterSnapshot()`
- 但调整返回结构，使其支持左侧递归树和右侧列表联动
- 产品详情页则在现有详情 mock 结构中补充 `toolingSummary` 或同类字段

---

## 8. 当前页面问题

### 8.1 当前物料库存页没有真正的左侧导航树

现在页面是：

- 顶部搜索
- 下方多个分类手风琴
- 展开某个大类后，在下方显示一张表

这更像“一个个往下翻”，不是“从左边选分类，再在右边看结果”。

### 8.2 当前物料库存页只能支持一层分类，不适合模具治具继续下钻

当前 `groups` 数据结构只支持：

- 一级 root
- 二级 children

但你的需求里，“模具治具”至少需要支持：

- 模具治具
  - 产品
    - 模具型号 / 治具型号

现有结构层级不够。

### 8.3 当前物料库存页右侧数据只按展开根节点过滤，不支持按点击节点精确联动

现在的右侧数据实际是：

- 根据当前展开的根节点
- 找到该根节点下所有叶子 nodeId
- 再过滤出列表

这意味着：

- 可以看“原材料整类”
- 但不能点到“超队 3.0 > 注塑模”这种更细颗粒度

### 8.4 模具治具业务特性没有体现在物料库存页层级里

根据 `07-模具治具管理.md`：

- 模具治具统一归 `Inventory`
- 会关联 `Product`
- 模具的适配关系和试模记录很关键

所以模具治具天然更适合按“产品”继续细分，而不是和普通原材料完全并列展示。

### 8.5 产品详情页中的“物料分类区”与资料区职责重复

产品详情页现在如果同时承担：

- 资料查看
- 项目推进
- 版本查看
- 物料分类浏览

页面职责会变得过重。

而“物料分类浏览”本身更适合放在独立的库存页完成，不适合继续留在产品详情页中。

### 8.6 产品详情页资料区目前缺少“模具治具”这一类关键资料引用

从业务上看，模具治具对产品开发是核心资料之一，尤其涉及：

- 开模资料
- 试模记录
- 适配产品
- 当前工装准备情况

如果资料区没有“模具治具”字段，用户在产品详情页就无法快速知道：

- 这个产品当前关联了哪些模具治具
- 这些模具治具是否已经准备到位
- 是否需要进一步进入模具治具明细查看

---

## 9. 不确定点与本次默认理解

### 9.1 “左侧栏”默认理解

本次默认理解为：

- **物料库存页面内部左侧栏**

不是系统全局应用左侧主菜单。

### 9.2 点击父节点后的右侧行为

本次建议默认规则为：

- 点击叶子节点：右侧显示该叶子节点对应的精确列表
- 点击中间节点：右侧显示该节点及其所有下级节点的汇总列表

这样用户既能看总类，也能继续下钻。

### 9.3 模具治具最细层级

本次默认细分到：

- 产品
- 产品下的模具型号 / 治具型号

后续如果你要继续细到：

- 单套模具编号
- 单个治具编号
- 版本号

也可以继续往下扩，但本轮先按你当前要求控制层级。

### 9.4 产品详情页“模具治具字段”的默认理解

本次默认理解为：

- 在产品详情页资料区中新增一个“模具治具”资料字段或资料卡片
- 该字段用于展示当前产品关联的模具 / 治具摘要
- 点击后再进入对应模具治具详情或列表

不是把完整模具治具树结构直接搬进产品详情页。

---

## 10. 实施方案

### 10.1 物料库存页结构总改造方向

把当前页面从：

- 上方搜索
- 下方纵向手风琴

改成：

- 左侧树形分类区
- 右侧列表区

建议布局：

```text
物料库存
├─ 顶部：搜索 / 状态筛选 / 当前路径
└─ 内容区
   ├─ 左侧：分类树
   └─ 右侧：当前节点对应的物料列表
```

这样用户的操作路径会变成：

1. 先在左侧决定“看哪一类”
2. 再在右侧浏览对应清单

这比当前“挨个展开分类去试”效率更高。

### 10.2 物料库存页左侧分类树建议结构

建议把左侧树做成递归树，而不是固定两级。

基础结构如下：

```text
物料库存
├─ 原材料
├─ 功能件
├─ 包材
└─ 模具治具
   ├─ 超队 3.0
   │  ├─ 注塑模
   │  ├─ 热压治具
   │  └─ 包边治具
   ├─ 亮甲 3.0
   │  ├─ 注塑模
   │  └─ 冲切治具
   └─ Titanio 骑士 2.0
      ├─ 注塑模
      └─ 装配治具
```

### 10.3 左侧节点规则

建议规则：

- 一级节点：库存大类
- 二级节点：模具治具下的产品
- 三级节点：该产品下的模具型号 / 治具型号

每个节点右侧可显示数量，如：

- 原材料（15）
- 功能件（8）
- 包材（12）
- 模具治具（9）

对于产品节点也可显示：

- 超队 3.0（3）
- 亮甲 3.0（2）

### 10.4 右侧列表区建议行为

右侧列表不再跟“展开手风琴”绑定，而是跟“当前选中树节点”绑定。

建议行为：

#### 点击一级分类

例如点击：

- 原材料

右侧显示：

- 所有原材料列表

#### 点击模具治具下的产品节点

例如点击：

- 模具治具 > 超队 3.0

右侧显示：

- 超队 3.0 相关全部模具 / 治具列表

#### 点击模具型号节点

例如点击：

- 模具治具 > 超队 3.0 > 注塑模

右侧显示：

- 超队 3.0 下所有注塑模明细

### 10.5 右侧列表字段建议

#### 普通物料列表

建议保留：

- 物料编码
- 物料名称
- 规格
- 库存数量
- 状态
- 供应商

#### 模具治具列表

建议针对这一类单独优化列，至少显示：

- 工装编码
- 工装名称
- 所属产品
- 类型（模具 / 治具）
- 规格 / 型号
- 当前状态
- 供应商

如 mock 数据允许，可再加：

- 理论寿命
- 已用寿命

这样更贴合模具治具业务。

### 10.6 顶部辅助区建议

右侧列表上方建议增加一个轻量工具区：

- 搜索框
- 状态筛选
- 当前路径面包屑
- 当前节点说明

例如：

```text
物料库存 / 模具治具 / 超队 3.0 / 注塑模
```

这样用户在多层级下钻后不会迷路。

### 10.7 产品详情页调整方案

产品详情页建议同步做一条结构收敛：

- 去掉现有“物料分类”区域
- 保留并强化“资料区”
- 在资料区内新增“模具治具”字段

建议资料区中的“模具治具”展示方式为：

- 显示当前关联模具治具数量
- 显示 1-3 个关键名称摘要
- 显示当前状态摘要，如“2 套可用 / 1 套试模中”
- 支持点击进入对应明细页或弹出关联列表

示意理解：

```text
资料区
├─ BOM
├─ 工艺路线
├─ 图纸文件
├─ 测试资料
└─ 模具治具
```

这样改的原因是：

1. 物料分类浏览属于基础资料中心，适合放在库存页独立完成
2. 产品详情页更适合展示“这个产品当前挂了哪些关键资料”
3. 模具治具对产品开发链路很重要，应该作为资料引用维度被明确看见

### 10.8 与现有“基础资料简化方向”的关系

之前文档里，物料库存页是往“折叠、减噪、减少页面长度”方向收敛的。

这次并不冲突，只是进一步细化成更适合库存资料检索的结构：

- 原先手风琴适合“快速折叠”
- 新的左树 + 右表更适合“分类浏览 + 精准定位”

尤其是模具治具这种天然带产品归属的对象，树形结构更合适。

而产品详情页则反过来继续做“减重”：

- 把分类浏览职责移出去
- 把资料引用职责保留下来

这样两个页面的边界会更清晰。

---

## 11. 涉及数据结构的前端改造建议

### 11.1 当前结构不足

当前物料库存页数据结构偏扁平，只适合两层：

- root
- children

建议改成递归树：

```ts
interface InventoryTreeNode {
  nodeId: string
  label: string
  nodeType: 'category' | 'product' | 'tooling-model'
  children?: InventoryTreeNode[]
}
```

### 11.2 列表数据建议

右侧列表数据建议至少保留：

```ts
interface InventoryListItem {
  id: string
  nodeId: string
  code: string
  name: string
  spec: string
  stock: string
  status: string
  supplierName: string
  inventoryType: string
  productName?: string
}
```

其中：

- 普通物料直接按 `nodeId` 归类
- 模具治具项挂到“产品下的具体型号节点”

### 11.3 产品详情资料区数据建议

产品详情页建议补充一个资料摘要结构，例如：

```ts
interface ProductToolingSummary {
  totalCount: number
  availableCount: number
  trialCount: number
  toolingNames: string[]
}
```

这类结构只承担“资料区摘要展示”职责，不承担完整分类浏览职责。

### 11.4 前端过滤逻辑建议

建议从“当前 root 的 leafIds 过滤”改成：

- 当前点击节点
- 获取该节点本身及其所有子孙节点 id
- 右侧列表统一按这个 id 集合过滤

这样：

- 点父节点看汇总
- 点子节点看细分

一套逻辑就够了。

---

## 12. 计划改动代码文件

本轮只是沉淀方案，下面是**后续正式开发时计划改动的文件**。

### 12.1 页面文件

- `plm-web/src/views/inventory/InventoryCenterView.vue`
- `plm-web/src/views/product/ProductDetail.vue`

### 12.2 类型文件

- `plm-web/src/types/foundation.ts`

### 12.3 mock 数据文件

- `plm-web/src/mock/foundation.ts`

### 12.4 前端 API 文件

- `plm-web/src/api/modules/foundation.ts`

### 12.5 如需同步到全局菜单，再追加

- `plm-web/src/layout/Sidebar.vue`
- `plm-web/src/router/index.ts`

---

## 13. 计划修改逻辑说明

### 13.1 `InventoryCenterView.vue`

计划修改：

- 页面布局从纵向手风琴改为左右分栏
- 左侧增加树形分类区
- 右侧表格与左侧点击节点联动
- 顶部增加路径与筛选区
- 模具治具单独支持更深层级展示

修改原因：

- 当前结构无法支持按产品继续下钻
- 当前结构不适合快速定位具体模具型号

### 13.2 `ProductDetail.vue`

计划修改：

- 去掉产品详情页中的“物料分类”展示区
- 在资料区增加“模具治具”字段或资料卡片
- 将模具治具作为产品资料引用的一部分展示
- 资料区点击后再进入更详细的模具治具信息

修改原因：

- 产品详情页要聚焦资料与推进，不再承担库存分类浏览职责
- 模具治具是产品开发中的关键资料，需要在详情页资料区直接可见

### 13.3 `types/foundation.ts`

计划修改：

- 增加递归树节点类型
- 扩展库存列表项类型
- 增加产品详情页模具治具摘要类型

修改原因：

- 当前类型只适合两层分类
- 无法表达“模具治具 > 产品 > 型号”结构
- 无法表达产品详情资料区中的模具治具摘要

### 13.4 `mock/foundation.ts`

计划修改：

- 重新组织库存分类树 mock
- 增加模具治具按产品划分的数据
- 增加节点计数和层级归属
- 增加产品详情页资料区的模具治具摘要 mock

修改原因：

- 页面结构变了，mock 必须能支撑演示

### 13.5 `api/modules/foundation.ts`

计划修改：

- 保持原有接口名不变
- 调整返回值结构以适配新页面

修改原因：

- 尽量减少页面调用方式变化
- 让前端实施改动更集中

---

## 14. 计划使用技术

本轮后续如果进入前端开发，建议继续使用现有技术，不新增依赖。

### 14.1 Vue 3

主要使用：

- `ref`
- `computed`
- `onMounted`

### 14.2 Element Plus

建议使用：

- `el-tree`
- `el-table`
- `el-input`
- `el-select`
- `el-tag`
- `el-breadcrumb`

产品详情页资料区如需摘要卡片或轻量列表，继续沿用现有组件体系即可，不需要新增 UI 库。

### 14.3 mock 驱动

本轮仍是前端演示阶段，建议继续使用本地 mock 数据驱动页面。

---

## 15. 风险点

### 15.1 “左侧栏”理解风险

如果你后续说的“左侧栏”其实是系统全局主菜单，而不是物料库存页内部左侧树，那么后续改动范围会扩大。

### 15.2 模具治具最细粒度风险

当前先按“产品 > 模具型号 / 治具型号”理解。

如果后续你希望细到：

- 单套模具编号
- 模具版本
- 试模批次

那树结构和右侧列表字段还要继续增强。

### 15.3 普通物料是否继续细分

当前默认：

- 原材料
- 功能件
- 包材

先只保留一级。

如果后续要像模具治具一样继续细分，如：

- 原材料 > TPU
- 原材料 > PC
- 包材 > 彩盒
- 包材 > 标签

也可以继续往下扩。

### 15.4 产品详情页“模具治具”展示深度风险

当前默认是资料区摘要展示。

如果后续你希望在产品详情页里直接展开：

- 全部模具清单
- 全部治具清单
- 试模记录
- 寿命信息

那产品详情页会重新变重，信息密度会接近一个二级资料中心页面。

因此本轮建议先控制为：

- 资料区摘要
- 点击后再进入详情

---

## 16. 交付文档结构

本次交付的是一份实施前沉淀文档，内容包含：

1. 文档说明
2. 开发前已阅读文档
3. 任务理解
4. 所属 PLM 业务链路
5. 涉及前端页面
6. 涉及后端模块
7. 涉及数据对象 / 状态 / API
8. 当前页面问题
9. 不确定点与默认理解
10. 实施方案
11. 前端数据结构改造建议
12. 计划改动代码文件
13. 计划修改逻辑说明
14. 计划使用技术
15. 风险点

---

## 17. 结论

这次需求的核心，不是简单给库存页多加几个分类，而是同时完成两件事：

1. 把“物料库存”从一个按块展开的浏览页，升级成一个真正可检索、可下钻的资料页
2. 把“产品详情页”继续收敛回“资料与推进中心”，去掉物料分类浏览区，把模具治具纳入资料区

最关键的变化是三点：

1. 物料分类从“页面内纵向手风琴”改成“左侧树 + 右侧列表”
2. 模具治具不再和普通物料完全同级浏览，而是体现出“按产品继续细分”的业务结构
3. 产品详情页去掉物料分类区，在资料区中增加“模具治具”字段，改为资料引用视角展示

如果按这份方案进入下一步开发，前端改造方向会比较清晰，而且不会碰后端边界。
