spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://8.135.19.108:5432/plm_data_management}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      database: ${REDIS_DB:0}

app:
  security:
    enabled: true
