spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/plm
    username: plm
    password: plm123
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

app:
  security:
    enabled: false
