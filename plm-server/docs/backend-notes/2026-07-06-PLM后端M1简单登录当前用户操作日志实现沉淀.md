# PLM 后端 M1 简单登录、当前用户与操作日志实现沉淀

> 日期：2026-07-06  
> 适用仓库：`D:\Yuewei\git\YUEWEI\plm-server`  
> 依据文档：`docs/backend-notes/2026-07-03-PLM后端M1简单登录当前用户操作日志实施文档.md`  
> 沉淀目录：`D:\Yuewei\资料\PLM\docs\后端-沉淀`

## 1. 本次目标

实现 M1 后端最小闭环：

```text
账号密码登录
随机 token 会话
当前用户上下文
未登录拦截
profile / logout
操作日志写入和查询
Product 冻结动作使用当前用户并写日志
```

本阶段不做完整 RBAC，不做菜单/按钮权限，不做外部系统正式集成。

## 2. 开发前引用文档

本次开发前已读取：

```text
D:\Yuewei\资料\PLM\docs\文件沉淀\开发提示词.md
D:\Yuewei\资料\PLM\docs\README.md
D:\Yuewei\资料\PLM\docs\01-开发框架总纲.md
D:\Yuewei\资料\PLM\docs\02-系统架构设计.md
D:\Yuewei\资料\PLM\docs\04-AI开发规范.md
D:\Yuewei\资料\PLM\docs\05-数据模型与编码规范.md
D:\Yuewei\资料\PLM\docs\07-权限与审批流规范.md
D:\Yuewei\资料\PLM\docs\08-测试验收规范.md
D:\Yuewei\资料\PLM\docs\文件沉淀\手机壳制造业PLM系统需求规格说明书-完善版.md
D:\Yuewei\资料\PLM\docs\modules\01-产品主数据与SKU.md
```

遵守边界：

```text
只复用 Product 等七个核心对象。
操作日志是系统审计能力，不新增业务根对象。
Product freeze 是 Product 上的关键动作留痕。
不实现完整 RBAC。
不做 ERP/MES/WMS/CRM 正式集成。
```

## 3. 修改和新增的文档

| 文件 | 作用 |
| --- | --- |
| `docs/backend-notes/2026-07-03-PLM后端M1简单登录当前用户操作日志实施文档.md` | M1 实施前文档，定义接口、表结构、实现步骤和 Apifox 验证步骤 |
| `docs/backend-notes/2026-07-06-PLM后端M1简单登录当前用户操作日志实现沉淀.md` | 本次实现沉淀，记录实际代码清单和测试方式 |
| `docs/superpowers/plans/2026-07-03-m1-auth-operation-log.md` | M1 执行计划记录 |
| `README.md` | 补充后端底座、认证和运行说明 |

本沉淀文档需要同步复制到：

```text
D:\Yuewei\资料\PLM\docs\后端-沉淀\2026-07-06-PLM后端M1简单登录当前用户操作日志实现沉淀.md
```

## 4. 修改和新增的代码

### 4.1 通用配置和安全

| 文件 | 说明 |
| --- | --- |
| `src/main/java/com/yuewei/plm/common/config/AppProperties.java` | 读取 `app.security.enabled`、`app.security.dev-token`、storage 等应用配置 |
| `src/main/java/com/yuewei/plm/common/config/SecurityConfig.java` | 配置 stateless security、公开路径、未登录统一 JSON 响应、`PasswordEncoder` |
| `src/main/java/com/yuewei/plm/common/security/CurrentUser.java` | 当前登录用户模型，包含 `userId`、`username`、`displayName`、`allPermissions` |
| `src/main/java/com/yuewei/plm/common/security/CurrentUserContext.java` | ThreadLocal 当前用户上下文 |
| `src/main/java/com/yuewei/plm/common/security/SimpleTokenAuthenticationFilter.java` | 解析 Bearer token，校验会话，写入 `CurrentUserContext` 和 `SecurityContext` |
| `src/main/java/com/yuewei/plm/common/filter/RequestContextFilter.java` | 生成或读取 `X-Request-Id`，写入响应和 MDC |
| `src/main/java/com/yuewei/plm/common/util/RequestIdUtil.java` | 统一获取 requestId |
| `src/main/java/com/yuewei/plm/common/exception/GlobalExceptionHandler.java` | 统一业务异常、参数异常、认证异常响应 |

### 4.2 用户和登录模块

| 文件 | 说明 |
| --- | --- |
| `src/main/java/com/yuewei/plm/module/user/entity/SysUser.java` | `sys_user` 表实体 |
| `src/main/java/com/yuewei/plm/module/user/repository/SysUserRepository.java` | MyBatis-Plus 用户 Mapper |
| `src/main/java/com/yuewei/plm/module/auth/dto/LoginDTO.java` | 登录入参 |
| `src/main/java/com/yuewei/plm/module/auth/vo/CurrentUserVO.java` | 当前用户响应 |
| `src/main/java/com/yuewei/plm/module/auth/vo/LoginVO.java` | 登录响应，包含 token 和用户 |
| `src/main/java/com/yuewei/plm/module/auth/service/TokenSessionService.java` | token 会话接口 |
| `src/main/java/com/yuewei/plm/module/auth/service/impl/InMemoryTokenSessionService.java` | 内存随机 token 会话，默认 8 小时过期 |
| `src/main/java/com/yuewei/plm/module/auth/service/AuthService.java` | 登录、profile、logout 服务接口 |
| `src/main/java/com/yuewei/plm/module/auth/service/impl/AuthServiceImpl.java` | 从 `sys_user` 查用户，BCrypt 校验密码，创建 token，会话注销 |
| `src/main/java/com/yuewei/plm/module/auth/controller/AuthController.java` | `POST /auth/login`、`GET /auth/profile`、`POST /auth/logout` |

### 4.3 操作日志模块

| 文件 | 说明 |
| --- | --- |
| `src/main/java/com/yuewei/plm/module/operationlog/entity/OperationLog.java` | `plm_operation_log` 表实体 |
| `src/main/java/com/yuewei/plm/module/operationlog/repository/OperationLogRepository.java` | MyBatis-Plus 操作日志 Mapper |
| `src/main/java/com/yuewei/plm/module/operationlog/constant/OperationActionConstants.java` | 操作动作常量：`AUTH_LOGIN`、`AUTH_LOGOUT`、`TEST_WRITE`、`PRODUCT_FREEZE` 等 |
| `src/main/java/com/yuewei/plm/module/operationlog/dto/OperationLogQueryDTO.java` | 操作日志分页查询条件 |
| `src/main/java/com/yuewei/plm/module/operationlog/dto/OperationLogTestDTO.java` | M1 测试写日志入参 |
| `src/main/java/com/yuewei/plm/module/operationlog/vo/OperationLogVO.java` | 操作日志响应 |
| `src/main/java/com/yuewei/plm/module/operationlog/service/OperationLogCreateCommand.java` | 写日志命令对象 |
| `src/main/java/com/yuewei/plm/module/operationlog/service/OperationLogService.java` | 写日志和分页查询接口 |
| `src/main/java/com/yuewei/plm/module/operationlog/service/impl/OperationLogServiceImpl.java` | 从 `CurrentUserContext`、requestId、request 信息组装并落库 |
| `src/main/java/com/yuewei/plm/module/operationlog/controller/OperationLogController.java` | `GET /operation-logs`、`POST /operation-logs/test` |

### 4.4 Product 冻结动作留痕

| 文件 | 说明 |
| --- | --- |
| `src/main/java/com/yuewei/plm/controller/ProductController.java` | `POST /api/v1/products/{id}/freeze` 不再接收可伪造的 operator 参数 |
| `src/main/java/com/yuewei/plm/service/ProductService.java` | freeze 方法接收 `reason` 和 `HttpServletRequest` |
| `src/main/java/com/yuewei/plm/service/impl/ProductServiceImpl.java` | 从 `CurrentUserContext` 获取操作人，写入冻结人和 `PRODUCT_FREEZE` 操作日志 |

### 4.5 数据库脚本

| 文件 | 说明 |
| --- | --- |
| `src/main/resources/db/migration/V20260703_1600__m1_auth_and_operation_log.sql` | 创建/补齐 `sys_user`，创建 `plm_operation_log`，插入工程部两个开发用户 |

开发种子用户：

```text
username: engineer01
password: plm123456
allPermissions: true

username: engineer02
password: plm123456
allPermissions: true
```

## 5. 自动化测试

### 5.1 测试文件

| 文件 | 验证点 |
| --- | --- |
| `src/test/java/com/yuewei/plm/module/auth/service/impl/AuthServiceImplTest.java` | 正确账号登录成功；错误密码返回统一认证错误 |
| `src/test/java/com/yuewei/plm/module/auth/service/impl/InMemoryTokenSessionServiceTest.java` | token 创建、读取当前用户 |
| `src/test/java/com/yuewei/plm/common/security/SimpleTokenAuthenticationFilterTest.java` | 缺 token 返回 40101；有效 token 写入当前用户 |
| `src/test/java/com/yuewei/plm/module/operationlog/service/impl/OperationLogServiceImplTest.java` | 写日志包含 requestId、操作人、动作、业务对象 |
| `src/test/java/com/yuewei/plm/service/impl/ProductServiceImplTest.java` | Product 冻结使用当前用户，并写 `PRODUCT_FREEZE` 操作日志 |

### 5.2 后端测试命令

执行：

```powershell
cd D:\Yuewei\git\YUEWEI\plm-server
& "D:\Yuewei\IntelliJ IDEA 2026.1.2\plugins\maven\lib\maven3\bin\mvn.cmd" test
```

本次实测结果：

```text
Tests run: 14, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

## 6. 本地启动方式

M1 验证必须显式打开安全开关：

```powershell
cd D:\Yuewei\git\YUEWEI\plm-server
$env:APP_SECURITY_ENABLED="true"
& "D:\Yuewei\IntelliJ IDEA 2026.1.2\plugins\maven\lib\maven3\bin\mvn.cmd" spring-boot:run
```

如果 `APP_SECURITY_ENABLED=false`，业务接口会放行，不能用于 M1 未登录拦截验收。

## 7. Apifox 环境配置

新建环境：

```text
环境名称：PLM 后端本地 M1
baseUrl：http://localhost:8080
username：engineer01
password：plm123456
token：空
requestId：m1-apifox-log-001
productFreezeRequestId：m1-product-freeze-001
productId：按实际创建或已有可冻结 Product 填写
```

通用 Header：

```text
Content-Type: application/json
```

登录后接口 Header：

```text
Authorization: Bearer {{token}}
```

测试写日志 Header：

```text
X-Request-Id: {{requestId}}
```

## 8. Apifox 测试步骤

### 8.1 健康检查

请求：

```text
GET {{baseUrl}}/api/v1/health
```

预期：

```text
HTTP 200
code = 0
data.status = UP
requestId 有值
```

断言：

```javascript
const json = pm.response.json();
pm.test("健康检查成功", function () {
  pm.expect(pm.response.code).to.eql(200);
  pm.expect(json.code).to.eql(0);
  pm.expect(json.data.status).to.eql("UP");
  pm.expect(json.requestId).to.not.be.empty;
});
```

### 8.2 未登录访问业务接口失败

请求：

```text
GET {{baseUrl}}/api/v1/operation-logs
```

不要带 `Authorization`。

预期：

```text
HTTP 401
code = 40101
message = 未登录或登录已失效
```

断言：

```javascript
const json = pm.response.json();
pm.test("未登录被拦截", function () {
  pm.expect(pm.response.code).to.eql(401);
  pm.expect(json.code).to.eql(40101);
  pm.expect(json.message).to.contain("未登录");
});
```

### 8.3 错误账号密码登录失败

请求：

```text
POST {{baseUrl}}/api/v1/auth/login
```

Body：

```json
{
  "username": "wrong-user",
  "password": "wrong-password"
}
```

预期：

```text
code = 40101
message = 账号或密码错误
data 无 token
```

断言：

```javascript
const json = pm.response.json();
pm.test("错误账号密码不能登录", function () {
  pm.expect(json.code).to.eql(40101);
  pm.expect(json.message).to.eql("账号或密码错误");
});
```

### 8.4 正确账号密码登录成功

请求：

```text
POST {{baseUrl}}/api/v1/auth/login
```

Body：

```json
{
  "username": "{{username}}",
  "password": "{{password}}"
}
```

预期：

```text
HTTP 200
code = 0
data.token 有值
data.tokenType = Bearer
data.user.username = engineer01
data.user.allPermissions = true
```

后置脚本：

```javascript
const json = pm.response.json();
pm.test("登录成功", function () {
  pm.expect(pm.response.code).to.eql(200);
  pm.expect(json.code).to.eql(0);
  pm.expect(json.data.token).to.not.be.empty;
  pm.expect(json.data.tokenType).to.eql("Bearer");
  pm.expect(json.data.user.username).to.eql(pm.environment.get("username"));
  pm.expect(json.data.user.allPermissions).to.eql(true);
});
pm.environment.set("token", json.data.token);
```

### 8.5 获取当前用户 profile

请求：

```text
GET {{baseUrl}}/api/v1/auth/profile
```

Header：

```text
Authorization: Bearer {{token}}
```

预期：

```text
HTTP 200
code = 0
data.username = engineer01
data.displayName 有值
data.allPermissions = true
```

断言：

```javascript
const json = pm.response.json();
pm.test("profile 返回当前用户", function () {
  pm.expect(pm.response.code).to.eql(200);
  pm.expect(json.code).to.eql(0);
  pm.expect(json.data.username).to.eql(pm.environment.get("username"));
  pm.expect(json.data.displayName).to.not.be.empty;
  pm.expect(json.data.allPermissions).to.eql(true);
});
```

### 8.6 测试写操作日志

请求：

```text
POST {{baseUrl}}/api/v1/operation-logs/test
```

Header：

```text
Authorization: Bearer {{token}}
X-Request-Id: {{requestId}}
Content-Type: application/json
```

Body：

```json
{
  "businessType": "M1_TEST",
  "businessId": "apifox-001",
  "businessCode": "M1-TEST-001",
  "businessName": "M1 Apifox 测试日志",
  "detail": {
    "source": "apifox",
    "purpose": "verify operation log"
  }
}
```

预期：

```text
HTTP 200
code = 0
data.logId 有值
requestId = m1-apifox-log-001
```

后置脚本：

```javascript
const json = pm.response.json();
pm.test("测试日志写入成功", function () {
  pm.expect(pm.response.code).to.eql(200);
  pm.expect(json.code).to.eql(0);
  pm.expect(json.data.logId).to.not.be.undefined;
  pm.expect(json.requestId).to.eql(pm.environment.get("requestId"));
});
pm.environment.set("logId", json.data.logId);
```

### 8.7 按 requestId 查询操作日志

请求：

```text
GET {{baseUrl}}/api/v1/operation-logs?requestId={{requestId}}&page=1&size=20
```

Header：

```text
Authorization: Bearer {{token}}
```

预期：

```text
HTTP 200
code = 0
content 中存在 requestId = m1-apifox-log-001 的记录
action = TEST_WRITE
businessType = M1_TEST
operatorUserId 有值
operatorUserName 有值
createdAt 有值
```

断言：

```javascript
const json = pm.response.json();
pm.test("能按 requestId 查到操作日志", function () {
  pm.expect(pm.response.code).to.eql(200);
  pm.expect(json.code).to.eql(0);
  const target = json.data.content.find(item => item.requestId === pm.environment.get("requestId"));
  pm.expect(target).to.not.be.undefined;
  pm.expect(target.action).to.eql("TEST_WRITE");
  pm.expect(target.businessType).to.eql("M1_TEST");
  pm.expect(target.operatorUserId).to.not.be.undefined;
  pm.expect(target.operatorUserName).to.not.be.empty;
  pm.expect(target.createdAt).to.not.be.empty;
});
```

### 8.8 数据库直查操作日志

SQL：

```sql
select
    log_id,
    request_id,
    operator_user_id,
    operator_user_name,
    action,
    business_type,
    business_id,
    business_code,
    business_name,
    created_at
from plm_operation_log
where request_id = 'm1-apifox-log-001'
order by created_at desc
limit 20;
```

预期：

```text
能查到 TEST_WRITE 记录。
operator_user_id 不为空。
operator_user_name 不为空。
created_at 有值。
```

### 8.9 退出登录

请求：

```text
POST {{baseUrl}}/api/v1/auth/logout
```

Header：

```text
Authorization: Bearer {{token}}
```

预期：

```text
HTTP 200
code = 0
```

断言：

```javascript
const json = pm.response.json();
pm.test("退出登录成功", function () {
  pm.expect(pm.response.code).to.eql(200);
  pm.expect(json.code).to.eql(0);
});
```

### 8.10 退出后 token 失效

请求：

```text
GET {{baseUrl}}/api/v1/auth/profile
```

Header：

```text
Authorization: Bearer {{token}}
```

预期：

```text
HTTP 401
code = 40101
message = 未登录或登录已失效
```

断言：

```javascript
const json = pm.response.json();
pm.test("logout 后 token 失效", function () {
  pm.expect(pm.response.code).to.eql(401);
  pm.expect(json.code).to.eql(40101);
});
```

### 8.11 Product 冻结写真实业务日志

先重新执行登录用例，获得新 token。

准备一个未冻结、未发布的 Product，设置环境变量：

```text
productId：实际产品 ID
```

请求：

```text
POST {{baseUrl}}/api/v1/products/{{productId}}/freeze?reason=M1操作人验证
```

Header：

```text
Authorization: Bearer {{token}}
X-Request-Id: {{productFreezeRequestId}}
```

预期：

```text
HTTP 200
code = 0
```

再查日志：

```text
GET {{baseUrl}}/api/v1/operation-logs?requestId={{productFreezeRequestId}}&page=1&size=20
```

预期：

```text
action = PRODUCT_FREEZE
businessType = PRODUCT
businessId = productId
operatorUserId = 当前登录用户 ID
operatorUserName = 当前登录用户姓名
requestId = m1-product-freeze-001
createdAt 有值
```

说明：

```text
Product freeze 已废弃 query 参数 operator。
操作人必须来自当前 token 对应的 CurrentUserContext，不能由前端伪造。
```

## 9. 验收结论

满足 M1 推进条件：

```text
正确账号密码可以登录。
错误账号密码返回明确错误。
携带 token 能获取 profile。
未携带 token 访问业务接口返回未登录。
logout 后 token 失效。
测试写日志接口可落库。
操作日志包含业务对象、动作、操作人、requestId、时间。
Product freeze 能写真实业务操作日志。
mvn test 已通过。
```

后续建议：

```text
M2 起所有关键业务动作统一接入 OperationLogService。
多实例部署前，将 InMemoryTokenSessionService 迁移为 RedisTokenSessionService。
完整 RBAC 在权限矩阵确认后再实现，不在 M1 扩大范围。
```
