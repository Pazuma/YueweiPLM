# 33-工艺路线、产品详情、导航栏与库存页前端优化实施记录

## 1. 本次修改目标

基于《33-工艺路线_产品详情_导航栏_库存页前端优化方案》，本次前端实施聚焦四个问题：

1. 工艺路线页信息堆叠过多，改为顶部板块切换后按需展示。
2. 产品详情页全部信息同时展开，改为按业务板块切换浏览。
3. 左侧导航栏支持单分组展开，点击菜单后自动收起。
4. 物料库存页左树右表错位和留白，改成统一高度承载结构。

本次只涉及前端，不涉及后端接口、数据库和权限逻辑变更。

## 2. 修改文件

- `plm-web/src/views/process/ProcessCenterView.vue`
- `plm-web/src/views/product/ProductDetail.vue`
- `plm-web/src/layout/Sidebar.vue`
- `plm-web/src/views/inventory/InventoryCenterView.vue`

## 3. 各文件修改内容

### 3.1 `ProcessCenterView.vue`

#### 修改内容

1. 新增右侧详情区一级板块切换：
   - 路线概览
   - 工序明细
   - 确认门禁
   - 资料挂接
   - 版本变更
   - 联动影响
2. 保留顶部关键指标卡，但不再把所有 section 一次性全部展开。
3. 将原先长页面拆成按板块条件渲染。
4. 顺手修正页面中文文案，避免继续使用乱码文本。

#### 这样修改的逻辑

工艺路线中心本质上是围绕 `Process` 对象做多维查看，但用户每次只会聚焦某一类信息。  
因此把右侧信息从“全展开”改成“先选择板块，再展开对应内容”，能降低阅读压力，同时更贴合工程、采购、品质等角色的差异化使用方式。

#### 使用技术

- Vue 3 `ref`
- Vue 3 `computed`
- Element Plus `el-segmented`
- `v-if` 条件渲染

### 3.2 `ProductDetail.vue`

#### 修改内容

1. 在产品详情页顶部增加一级板块切换：
   - 基础信息
   - 项目时间轴
   - 资料区
   - 商务区
   - 质量区
2. 保留顶部指标卡作为统一上下文。
3. 把时间轴、BOM 版本、成本、质量记录等内容从整页并排堆叠，改成分区浏览。
4. 保留节点推进弹窗逻辑，并让它继续附着在时间轴板块中。
5. 修正页面中文文案，避免乱码继续影响展示效果。

#### 这样修改的逻辑

产品详情页承载的是 `Product` 对象的多角色协同信息。  
如果所有区块同时展开，用户会在时间轴、BOM、成本、测试之间来回分神。  
改成板块切换后，详情页仍保留完整信息，但阅读路径更清楚，用户会先确定“现在要处理什么”，再进入对应区块。

#### 使用技术

- Vue 3 `ref`
- Vue 3 `computed`
- Element Plus `el-segmented`
- Element Plus `el-tabs`
- Element Plus `el-dialog`
- `v-if` 条件渲染

### 3.3 `Sidebar.vue`

#### 修改内容

1. 将原有多分组可同时展开，改为单分组展开。
2. 根据当前路由自动识别激活分组。
3. 点击菜单项后执行路由跳转，但不再自动收起侧栏。
4. 侧栏是否收起，完全由用户手动控制。
5. 保留当前激活菜单和分组高亮。
6. 修正侧栏中文文案。

#### 这样修改的逻辑

原有侧栏在业务模块增多后，会因为多组同时展开变得冗长。  
改成“当前分组单开”后，用户每次只聚焦一个业务簇。  
侧栏的展开和收起改为由用户自己决定，这样能避免用户在模块间切换时反复重新展开导航。

#### 使用技术

- Vue 3 `ref`
- Vue 3 `computed`
- Vue 3 `watch`
- Pinia `useAppStore`
- Vue Router `useRoute` / `useRouter`

### 3.4 `InventoryCenterView.vue`

#### 修改内容

1. 保留左树右表结构，但重构内部布局。
2. 左右区域统一改成：
   - `display: flex`
   - `flex-direction: column`
   - `min-height: 0`
3. 右侧区域新增稳定的表格承载层 `inventory-layout__table-wrap`。
4. 左侧树区域也使用独立滚动体，保证高度对齐。
5. 修正页面中文文案。

#### 这样修改的逻辑

原问题不是业务数据，而是容器高度关系不稳定。  
右侧表格直接 `height="100%"`，但父级高度关系不清晰，就容易出现白块、错位和视觉断层。  
通过把左右栏都改成统一的纵向伸缩容器，再给右表加明确的承载层，表格才能稳定填满剩余空间。

#### 使用技术

- CSS Grid
- Flex 布局
- `min-height: 0`
- Element Plus `el-table`

## 4. 页面交互变化

### 4.1 工艺路线页

- 以前：所有内容一起显示，页面很长
- 现在：顶部先切板块，再只显示当前板块内容

### 4.2 产品详情页

- 以前：基础信息、时间轴、资料区、商务区、质量区同时出现
- 现在：用户先切换到目标板块，再浏览对应内容

### 4.3 导航栏

- 以前：多个分组可同时展开
- 现在：同一时间只展开一个分组，侧栏是否收起由用户手动控制

### 4.4 物料库存页

- 以前：左树右表容易上下错位，右侧存在空白区
- 现在：左右结构统一拉齐，右侧表格填满主区域

## 5. 修改背后的业务考虑

本次改动没有扩展新的业务范围，也没有新增新的对象或字段，而是把现有前端页面从“信息堆满”调整为“按任务分层”：

1. 让用户先决定要看哪一类内容。
2. 让高频操作入口更靠近业务对象本身。
3. 让布局在表格、树、时间轴这类密集界面下更稳定。

这与 PLM 页面“服务业务操作，不做展示页”的原则一致。

## 6. 风险与注意事项

### 6.1 中文乱码

本次实施过程中发现部分页面源码原本就存在乱码内容。  
本次已在修改文件中直接替换为正常中文文案，但仓库里其它未触达文件是否仍有同类问题，后续仍需继续排查。

### 6.2 移动端与窄屏

本次改动已经保留响应式降级规则，但复杂页面在窄屏下仍建议继续做二轮体验优化，尤其是：

- 时间轴区
- 大表格区
- 分段切换按钮区

## 7. 后续可继续优化项

1. 工艺路线页可进一步把板块切换与路由 query 绑定，支持刷新后保留当前位置。
2. 产品详情页可进一步补“当前板块下的二级筛选”。
3. 侧栏收起后可增加悬浮提示，提升缩栏状态下的可辨识度。
4. 库存页可进一步对树节点和右侧列表加联动统计。

## 8. 结论

本次前端实施完成了方案文档中定义的四项改造：

1. 工艺路线页分板块展示
2. 产品详情页分板块展示
3. 侧栏单分组展开，收起动作由用户手动控制
4. 库存页左右结构对齐与留白修复

改动范围控制在前端页面与交互层，未影响后端接口与数据模型。
