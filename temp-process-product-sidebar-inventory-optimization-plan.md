# 工艺路线页、产品详情页、导航栏与库存页布局优化方案

## 1. 任务理解

本次先停留在文档方案阶段，不修改代码。目标是围绕以下四类前端问题形成一份可直接指导后续实施的沉淀文档：

1. 工艺路线页面信息堆叠过多，需要在顶部增加筛选/切换栏，点击后再展示对应板块内容。
2. 产品详情页面同理，需要把主要信息区改成顶部切换后按需展示，而不是整页全部同时展开。
3. 左侧导航栏需要改成“只展开当前点击项所属分组”，并在点击进入页面后自动收起侧栏。
4. 物料库存页左侧树、右侧表格存在错位和留白，需要调整布局结构，解决白块和高度不齐的问题。

本轮只输出方案、涉及文件、改造逻辑和后续沉淀要求，不进入代码实现。

## 2. 已阅读内容

### 2.1 开发提示词

- `D:\Yuewei\资料\PLM\docs\文件沉淀\开发提示词.md`

### 2.2 本次直接相关代码

- `plm-web/src/views/process/ProcessCenterView.vue`
- `plm-web/src/views/product/ProductDetail.vue`
- `plm-web/src/layout/Sidebar.vue`
- `plm-web/src/views/inventory/InventoryCenterView.vue`
- `plm-web/src/stores/app.ts`

## 3. 所属 PLM 业务链路

本次改造属于前端交互重构，不改变 PLM 业务范围，主要服务于以下链路：

1. `Process` 对象的工艺路线浏览、确认、变更和资料挂接查看。
2. `Product` 对象的产品详情、项目推进、版本资料与质量/商务查看。
3. `Inventory` 对象的分类树检索、模具治具浏览和库存明细查看。
4. 全局导航的模块切换与页面进入效率提升。

## 4. 涉及前端页面

### 4.1 工艺路线页面

- 页面文件：`plm-web/src/views/process/ProcessCenterView.vue`

### 4.2 产品详情页面

- 页面文件：`plm-web/src/views/product/ProductDetail.vue`

### 4.3 左侧导航栏

- 页面文件：`plm-web/src/layout/Sidebar.vue`
- 状态文件：`plm-web/src/stores/app.ts`

### 4.4 物料库存页面

- 页面文件：`plm-web/src/views/inventory/InventoryCenterView.vue`

## 5. 当前问题拆解

### 5.1 工艺路线页面的问题

当前页面右侧详情区把以下内容一次性全部展示：

- 路线指标
- 概览说明
- 工序明细
- 确认与门禁
- 资料挂接
- 版本变更
- 联动影响

问题在于：

1. 首屏信息密度过高，用户进入页面后不容易快速判断“应该先看哪一块”。
2. 不同角色关注点不同，但页面没有把“概览”和“深度细节”分层。
3. 内容过长后，工序表格、门禁卡片和变更卡片混在一个滚动流中，阅读成本高。

### 5.2 产品详情页面的问题

当前页面同时展示：

- 基础信息
- 项目时间轴
- 资料区
- 商务区
- 质量区

问题在于：

1. 信息结构是“全部展开式”，没有把主任务路径和辅助信息分开。
2. 时间轴虽然是关键入口，但仍被其它区块压在长页面中。
3. 右侧商务区和质量区长期占位，会拉长页面并分散注意力。

### 5.3 导航栏的问题

当前侧栏支持折叠，但仍存在两个体验问题：

1. 多个分组可以同时展开，展开态容易越积越多。
2. 点击具体菜单进入页面后，侧栏不会自动收起，内容区横向空间被持续占用。

### 5.4 物料库存页的问题

当前页面是左树右表结构，但布局上存在以下风险：

1. 左右两栏虽然同属一个 grid，但内部高度策略不一致。
2. 右侧表格直接使用 `height="100%"`，而父级并不是始终有稳定的确定高度。
3. 这会导致右侧表格区域出现留白、错位，或者表头与左树顶部不够齐整。

## 6. 优化目标

本次前端优化希望达到以下效果：

1. 页面先让用户做“看哪一块”的选择，再展示对应内容。
2. 长页面改成分段浏览，降低视觉噪音。
3. 导航从“展开很多组”改成“聚焦当前工作模块”。
4. 左树右表结构在高度、对齐和滚动上保持稳定。
5. 后续代码改动应尽量复用现有 Vue 3 + Element Plus 页面模式，不额外引入复杂依赖。

## 7. 实施方案

### 7.1 工艺路线页面改造方案

#### 7.1.1 页面结构调整

保留左侧“路线列表”，但把右侧详情区改成“两层结构”：

1. 顶部固定保留：
   - 页面标题
   - 状态
   - 关键指标
   - 顶部板块切换栏
2. 中部内容区只展示当前所选板块，不再整页同时展开全部板块。

#### 7.1.2 顶部切换栏建议

建议把右侧详情拆成以下几个板块入口：

- `路线概览`
- `工序明细`
- `确认门禁`
- `资料挂接`
- `版本变更`
- `联动影响`

切换逻辑：

1. 默认进入 `路线概览`。
2. 用户点击任一板块，只显示该板块对应内容。
3. 表格型内容和卡片型内容不再同屏堆叠。

#### 7.1.3 交互价值

这样做的核心收益：

1. 先分流，再深入，符合“先定位任务，再看细节”的业务浏览习惯。
2. 工艺、工程、采购、质量看的是同一条路线，但关注板块不同，切换式结构更适配角色差异。
3. 右侧详情区长度可控，减少连续滚动阅读。

### 7.2 产品详情页面改造方案

#### 7.2.1 页面结构调整

保留顶部指标卡，但把中下部改成“顶部切换 + 当前内容区”的结构。

建议的一级板块：

- `基础信息`
- `项目时间轴`
- `资料区`
- `商务区`
- `质量区`

#### 7.2.2 板块职责划分

1. `基础信息`
   - 产品线
   - 型号/颜色
   - 产品编码
   - 客户来源
2. `项目时间轴`
   - 节点列表
   - 当前节点详情
   - 推进动作入口
3. `资料区`
   - BOM 版本对比
   - 版本物料明细
   - 图纸/文件
   - 模具治具摘要
4. `商务区`
   - 预计成本
   - 实际成本
   - 供应商信息
5. `质量区`
   - 测试记录
   - 结果状态
   - 问题备注

#### 7.2.3 时间轴定位

时间轴依旧是产品详情的关键业务入口，但不再要求整页所有区域始终同屏可见。  
更合理的方式是：

1. 顶部切到 `项目时间轴` 时，聚焦推进节点和当前动作。
2. 切到 `资料区` 时，聚焦版本资料和 BOM 明细。
3. 切到 `商务区` 或 `质量区` 时，单独看成本或测试，不被主链路信息打断。

### 7.3 导航栏交互方案

#### 7.3.1 改造目标

导航栏改成“单分组展开 + 进入页面后自动收起”。

#### 7.3.2 具体行为

1. 侧栏未收起时：
   - 点击某个分组标题，只展开当前分组。
   - 其他分组自动关闭。
2. 点击某个具体菜单后：
   - 执行路由跳转。
   - 自动调用侧栏收起逻辑，让主内容区获得更大宽度。
3. 当前路由所属分组仍应保留高亮识别。

#### 7.3.3 适配现有状态管理

目前 `app.ts` 中已有：

- `sidebarCollapsed`
- `toggleSidebar()`
- `setSidebarCollapsed(value)`

后续代码实施时可以复用这套状态，不需要引入新的全局状态方案。  
`Sidebar.vue` 中只需增加：

1. 当前激活分组控制
2. 点击菜单后自动调用 `setSidebarCollapsed(true)`
3. 分组展开状态由“多开”改为“单开”

### 7.4 物料库存页布局修复方案

#### 7.4.1 当前结构问题

当前页面的关键布局问题不在业务数据，而在容器高度策略：

1. 左右栏均使用 `page-panel`，但内部不是同一种伸缩模型。
2. `el-table height="100%"` 依赖父级有明确高度，否则会出现空白或撑不开。
3. 左树是自然高度，右表是百分比高度，二者容易错位。

#### 7.4.2 改造方向

将库存页改成“统一外层高度 + 内部纵向分区”：

1. 外层 `inventory-layout` 保持 grid。
2. 左栏和右栏内部都改成：
   - `display: flex`
   - `flex-direction: column`
   - `min-height: 0`
3. 右侧主区域拆为：
   - 顶部标题/说明区
   - 表格容器区
4. 表格容器区使用：
   - `flex: 1`
   - `min-height: 0`
5. `el-table` 的高度应绑定到真实可伸缩容器，而不是单纯写 `height="100%"`

#### 7.4.3 预期结果

1. 左树和右表顶部对齐。
2. 右侧表格填满可用区域，不再出现大面积白块。
3. 页面在分类节点切换时布局稳定，不会忽高忽低。

## 8. 拟改动代码文件

本轮不改代码，但后续正式实施时建议优先修改以下文件。

### 8.1 `plm-web/src/views/process/ProcessCenterView.vue`

拟改内容：

1. 新增右侧详情区板块切换状态。
2. 将当前所有详情模块拆成条件渲染。
3. 重构右侧内容区布局，避免所有 section 同时展示。

### 8.2 `plm-web/src/views/product/ProductDetail.vue`

拟改内容：

1. 新增产品详情一级板块切换状态。
2. 将基础信息、时间轴、资料区、商务区、质量区拆为分段展示。
3. 减少整页同时展开的内容量。

### 8.3 `plm-web/src/layout/Sidebar.vue`

拟改内容：

1. 把分组展开策略改为单开。
2. 菜单点击跳转后自动收起侧栏。
3. 保留当前分组高亮与激活态判断。

### 8.4 `plm-web/src/stores/app.ts`

拟改内容：

1. 视需要补充更明确的侧栏控制方法。
2. 若 `Sidebar.vue` 逻辑足够，也可以不改 store，只复用现有 `setSidebarCollapsed`。

### 8.5 `plm-web/src/views/inventory/InventoryCenterView.vue`

拟改内容：

1. 调整左右栏内部布局为统一 flex 结构。
2. 为右侧表格增加稳定的表格容器层。
3. 修复高度继承和留白问题。

## 9. 使用技术与实现方式

本次前端改造建议继续沿用现有技术，不新增复杂技术栈。

### 9.1 框架与组件

- Vue 3 `ref / computed`
- Element Plus
- 现有 `PageContainer`
- 现有 `StatusTag`

### 9.2 交互实现方式

1. 页面板块切换：
   - 适合用 `el-segmented`、`el-tabs` 或轻量按钮组
   - 推荐优先使用 `el-segmented` 或结构更紧凑的自定义按钮组
2. 条件渲染：
   - 使用 `v-if` / `v-show` 分板块展示
3. 布局修复：
   - 使用 `grid + flex`
   - 对滚动区域使用 `min-height: 0`
4. 侧栏联动：
   - 路由跳转时结合现有 Pinia store 做自动收起

## 10. 风险点与注意事项

### 10.1 中文乱码问题

当前读取部分 Vue 文件时，控制台输出出现中文乱码。  
从现象看，更像是终端显示编码问题，但也不排除部分文件文本本身已经受影响。

后续正式开发前建议先确认：

1. 文件实际编码是否为 UTF-8。
2. 页面里显示的中文是否已经异常。
3. 改造时避免在乱码状态下直接覆盖业务文案。

### 10.2 状态切换过多导致的内容跳跃

如果板块切换做得过重，页面会变得像“多个子页面拼装”，失去连续性。  
因此实施时需要保留适量的固定区：

1. 顶部标题
2. 当前状态
3. 关键指标

把这些作为全局上下文固定下来，其余内容再切换。

### 10.3 表格高度适配

库存页如果只改样式、不改容器结构，问题可能反复出现。  
因此后续实施时要优先修容器关系，再修表格高度。

## 11. 后续实施顺序建议

建议按以下顺序落地，能减少相互影响：

1. 先修复乱码确认问题
2. 再改 `Sidebar.vue` 的单开与自动收起
3. 再改 `InventoryCenterView.vue` 的布局高度问题
4. 再改 `ProcessCenterView.vue` 的板块切换
5. 最后改 `ProductDetail.vue` 的板块切换

原因：

1. 导航和库存页属于结构型改动，风险较小，能先把整体体验稳定下来。
2. 工艺路线页和产品详情页属于内容组织重构，适合在基础布局稳定后再做。

## 12. 文档沉淀要求

后续正式改代码时，除了提交实现，还应同步补一份实施记录文档，至少包含：

1. 修改目标
2. 修改页面
3. 具体改动文件
4. 每个文件修改了什么
5. 为什么这样改
6. 使用了哪些技术手段
7. 页面交互如何变化
8. 是否有样式/布局兼容点
9. 是否存在待后续继续优化事项

## 13. 本轮结论

本轮不建议直接小修小补，而是按“信息分层 + 单任务聚焦 + 稳定布局”的思路整体处理。

结论如下：

1. 工艺路线页和产品详情页都应从“全展开”改为“顶部切换后按需展示”。
2. 导航栏应改为“当前分组单开，点击菜单后自动收起”。
3. 物料库存页的错位和留白问题应通过统一容器高度与表格承载结构解决。
4. 正式实施前，应先确认中文显示与文件编码状态，避免把乱码带入新改动。

---

本文件为方案沉淀文档，当前未修改任何代码。
