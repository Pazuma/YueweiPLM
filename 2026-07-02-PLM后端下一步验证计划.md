# PLM 后端下一步验证计划

> 日期：2026-07-02  
> 文档类型：后端验证沉淀  
> 当前状态：后端已完成编译、启动、OpenAPI、产品列表读取、产品创建写入验证。  
> 下一目标：验证 Product 基础业务链路是否稳定，为后续工作台时间轴、BOM、工艺路线、文件归档实施做准备。

## 1. 当前已通过验证

已经完成：

```text
1. Maven 可用
2. mvn test 通过，BUILD SUCCESS
3. Spring Boot 启动成功
4. Tomcat 运行在 8080
5. /v3/api-docs 可访问
6. /api/v1/products GET 可访问
7. /api/v1/products POST 可创建产品
8. 数据库读写链路已打通
```

说明：

```text
当前后端不是只“能启动”，而是已经证明：
PowerShell -> HTTP API -> Spring Boot -> MyBatis-Plus -> PostgreSQL -> ResponseVO
这条基础链路可用。
```

## 2. 下一步验证总目标

下一步不急着开发新模块，先把已有 Product 基础链路验证完整。

验证顺序：

```text
1. 记录测试产品 ID
2. 查询产品详情
3. 更新产品基础字段
4. 冻结产品
5. 验证冻结后禁止修改
6. 验证 draft 不能直接发布
7. 创建 developing/reviewing 状态测试路径或记录当前限制
8. 验证不存在 ID 的错误返回
9. 验证缺字段错误返回
10. 形成当前后端问题清单
```

这一步完成后，再进入：

```text
简单登录
操作日志
工作台进行中项目
项目时间轴
BOM
工艺路线
时间轴文件上传
```

## 3. 验证前准备

### 3.1 确认后端仍在运行

正在运行的终端不要关闭。应看到类似：

```text
Tomcat started on port 8080
Started PlmApplication
```

如果后端已关闭，重新启动：

```powershell
cd D:\Yuewei\git\YUEWEI\plm-server
& "D:\Yuewei\IntelliJ IDEA 2026.1.2\plugins\maven\lib\maven3\bin\mvn.cmd" spring-boot:run
```

### 3.2 打开第二个 PowerShell 终端

后续接口测试在第二个终端执行，避免影响后端运行进程。

进入目录：

```powershell
cd D:\Yuewei\git\YUEWEI\plm-server
```

### 3.3 获取测试产品 ID

执行：

```powershell
$list = Invoke-RestMethod http://localhost:8080/api/v1/products
$list.data.content | Format-Table productId, productCode, productName, status, lockStatus
```

选择刚才创建的测试产品，把 ID 记录下来：

```powershell
$productId = 这里换成真实ID
```

示例：

```powershell
$productId = 1
```

备注：

```text
后面所有命令都依赖 $productId。
如果 ID 不对，会测到“产品不存在”。
```

## 4. 验证步骤

### 4.1 查询产品详情

目的：

```text
验证 GET /api/v1/products/{id}
验证后端能按主键读取单个产品
验证返回字段是否完整
```

命令：

```powershell
Invoke-RestMethod "http://localhost:8080/api/v1/products/$productId"
```

预期：

```text
code=0
message=success
data.productId 等于 $productId
data.productName 是测试产品
data.status 初始通常是 draft
data.lockStatus 初始通常是 unlocked
```

如果失败：

```text
404/400 产品不存在：检查 $productId 是否正确
500：查看后端运行终端的 Unhandled exception 和 Caused by
```

### 4.2 更新产品基础字段

目的：

```text
验证 PUT /api/v1/products/{id}
验证未冻结、未发布的产品可以修改
验证 updatedBy 能写入
```

命令：

```powershell
$updateBody = @{
  productName = "DEV-PLM测试产品-已更新"
  seriesName = "DEV测试系列-更新"
  model = "DEV-MODEL-UPDATED"
  color = "透明黑"
  material = "TPU+PC"
  packageType = "测试包装-更新"
  surfaceProcess = "喷涂"
  coreProcess = "注塑+组装"
  updatedBy = "dev"
  remark = "后端更新接口冒烟测试"
} | ConvertTo-Json

Invoke-RestMethod `
  -Uri "http://localhost:8080/api/v1/products/$productId" `
  -Method Put `
  -ContentType "application/json" `
  -Body $updateBody
```

预期：

```text
code=0
message=success
data.productName 变为 DEV-PLM测试产品-已更新
data.lockStatus 仍为 unlocked
```

备注：

```text
当前后端 update 会检查 lockStatus=frozen 和 status=released。
所以这一步必须在 freeze/publish 前做。
```

### 4.3 冻结产品

目的：

```text
验证 POST /api/v1/products/{id}/freeze
验证冻结字段能写入
验证 lockStatus 变为 frozen
```

命令：

```powershell
Invoke-RestMethod `
  -Uri "http://localhost:8080/api/v1/products/$productId/freeze?operator=dev&reason=后端冒烟测试冻结" `
  -Method Post
```

然后查询详情：

```powershell
Invoke-RestMethod "http://localhost:8080/api/v1/products/$productId"
```

预期：

```text
freeze 接口返回 code=0
详情中 lockStatus=frozen
frozenBy=dev
freezeReason=后端冒烟测试冻结
frozenAt 有时间
```

### 4.4 验证冻结后禁止修改

目的：

```text
验证冻结保护是否生效
验证业务错误是否返回明确 message
```

命令：

```powershell
$updateFrozenBody = @{
  productName = "DEV-冻结后不应允许修改"
  updatedBy = "dev"
} | ConvertTo-Json

Invoke-RestMethod `
  -Uri "http://localhost:8080/api/v1/products/$productId" `
  -Method Put `
  -ContentType "application/json" `
  -Body $updateFrozenBody
```

预期：

```text
不应成功
应返回 400 或明确业务错误
message 包含 当前版本已冻结
```

备注：

```text
如果这里返回 500，说明异常处理还要优化。
如果这里居然成功，说明冻结保护有严重问题。
```

### 4.5 验证 draft 不能直接发布

目的：

```text
验证 Product 状态流转规则
当前 ProductStatusConstants 不允许 draft -> released
```

命令：

```powershell
$publishBody = @{ operator = "dev" } | ConvertTo-Json

Invoke-RestMethod `
  -Uri "http://localhost:8080/api/v1/products/$productId/publish" `
  -Method Post `
  -ContentType "application/json" `
  -Body $publishBody
```

预期：

```text
如果产品 status=draft，应返回业务错误
message 类似 不允许从 draft 变更为 released
```

备注：

```text
这不是坏事。
它说明当前后端确实在执行状态流转约束。
```

### 4.6 验证不存在 ID

目的：

```text
验证资源不存在时返回明确错误
```

命令：

```powershell
Invoke-RestMethod http://localhost:8080/api/v1/products/99999999
```

预期：

```text
不应返回成功
应返回产品不存在
```

如果 PowerShell 直接抛异常，可以用下面命令查看错误内容：

```powershell
try {
  Invoke-RestMethod http://localhost:8080/api/v1/products/99999999
} catch {
  $_.Exception.Response.StatusCode.value__
  $_.ErrorDetails.Message
}
```

### 4.7 验证缺字段创建

目的：

```text
验证参数校验是否生效
验证缺 productType 是否返回 400，而不是 500
```

命令：

```powershell
$badBody = @{
  productName = "DEV-缺产品类型"
  versionNo = "V1.0"
  createdBy = "dev"
} | ConvertTo-Json

try {
  Invoke-RestMethod `
    -Uri http://localhost:8080/api/v1/products `
    -Method Post `
    -ContentType "application/json" `
    -Body $badBody
} catch {
  $_.Exception.Response.StatusCode.value__
  $_.ErrorDetails.Message
}
```

预期：

```text
HTTP 状态码：400
message：参数校验失败
errors 中包含 productType
```

备注：

```text
你之前缺 productType 时出现过 500。
如果这里仍是 500，需要优先修复异常处理或排查真实异常原因。
```

## 5. 结果记录模板

测试时建议记录：

```text
测试产品 ID：
测试产品编码：
测试数据库：
后端端口：8080

1. 查询详情：通过 / 不通过
2. 更新产品：通过 / 不通过
3. 冻结产品：通过 / 不通过
4. 冻结后修改拦截：通过 / 不通过
5. draft 直接发布拦截：通过 / 不通过
6. 不存在 ID 错误：通过 / 不通过
7. 缺字段校验：通过 / 不通过
```

## 6. 下一步判断

### 6.1 如果全部通过

可以进入第一阶段后端开发：

```text
1. 简单登录
2. OperationLogService
3. 工作台进行中项目列表
4. 项目时间轴查询
5. 时间轴节点推进 / 确认 / 退回
```

### 6.2 如果冻结或状态流转不符合预期

先修复 Product 基础生命周期：

```text
ProductStatusConstants
ProductServiceImpl.freeze
ProductServiceImpl.publish
ProductServiceImpl.update
GlobalExceptionHandler
```

### 6.3 如果缺字段仍返回 500

优先修复异常处理：

```text
GlobalExceptionHandler.handleValidation
Controller @Valid 是否生效
请求 Content-Type 是否为 application/json
后端日志中的真实 Caused by
```

### 6.4 如果数据库数据污染

当前测试数据都用 `DEV-` 前缀，后续可以通过数据库工具人工清理，或等后端提供软删除接口后清理。

## 7. 备注

```text
1. 这份文档只验证当前已有 Product 后端基础链路，不验证尚未实现的时间轴、BOM、工艺和文件上传。
2. 当前没有自动化测试源码，所以 Maven test 只能证明编译通过，不能证明业务逻辑全部正确。
3. 后续进入开发时，应逐步补 JUnit / Spring Boot Test。
4. 当前 Java 运行版本为 26，pom 目标是 Java 17；短期可用，长期建议配置 JDK 17，减少 Maven 反射警告。
5. 当前安全配置仍出现 Spring Security 默认密码提示，说明简单登录还没实现，这是后续第一批开发任务。
```
