# PLM 后端总体方案与项目结构设计

> 版本：v1.1  
> 日期：2026-07-02  
> 适用阶段：PLM 核心业务闭环后端联调前  
> 业务修正：工程部两个人按流程在工作台推进项目，围绕进行中项目时间轴完成审批、进度查看、BOM 管理、工艺路线和文件归档。

## 1. 核心结论

第一阶段后端不应按“多个页面分别补接口”的方式建设，而应采用 **工作台进行中项目时间轴驱动的模块化单体架构**。

实际业务主线是：

```text
登录
-> 工作台查看进行中项目
-> 打开项目当前时间轴节点
-> 工程部按节点推进 / 确认
-> 在节点中维护 BOM、工艺路线和相关资料
-> 在时间轴节点上传文件
-> 文件元数据进入 plm_attachment
-> 文件中心归档、检索、查看
-> 项目冻结 / 发布 / 归档
```

这意味着后端第一阶段的核心聚合不是单独的 Order、Product、BOM、Process 页面集合，而是：

```text
Project / Product Timeline Aggregate
```

BOM 管理、工艺路线、文件上传、状态推进和简化审批，都围绕这个项目时间轴聚合发生。

## 2. 已确认业务事实

```text
真实使用人员：工程部两个人左右
权限方式：两位正式用户拥有全部权限
核心入口：工作台 / 项目管理的进行中项目
推进方式：工程部按业务流程在项目时间轴上推动节点
审批方式：第一版表现为时间轴节点确认、门禁节点确认、状态推进
文件上传：发生在工作台进行中项目的时间轴节点上
文件归档：上传后写入 plm_attachment，并在文件中心归档查看
BOM 管理：项目推进中的核心成果物，必须能按 Product / 版本维护
工艺路线：项目推进中的核心成果物，必须能按 Product / 版本维护
数据库：沿用现有远程 PostgreSQL 表结构为主
部署方式：本机 Spring Boot 后端 + 远程 PostgreSQL
文件存储：本机服务器目录，数据库保存元数据
```

## 3. 当前前端界面对应关系

前端已经体现了正确的业务入口：

```text
项目管理 / 进行中
├─ 新产品线重要节点时间轴
│  ├─ 立项确认
│  ├─ 设计确认
│  ├─ 开模试模
│  ├─ 样品与工艺
│  ├─ 小批与 MX 验证
│  └─ 投产决策
├─ 新型号线重要节点时间轴
│  ├─ 扩展确认
│  ├─ 差异设计
│  ├─ 模具判断
│  ├─ 差异验证
│  ├─ 小批与 MX 验证
│  └─ 冻结发布
└─ 项目详情
   ├─ 当前节点
   ├─ 项目流程
   ├─ BOM 管理
   ├─ 工序明细 / 工艺路线
   ├─ 资料区
   ├─ 质量区
   └─ 生产资料
```

后端应优先支撑这一套交互，而不是优先做独立审批中心或独立文件中心上传。

## 4. 第一阶段架构方案

### 4.1 技术栈

```text
后端框架：Spring Boot 3
数据访问：MyBatis-Plus
数据库：PostgreSQL
接口风格：REST API + JSON
接口前缀：/api/v1
文件上传：Spring MultipartFile + 本地文件目录
认证方式：简单 token
文档：springdoc-openapi
运行方式：本机后端 + 远程 PostgreSQL
```

### 4.2 架构风格

采用模块化单体，不拆微服务，不引入复杂工作流引擎。

```text
com.yuewei.plm
├─ common
├─ security
├─ modules
│  ├─ dashboard
│  ├─ project
│  ├─ timeline
│  ├─ product
│  ├─ bom
│  ├─ process
│  ├─ attachment
│  ├─ filecenter
│  ├─ lifecycle
│  └─ system
└─ infrastructure
   ├─ storage
   ├─ logging
   └─ integration
```

第一阶段允许现有 `controller / service / repository / entity` 平铺结构继续存在；新增模块优先按 `modules/<module>` 组织，后续再逐步迁移。

## 5. 后端核心聚合

### 5.1 Project / Product Timeline Aggregate

这是第一阶段最重要的后端聚合。

```text
职责：
├─ 查询工作台进行中项目列表
├─ 查询项目详情与当前节点
├─ 查询完整时间轴节点
├─ 推进当前节点
├─ 确认门禁节点
├─ 退回上一节点或退回修改
├─ 关联 BOM 版本
├─ 关联工艺路线版本
├─ 关联节点上传文件
├─ 计算项目进度
└─ 写操作日志
```

建议接口：

```text
GET  /api/v1/workbench/projects/in-progress
GET  /api/v1/projects/{projectId}
GET  /api/v1/projects/{projectId}/timeline
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/advance
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/confirm
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/return
GET  /api/v1/projects/{projectId}/summary
```

说明：

- `projectId` 第一阶段可以直接使用 `productId` 承载，避免新增复杂项目主表。
- 如果现有表中已有项目表或项目字段，则优先复用。
- 时间轴节点定义可以先在后端常量或字典中维护，后续再数据库化。

### 5.2 简化审批

第一版审批不做独立审批流引擎。

```text
审批第一版 =
时间轴节点确认
+ 门禁节点确认
+ 状态推进
+ 操作日志
```

适合当前“两个人都有全权限”的使用方式。审批中心页面可以先保留 mock 或仅查看节点日志，后续需要多人分工时再升级为正式审批任务。

### 5.3 BOM 管理

BOM 是项目推进中的核心成果物，不是孤立页面。

```text
职责：
├─ 按 Product 查询 BOM 版本
├─ 维护 BOM 头
├─ 维护 BOM 明细
├─ 支持 EBOM / MBOM / 包装 BOM
├─ 支持版本号
├─ 支持冻结
├─ 与时间轴节点关联
└─ BOM 变更写操作日志
```

建议接口：

```text
GET    /api/v1/projects/{projectId}/boms
GET    /api/v1/boms/{bomId}
POST   /api/v1/projects/{projectId}/boms
PUT    /api/v1/boms/{bomId}
POST   /api/v1/boms/{bomId}/items
PUT    /api/v1/boms/{bomId}/items/{itemId}
DELETE /api/v1/boms/{bomId}/items/{itemId}
POST   /api/v1/boms/{bomId}/freeze
POST   /api/v1/projects/{projectId}/timeline/{nodeKey}/link-bom
```

### 5.4 工艺路线

工艺路线也是项目推进中的核心成果物，重点服务“样品与工艺”“差异验证”“冻结发布”等节点。

```text
职责：
├─ 按 Product 查询工艺路线
├─ 维护工艺路线版本
├─ 维护工序顺序或工序摘要
├─ 记录工艺参数摘要
├─ 关联 SOP / SIP 文件
├─ 支持冻结
├─ 与时间轴节点关联
└─ 工艺变更写操作日志
```

建议接口：

```text
GET  /api/v1/projects/{projectId}/process-routes
GET  /api/v1/process-routes/{processId}
POST /api/v1/projects/{projectId}/process-routes
PUT  /api/v1/process-routes/{processId}
POST /api/v1/process-routes/{processId}/freeze
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/link-process
```

### 5.5 时间轴文件上传与文件中心归档

文件上传入口第一阶段应放在时间轴节点中。

```text
时间轴节点上传
-> 保存文件到本机目录
-> 写入 plm_attachment
-> 记录 projectId / productId
-> 记录 timeline_node_key
-> 记录 business_type / business_id
-> 写操作日志
-> 文件中心可按项目、节点、分类查询
```

建议接口：

```text
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/attachments
GET  /api/v1/projects/{projectId}/timeline/{nodeKey}/attachments
GET  /api/v1/file-center/attachments
GET  /api/v1/attachments/{id}/download
DELETE /api/v1/attachments/{id}
```

上传元数据建议：

```text
projectId / productId
timelineNodeKey
businessType: project / product / bom / process / quality
businessId
fileCategory: drawing / bom / sop / sip / test_report / customer_confirm / production_doc / other
versionNo
remark
```

文件中心定位：

```text
文件中心是归档、检索、查看、下载入口
不是第一阶段主上传入口
```

## 6. 数据库使用原则

第一阶段优先沿用现有表：

```text
plm_product
plm_order
plm_product_bom
plm_product_bom_item
plm_process
plm_attachment
plm_operation_log
sys_user
```

如现有 `plm_attachment` 缺少时间轴节点字段，建议通过增量脚本补充：

```text
project_id
timeline_node_key
stage_key
```

如果现有字段可表达，则优先复用，不强行改表。

远程库规则：

```text
严禁重跑 V1.0 / V1.1 全量初始化脚本
结构变化只走增量迁移
删除优先软删除
测试数据必须有 DEV / TEST 标记
所有节点推进、BOM 修改、工艺修改、文件上传都写 plm_operation_log
```

## 7. 状态流转设计

第一版状态围绕项目时间轴，而不是围绕独立审批任务。

```text
pending -> current -> completed
current -> returned
completed -> archived
```

项目级状态可映射为：

```text
draft -> developing -> reviewing -> frozen -> released -> archived
developing/reviewing -> abandoned
```

节点动作：

```text
advance：推进到下一节点
confirm：确认当前节点或门禁节点
return：退回修改
freeze_bom：冻结 BOM 成果
freeze_process：冻结工艺成果
upload_attachment：上传节点资料
publish：发布项目 / Product
archive：归档项目
```

## 8. 推荐开发顺序

```text
1. 配置本机后端连接远程 PostgreSQL
2. 简单登录与当前用户上下文
3. 操作日志基础能力
4. 工作台进行中项目列表接口
5. 项目详情与时间轴查询接口
6. 时间轴节点推进 / 确认 / 退回接口
7. BOM 管理接口，并关联项目时间轴
8. 工艺路线接口，并关联项目时间轴
9. 时间轴节点文件上传接口
10. 文件中心归档查询与下载接口
11. 冻结 / 发布 / 归档状态动作
12. 前端从 mock 切真实接口联调
```

## 9. 第一阶段不做或暂缓

```text
暂缓完整 RBAC
暂缓复杂审批流引擎
暂缓独立审批中心真实任务流
暂缓 ERP / MES 正式集成
暂缓供应商完整协同门户
暂缓复杂成本核算模型
暂缓质量管理完整闭环
暂缓报表中心复杂聚合计算
暂缓大批量导入
```

## 10. 最终建议

第一阶段后端要围绕工程部真实工作方式建设：

```text
工程部两个人在工作台看项目
在进行中项目时间轴上推进节点
在节点里确认 BOM、工艺路线和资料
在节点里上传文件
文件自动归档到文件中心
关键动作全部留痕
项目最终冻结、发布、归档
```

这条链路打通后，PLM 才算真正进入可试运行状态。后续再把审批中心、质量、成本、供应商、库存、ERP/MES 等模块逐步接入这条时间轴主线。
