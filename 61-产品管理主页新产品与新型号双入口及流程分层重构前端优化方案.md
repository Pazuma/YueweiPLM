# 61-产品管理主页新产品与新型号双入口及流程分层重构前端优化方案

## 1. 任务理解
本次任务只做方案沉淀，不修改 `plm-web` 项目代码。

结合你这次的要求，产品管理主页需要重新设计一版，并且核心不是简单换个排版，而是要把下面两件事在首页就讲清楚：

1. 什么叫 `新产品`。
2. 什么叫 `新型号`。
3. 这两类对象分别该走什么制作流程。
4. 使用人进入页面后，应该点哪里、先看什么、下一步做什么。

也就是说，这次不是继续强化“阶段筛选”，而是要把产品管理首页从“统一队列表”升级成“业务分流工作台”。

本轮目标可以总结为：

```text
进入产品管理主页
-> 先判断这是新产品还是新型号
-> 在主页完成业务分流与对象定位
-> 进入对应工作台
-> 从现有入口继续处理后续动作
```

需要特别强调的是：

1. `新产品` 与 `新型号` 仍然都属于 `Product` 体系。
2. 本轮只是产品管理主页前端方案重构，不引入新的根对象。
3. 本轮只输出沉淀文档，不修改前端代码，不扩展后端接口。
4. 本轮只允许调整 `主页产品管理模块`，其他模块禁止改动。

## 2. 执行前阅读结论
本次方案基于已阅读的开发文档和现有页面代码整理，重点遵循以下边界：

1. `Product` 是产品主数据主对象，`SKU` 不是独立根对象。
2. `新产品线` 与 `新型号线` 是同一主对象下的两种不同推进模式。
3. 系统设计要贴合真实研发场景，但本轮只落在 `主页产品管理模块`。
4. 首页要服务“判断与分流”，其他页面本轮保持现状，不进入改造范围。

结合现有代码可确认，本轮直接涉及页面只有：

- `plm-web/src/views/product/ProductList.vue`

其他页面仅作为现状边界参考，不纳入本轮修改范围。

当前系统已经有：

1. `productType = product_line / model_variant` 的数据边界。
2. 新产品线与新型号线不同的阶段定义。
3. 产品管理主页已经具备阶段入口、重点对象和产品队列表的基本骨架。

因此本轮最合理的做法不是联动其他页面，而是在 `ProductList.vue` 内部重做主页的信息分流方式。

## 3. 所属 PLM 业务链路
本次调整属于 `产品主数据入口分流 + 研发主流程可视化` 的首页级改造。

涉及核心对象建议理解为：

- `Product`
- `Process`
- `BOM`
- `Project`
- `File`
- `Quality`

其中：

1. `新产品` 是完整研发链路对象，强调从立项到发布的全流程推进。
2. `新型号` 是基于已有产品线的差异扩展对象，强调父产品继承和差异验证。
3. `BOM / File / Quality / Process` 都不应在首页各自散开，而应服务于产品主页的判断与推进。

## 4. 新产品与新型号的业务区别
这部分是本次首页必须显式表达的重点。

### 4.1 新产品的业务定义
`新产品` 指的是一条新的产品线、新的产品方案或新的完整开发对象。

典型特点是：

1. 要从立项开始。
2. 要走完整的设计、开模、样品、工艺、BOM、资料冻结、量产验证链路。
3. 很多资料、模具、测试、工艺都不是继承，而是从零建立或完整重跑。

建议首页上把 `新产品` 明确解释为：

```text
没有可直接继承的成熟父产品，需要走完整研发链路的产品对象。
```

### 4.2 新型号的业务定义
`新型号` 指的是基于已有产品线做出来的新机型扩展、新差异版本或子版本。

典型特点是：

1. 一定依附父产品。
2. 不需要把整条研发链路全部重跑。
3. 重点是确认差异项、差异 BOM、差异工艺、差异测试和版本冻结。
4. 模具可能是改模、重开模，也可能无需变更。

建议首页上把 `新型号` 明确解释为：

```text
基于已有产品线做出的型号扩展对象，重点管理差异项，而不是重复走整条研发链路。
```

### 4.3 两者流程差异的业务结论
首页必须让用户一眼看到下面这个判断：

```text
如果这是一个全新的产品方向 -> 走新产品入口
如果这是在已有产品线上增加新机型 / 新差异版本 -> 走新型号入口
```

这条判断如果不在主页讲清楚，后面的阶段、节点、详情页都会越做越复杂。

## 5. 当前页面问题判断
结合当前 `ProductList.vue` 实现，主页已经有一定基础，但仍有 6 个关键问题。

### 5.1 已区分统计，但没有真正分流入口
当前页面里虽然有：

- `新产品线`
- `新型号线`

两个统计卡片，但它们还只是筛选器，不是明确的业务入口。

这会导致用户知道“系统里有这两类对象”，但仍然不知道“我现在该走哪条路”。

### 5.2 页面主轴仍然是阶段，不是业务对象
当前首页最强的结构是：

- 立项确认
- 设计差异
- 模具处理
- 样品签核
- 工艺 / BOM
- 小批验证
- MX 验证
- 冻结发布

这种设计更像项目推进看板，而不是产品入口工作台。

问题在于：

1. 阶段顺序是系统视角。
2. 新产品 / 新型号判断才是业务使用者的第一视角。

### 5.3 新建动作过于笼统
当前顶部只有一个：

- `新建产品`

但真实业务里需要至少区分：

1. `新建新产品`
2. `新建新型号`

否则使用人点进去之前，仍然不知道自己即将创建的对象属于哪种流程。

### 5.4 列表把两类对象混在一起看
当前产品队列表里同时展示：

1. 新产品线。
2. 新型号线。

虽然表格里有 `流程类型` 字段，但还是要靠用户自己二次识别，不够高效。

### 5.5 首页没有直接告诉用户“怎么用”
你这次特别强调要让使用人知道怎么用、提高效率。

而当前首页缺少一个明确的指导层：

1. 什么情况选新产品。
2. 什么情况选新型号。
3. 两者完成路径分别是什么。

### 5.6 当前主页边界说明还不够明确
当前主页没有明确告诉用户：

1. 本页负责什么。
2. 本页不负责什么。
3. 用户应先在主页完成哪一步判断。

这样会让使用人把“入口判断、流程识别、对象筛选、后续处理”混在一起理解，增加上手成本。

## 6. 重设计目标
本次产品管理主页重设计建议围绕 4 个目标展开。

### 6.1 目标一：先分流，再管理
首页第一层不再先问“你当前处于哪个阶段”，而是先问：

```text
这是新产品，还是新型号？
```

### 6.2 目标二：入口即说明
每个入口不仅是一个按钮，还要直接附带：

1. 适用场景。
2. 流程摘要。
3. 当前待办数量。
4. 推荐动作。

### 6.3 目标三：列表按业务语义拆开
不要只靠一个 `流程类型` 字段做提示，而是把列表本身按 `新产品工作台` 和 `新型号工作台` 拆开。

### 6.4 目标四：首页做判断，其他模块保持原状
首页只承担：

1. 识别对象。
2. 进入对应流程。
3. 看当前重点对象。
4. 提供清晰的后续动作入口。

本轮明确不承担：

1. 改造详情页。
2. 改造新建表单页。
3. 改造 SKU 页面。
4. 改造其他业务模块。

## 7. 首页信息架构重设计方案
建议把产品管理主页重构为 4 层结构。

### 7.1 第一层：双入口分流区
放在页面最上方，作为首页真正主入口。

建议做成两个并列的大卡片：

1. `新产品开发`
2. `新型号扩展`

每张卡片建议同时展示：

- 适用场景
- 流程长度
- 当前进行中数量
- 当前阻塞数量
- 主操作按钮

示意结构：

```text
┌ 新产品开发 ─────────────────────┐  ┌ 新型号扩展 ─────────────────────┐
│ 适用于：全新产品线 / 全新方案      │  │ 适用于：已有产品线下的新机型扩展 │
│ 流程：完整研发链路                 │  │ 流程：差异验证链路               │
│ 当前进行中：8                      │  │ 当前进行中：13                   │
│ 当前阻塞：2                        │  │ 当前阻塞：4                      │
│ [新建新产品] [查看新产品工作台]    │  │ [新建新型号] [查看新型号工作台]  │
└──────────────────────────────────┘  └──────────────────────────────────┘
```

### 7.2 第二层：使用判断说明区
这一层的作用不是装饰，而是降低误操作。

建议在双入口下方加一条轻量说明条：

```text
如果没有可继承父产品 -> 走新产品
如果基于已有产品线新增机型 / 差异版本 -> 走新型号
```

这里建议做成 `何时使用新产品 / 何时使用新型号` 的对照提示，而不是长段说明文字。

### 7.3 第三层：双工作台切换区
建议把当前首页的主体列表改造成三段式切换：

1. `新产品工作台`
2. `新型号工作台`
3. `全部任务`

默认推荐进入 `新产品工作台` 或 `新型号工作台`，而不是默认直接看“全部任务”。

### 7.4 第四层：当前重点对象区
保留当前“重点对象”思路，但按业务类型分组展示。

建议展示方式改成：

1. 新产品重点对象 3 条。
2. 新型号重点对象 3 条。

这样管理层或项目经理一打开首页，就能直接看到两条业务线分别卡在哪里。

## 8. 两条流程在首页的表达方式
### 8.1 新产品流程建议显示为完整链路
首页不需要把 22 步全部平铺，但要把关键阶段抽象成 6 段：

```text
立项 -> 设计 -> 模具 -> 样品 -> 工艺/BOM -> 验证发布
```

建议每个新产品卡片或列表行都标识当前落在哪一段。

### 8.2 新型号流程建议显示为差异链路
首页同样不需要把 16 步全部平铺，但要突出“依附父产品”和“差异处理”。

建议抽象成 6 段：

```text
选择父产品 -> 差异确认 -> 模具判断 -> 差异验证 -> 差异BOM/资料 -> 子版本发布
```

### 8.3 首页必须显式展示“模具判断分支”
新型号和新产品最容易混淆的地方之一，就是模具处理。

建议新型号工作台里明确显示：

- `改模`
- `新开模`
- `无需模具变更`

这样使用者就知道这不是完整开模流程，而是差异分支。

## 9. 列表结构重设计建议
### 9.1 新产品工作台列表字段
建议 `新产品工作台` 列表字段以完整研发链路为主：

- 产品编码
- 产品名称
- 立项来源
- 当前阶段
- 当前节点
- 模具状态
- BOM 主版本
- 资料完整率
- 负责人
- 操作

这里的重点是让用户先判断：

1. 新产品现在推进到哪。
2. 是不是已经进入完整资料阶段。
3. 是否接近冻结或发布。

### 9.2 新型号工作台列表字段
建议 `新型号工作台` 列表字段以差异扩展为主：

- 子版本编码
- 子版本名称
- 父产品
- 新型号 / 新机型
- 差异范围
- 模具策略
- 差异 BOM 版本
- 当前节点
- 负责人
- 操作

这里最重要的是把下面三个信息显式化：

1. 这个新型号挂在哪个父产品下。
2. 它改了什么。
3. 它现在处于差异流程的哪一步。

### 9.3 全部任务视图的角色
`全部任务` 应该降级为全局巡检视图，而不是默认主视图。

它适合：

1. 管理层横向查看总池子。
2. 负责人统一检索。
3. 跨流程做风险筛选。

但不适合作为普通业务用户的默认入口。

## 10. 首页交互方案
### 10.1 顶部主操作按钮重构
当前只有 `新建产品`，建议在主页模块内改成：

1. `新建新产品`
2. `新建新型号`

必要时把 `新建新型号` 做成强调按钮，因为它对日常业务更高频。

### 10.2 双入口卡片的点击行为
建议交互如下：

1. 点击 `新产品开发` 卡片主体 -> 切到 `新产品工作台`
2. 点击 `新型号扩展` 卡片主体 -> 切到 `新型号工作台`
3. 点击卡片按钮 -> 直接进入对应新建流程

### 10.3 首页增加“如何判断”快捷帮助
建议在入口卡片附近加入一条轻帮助弹层或说明：

```text
不知道该建新产品还是新型号？
先看是否存在可继承父产品。
```

这个帮助非常短，但对降低误建对象很有价值。

### 10.4 重点对象支持按类型分栏
建议当前重点对象改成左右两栏：

1. 左栏：新产品重点推进
2. 右栏：新型号重点推进

避免当前混排后，用户还要先辨认对象类型。

## 11. 涉及前端页面
本次核心页面为：

- `plm-web/src/views/product/ProductList.vue`

本轮禁止改动的相关页面为：

- `plm-web/src/views/product/ProductDetail.vue`
- `plm-web/src/views/product/ProductEdit.vue`
- `plm-web/src/views/product/SkuView.vue`

页面边界建议如下：

1. `ProductList.vue` 负责入口分流与工作台。
2. `ProductDetail.vue` 本轮不改，只保留既有行为。
3. `ProductEdit.vue` 本轮不改，只复用既有新建入口。
4. `SkuView.vue` 本轮不改，不并入产品主页主流程。

## 12. 涉及后端模块
本次不涉及正式后端模块改造。

本轮仍以当前前端已有数据结构为基础：

- `plm-web/src/types/product.ts`
- `plm-web/src/api/modules/product.ts`
- `plm-web/src/mock/data.ts`

也就是说：

1. 本轮只做前端展示结构和交互分流重构。
2. 不新增正式 API。
3. 不新增数据库表。
4. 不改变 `Product` 的领域边界。

## 13. 涉及数据对象 / 状态 / API
### 13.1 当前已有数据基础
当前 `ProductSummary` 已经具备足够支撑首页分流的字段：

```ts
export interface ProductSummary {
  productId: number
  parentProductId?: number | null
  productCode: string
  productName: string
  productType: 'product_line' | 'model_variant'
  seriesName: string
  model: string
  color: string
  ownerUserName: string
  versionNo: string
  status: CommonStatus
  currentStage: string
  currentStepNo?: number
  completionRate: number
  activeBomVersion: string
  lifecycle?: ProductLifecycle
  moldAction?: MoldAction | null
  nextAction?: string | null
  gateSummary?: string | null
}
```

这意味着本轮首页重构并不缺核心字段。

### 13.2 本轮建议新增的前端派生结构
为了让首页更清晰，建议增加前端派生展示模型，而不是改后端结构：

```ts
type ProductWorkbenchView = 'product-line' | 'model-variant' | 'all'

interface ProductEntryCard {
  key: ProductWorkbenchView
  title: string
  description: string
  scenarioHint: string
  flowHint: string
  activeCount: number
  blockedCount: number
  primaryActionLabel: string
}
```

这里的重点是：

1. 用前端派生对象承接首页入口卡片。
2. 不需要改后端接口。
3. 所有统计都可由当前 `ProductSummary[]` 计算得出。

### 13.3 当前 API 边界
本轮不新增 API。

建议继续复用：

```ts
getProductList()
```

首页重构只需要改变 `ProductList.vue` 的呈现方式和交互分流，不需要新增接口。

## 14. 计划修改的代码结构
如果后续正式落地，建议主要修改：

- `plm-web/src/views/product/ProductList.vue`

如需补充更明确的 mock 提示文案，可能会涉及：

- `plm-web/src/api/modules/product.ts`
- `plm-web/src/mock/data.ts`

但本轮只是方案文档，不要求实际修改。

## 15. 前端代码草稿
### 15.1 顶部工作台视图状态示意
```ts
type ProductWorkbenchView = 'product-line' | 'model-variant' | 'all'

const activeWorkbench = ref<ProductWorkbenchView>('product-line')

const entryCards = computed(() => {
  const source = rows.value

  const productLines = source.filter((item) => item.productType === 'product_line')
  const modelVariants = source.filter((item) => item.productType === 'model_variant')

  return [
    {
      key: 'product-line',
      title: '新产品开发',
      description: '适用于没有可直接继承父产品的全新产品对象。',
      scenarioHint: '从立项开始走完整研发链路。',
      flowHint: '立项 -> 设计 -> 模具 -> 样品 -> 工艺/BOM -> 验证发布',
      activeCount: productLines.filter((item) => item.status !== 'released').length,
      blockedCount: productLines.filter((item) => !item.frozenFlag && item.status === 'reviewing').length,
      primaryActionLabel: '新建新产品'
    },
    {
      key: 'model-variant',
      title: '新型号扩展',
      description: '适用于已有产品线下的新机型、新差异版本或子版本。',
      scenarioHint: '围绕父产品做差异确认与子版本发布。',
      flowHint: '选择父产品 -> 差异确认 -> 模具判断 -> 差异验证 -> 差异BOM/资料 -> 子版本发布',
      activeCount: modelVariants.filter((item) => item.status !== 'released').length,
      blockedCount: modelVariants.filter((item) => !item.frozenFlag && item.status === 'reviewing').length,
      primaryActionLabel: '新建新型号'
    }
  ]
})
```

### 15.2 首页双入口结构示意
```vue
<section class="product-entry-grid">
  <button
    v-for="card in entryCards"
    :key="card.key"
    class="product-entry-card"
    :class="{ 'is-active': activeWorkbench === card.key }"
    type="button"
    @click="activeWorkbench = card.key"
  >
    <div class="toolbar-row">
      <div>
        <h3 class="section-title">{{ card.title }}</h3>
        <p class="page-panel-desc">{{ card.description }}</p>
      </div>
      <el-tag effect="light">{{ card.scenarioHint }}</el-tag>
    </div>

    <p class="entry-flow-hint">{{ card.flowHint }}</p>

    <div class="entry-metric-row">
      <div class="info-card">
        <span class="subtle-text">进行中</span>
        <strong>{{ card.activeCount }}</strong>
      </div>
      <div class="info-card">
        <span class="subtle-text">待处理阻塞</span>
        <strong>{{ card.blockedCount }}</strong>
      </div>
    </div>

    <div class="toolbar-row">
      <el-button
        type="primary"
        @click.stop="card.key === 'product-line' ? router.push('/products/create?type=product_line') : router.push('/products/create?type=model_variant')"
      >
        {{ card.primaryActionLabel }}
      </el-button>
      <el-button plain>
        查看工作台
      </el-button>
    </div>
  </button>
</section>
```

### 15.3 使用判断说明区示意
```vue
<section class="page-panel product-usage-guide">
  <div class="guide-pill">
    <strong>没有可继承父产品</strong>
    <span class="subtle-text">走新产品开发</span>
  </div>
  <div class="guide-pill">
    <strong>已有父产品，只做新机型 / 差异扩展</strong>
    <span class="subtle-text">走新型号扩展</span>
  </div>
</section>
```

### 15.4 工作台列表切换示意
```ts
const productLineRows = computed(() =>
  filteredRows.value.filter((item) => item.productType === 'product_line')
)

const modelVariantRows = computed(() =>
  filteredRows.value.filter((item) => item.productType === 'model_variant')
)

const currentWorkbenchRows = computed(() => {
  if (activeWorkbench.value === 'product-line') return productLineRows.value
  if (activeWorkbench.value === 'model-variant') return modelVariantRows.value
  return filteredRows.value
})
```

### 15.5 新产品工作台列表结构示意
```vue
<el-table v-if="activeWorkbench === 'product-line'" :data="productLineRows" border stripe>
  <el-table-column prop="productCode" label="产品编码" min-width="180" />
  <el-table-column prop="productName" label="产品名称" min-width="220" />
  <el-table-column prop="customerName" label="立项来源" min-width="160" />
  <el-table-column prop="currentStage" label="当前阶段" min-width="180" />
  <el-table-column label="当前节点" width="120">
    <template #default="{ row }">第 {{ row.currentStepNo || '--' }} 步</template>
  </el-table-column>
  <el-table-column label="模具状态" width="130">
    <template #default="{ row }">{{ row.moldAction || '完整开模' }}</template>
  </el-table-column>
  <el-table-column prop="activeBomVersion" label="BOM 主版本" width="140" />
  <el-table-column label="资料完整率" min-width="180">
    <template #default="{ row }">
      <el-progress :percentage="Math.round(row.completionRate * 100)" :stroke-width="8" />
    </template>
  </el-table-column>
  <el-table-column prop="ownerUserName" label="负责人" width="120" />
  <el-table-column label="操作" width="120" fixed="right">
    <template #default="{ row }">
      <el-button link type="primary" @click="openProduct(row.productId)">详情</el-button>
    </template>
  </el-table-column>
</el-table>
```

### 15.6 新型号工作台列表结构示意
```vue
<el-table v-else-if="activeWorkbench === 'model-variant'" :data="modelVariantRows" border stripe>
  <el-table-column prop="productCode" label="子版本编码" min-width="180" />
  <el-table-column prop="productName" label="子版本名称" min-width="220" />
  <el-table-column label="父产品" min-width="200">
    <template #default="{ row }">{{ row.seriesName }}</template>
  </el-table-column>
  <el-table-column prop="model" label="新型号" width="140" />
  <el-table-column label="差异范围" min-width="180">
    <template #default="{ row }">{{ row.gateSummary || '待确认差异范围' }}</template>
  </el-table-column>
  <el-table-column label="模具策略" width="140">
    <template #default="{ row }">
      {{ row.moldAction === 'modify' ? '改模' : row.moldAction === 'new' ? '新开模' : row.moldAction === 'none' ? '无需变更' : '--' }}
    </template>
  </el-table-column>
  <el-table-column prop="activeBomVersion" label="差异 BOM" width="130" />
  <el-table-column prop="currentStage" label="当前节点" min-width="180" />
  <el-table-column prop="ownerUserName" label="负责人" width="120" />
  <el-table-column label="操作" width="120" fixed="right">
    <template #default="{ row }">
      <el-button link type="primary" @click="openProduct(row.productId)">详情</el-button>
    </template>
  </el-table-column>
</el-table>
```

### 15.7 顶部动作区重构示意
```vue
<template #actions>
  <el-button @click="router.push('/sku-view')">SKU 视图</el-button>
  <el-button @click="router.push('/products/create?type=product_line')">新建新产品</el-button>
  <el-button type="primary" @click="router.push('/products/create?type=model_variant')">新建新型号</el-button>
</template>
```

### 15.8 样式结构示意
```css
.product-entry-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}

.product-entry-card {
  display: flex;
  flex-direction: column;
  gap: 16px;
  padding: 18px;
  border: 1px solid var(--plm-color-border-light);
  border-radius: var(--plm-radius-base);
  background: #fff;
  text-align: left;
  cursor: pointer;
}

.product-entry-card.is-active {
  border-color: var(--plm-color-primary);
  box-shadow: var(--plm-shadow-md);
  background: rgba(37, 99, 235, 0.05);
}

.entry-flow-hint {
  margin: 0;
  color: var(--plm-color-text-secondary);
  font-size: var(--plm-font-size-sm);
  line-height: 1.6;
}

.entry-metric-row {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
}

.product-usage-guide {
  display: flex;
  align-items: stretch;
  gap: 12px;
}

.guide-pill {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 12px 14px;
  border-radius: var(--plm-radius-base);
  background: #f8fafc;
  border: 1px dashed var(--plm-color-border);
}
```

## 16. 预期效果
按本方案落地后，产品管理主页预期会形成下面这些变化：

1. 用户进入首页后，先看到的是 `新产品开发` 和 `新型号扩展` 两个清晰入口。
2. 用户不需要先理解系统阶段定义，就能先判断自己该走哪条业务流程。
3. `新建产品` 会拆成 `新建新产品` 和 `新建新型号`，减少误建对象。
4. 新产品工作台会更适合完整研发链路推进。
5. 新型号工作台会更适合差异项、父产品继承和子版本发布管理。
6. 首页会从“阶段化队列表”升级成“入口分流 + 工作台推进”的真正业务主页。
7. 使用人第一次进入页面时，也能较快理解怎么用、为什么这样分。
8. 整个优化范围会被严格限制在产品管理主页模块，不会扩散到其他页面。

## 17. 使用的技术与实现逻辑
### 17.1 计划使用的技术
- Vue 3 `script setup`
- TypeScript
- Element Plus `el-table`、`el-button`、`el-tag`、`el-progress`
- `computed` 做双入口统计、工作台切换和列表派生
- CSS Grid + Flex 做双入口卡片与说明区布局

### 17.2 关键实现逻辑
本次方案的核心不是增加更多筛选条件，而是重排首页的认知顺序。

关键逻辑如下：

1. 先按 `productType` 把产品池分成 `新产品` 与 `新型号` 两个工作台。
2. 首页第一层直接展示两类对象各自适用场景和流程摘要。
3. 顶部主操作拆成 `新建新产品` 与 `新建新型号`。
4. 列表字段按业务语义分别设计，而不是只在统一表格里增加“流程类型”一列。
5. 本轮只调整 `ProductList.vue` 首页模块，不改详情页、编辑页、SKU 页和其他模块。

## 18. 风险点与注意事项
1. 本轮只做前端方案文档，不修改代码，不扩展后端。
2. 本轮范围被限制在 `ProductList.vue`，因此新建按钮只能复用既有入口，不在本轮改表单结构。
3. 当前 mock 数据里已有 `productType / moldAction / lifecycle` 能力，理论上足够支撑首页重构，尽量不要牵连其他文件。
4. 首页虽然强调双入口，但不能把 `全部任务` 彻底删除，否则管理层会缺少总览口径。
5. 新型号入口一定要强调“依附父产品”，否则容易被误当成另一条完整产品线。

## 19. 本轮结论
本轮最合理的重设计方向，是把产品管理主页从“以阶段为主的统一队列表”，改造成“以新产品 / 新型号为主的双入口业务工作台”。

新产品强调完整研发链路，新型号强调父产品继承与差异扩展。首页只负责分流、判断、定位和进入对应工作台，其他页面本轮全部保持原状。

这样既贴合真实业务，也更方便简洁，能明显降低使用者的理解成本和误操作概率，同时保证改造范围严格锁定在主页产品管理模块。

本次只完成方案沉淀，不修改项目代码。
