# PLM 后端核心闭环实施前文档

> 日期：2026-07-02  
> 文档类型：实施前沉淀  
> 依据文档：《PLM 后端总体方案与项目结构设计 v1.1》《PLM 后端核心闭环实施计划 v1.1》  
> 实施目标：先打通工程部在工作台通过进行中项目时间轴推进项目、维护 BOM、维护工艺路线、上传文件并归档到文件中心的核心业务闭环。

## 1. 实施结论

本次后端实施不按“每个前端页面各自补接口”的方式推进，而是围绕一个主聚合建设：

```text
Project / Product Timeline Aggregate
```

第一阶段把 `plm_product` 作为项目承载对象，用项目时间轴表达工程部真实推进流程。BOM、工艺路线、文件上传、节点确认、冻结发布都围绕项目时间轴发生。

实际闭环：

```text
工程部用户登录
-> 工作台查看进行中项目
-> 打开项目详情和时间轴
-> 当前节点确认 / 推进 / 退回
-> 在节点中维护 BOM
-> 在节点中维护工艺路线
-> 在节点中上传图纸、SOP、SIP、测试资料等文件
-> 文件元数据写入 plm_attachment
-> 文件中心归档、检索、查看、下载
-> BOM / 工艺 / 项目冻结
-> 项目发布 / 归档
-> 全过程写 plm_operation_log
```

## 2. 已确认现状

### 2.1 项目目录

```text
前端目录：plm-web
后端目录：plm-server
数据库目录：plm-database
沉淀目录：D:\Yuewei\资料\PLM\docs\后端-沉淀
```

备注：后端目录是 `plm-server`，不是 `plm-service`，后续实施、运行、改文件时不要写错路径。

### 2.2 后端现状

```text
后端框架：Spring Boot 3.5.0
Java 版本：17
数据访问：MyBatis-Plus 3.5.7
数据库驱动：PostgreSQL 42.7.11
接口文档：springdoc-openapi 2.8.9
已有能力：Product 查询、创建、更新、冻结、发布
已有返回结构：ResponseVO
已有分页结构：PageVO
已有异常结构：BusinessException + GlobalExceptionHandler
已有本地存储抽象：StorageClient / LocalStorageClient
```

当前后端结构仍以平铺为主：

```text
plm-server/src/main/java/com/yuewei/plm
├─ controller
├─ controller/dto
├─ service
├─ service/impl
├─ service/vo
├─ repository
├─ repository/entity
├─ common
└─ infrastructure
```

第一阶段建议先沿用现有风格，新增模块可以放入现有分层目录；等核心链路稳定后，再逐步迁移为 `modules/<module>` 结构。

### 2.3 数据库现状

现有核心表：

```text
plm_product
plm_product_bom
plm_product_bom_item
plm_process
plm_attachment
plm_operation_log
sys_user
```

已存在 V1.3 脚本增强冻结和锁定字段：

```text
plm-database/postgres/V1.3__workflow_locking_alignment.sql
```

现有 `plm_attachment` 结构主要字段是：

```text
owner_object_type
owner_object_id
file_category
file_name
file_ext
storage_key
file_size
mime_type
checksum
version_no
status
```

当前不足：

```text
缺少明确 project_id
缺少 timeline_node_key
缺少 stage_key
缺少 storage_type
缺少 original_file_name 与 storage_path 的清晰区分
```

实施时可以两种方式处理：

```text
优先方案：新增增量迁移脚本补字段
临时方案：owner_object_type=Product，owner_object_id=product_id，remark 或扩展字段记录节点信息
```

推荐使用优先方案，避免文件中心后续难以按项目节点检索。

## 3. 用到的技术

### 3.1 后端技术

```text
Spring Boot Web：REST API
Spring Validation：请求参数校验
Spring Security：保留依赖，第一阶段做简单 token
MyBatis-Plus：Mapper、分页、条件查询
PostgreSQL：远程业务库
MultipartFile：时间轴节点文件上传
本地文件系统：保存上传文件
springdoc-openapi：接口说明
事务管理：节点推进、BOM、工艺、附件、日志写入保持一致
```

### 3.2 前端联调技术

```text
Vue 3
TypeScript
Element Plus
现有 request.ts 封装
现有 api/modules 拆分
```

### 3.3 数据库技术

```text
PostgreSQL schema=plm
增量 SQL 迁移
JSONB 用于 operation_log 快照
软删除 deleted_flag
状态字段 status / lock_status
```

## 4. 计划修改代码

### 4.1 后端配置

计划修改：

```text
plm-server/src/main/resources/application-dev.yml
```

修改内容：

```text
1. 本机后端连接远程 PostgreSQL
2. 数据库密码改为环境变量读取
3. 增加本地上传根目录配置
4. 增加 Multipart 文件大小限制
5. 明确 app.security.enabled 第一阶段策略
```

备注：

```text
当前 application-dev.yml 仍指向 localhost:5433/plm。
如果真实联调使用远程 PostgreSQL，实施前必须修改配置，且不要把真实密码提交到 Git。
```

### 4.2 登录与当前用户

计划新增或修改：

```text
plm-server/src/main/java/com/yuewei/plm/controller/AuthController.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/LoginDTO.java
plm-server/src/main/java/com/yuewei/plm/service/AuthService.java
plm-server/src/main/java/com/yuewei/plm/service/impl/AuthServiceImpl.java
plm-server/src/main/java/com/yuewei/plm/service/vo/LoginVO.java
plm-server/src/main/java/com/yuewei/plm/service/vo/CurrentUserVO.java
plm-server/src/main/java/com/yuewei/plm/common/config/SecurityConfig.java
plm-server/src/main/java/com/yuewei/plm/common/interceptor/CurrentUserInterceptor.java
plm-server/src/main/java/com/yuewei/plm/common/util/SimpleTokenUtil.java
```

核心逻辑：

```text
1. 用户输入账号密码
2. 后端校验 sys_user 或临时配置用户
3. 返回 token、用户信息、allPermissions=true
4. 业务接口从 Header 中解析当前用户
5. 两位正式工程部用户默认拥有全部权限
```

备注：

```text
第一阶段不做复杂 RBAC。
权限重点不是限制操作，而是保证每个推进、上传、冻结动作都有 operator。
```

### 4.3 操作日志

计划新增：

```text
plm-server/src/main/java/com/yuewei/plm/repository/entity/OperationLog.java
plm-server/src/main/java/com/yuewei/plm/repository/OperationLogRepository.java
plm-server/src/main/java/com/yuewei/plm/service/OperationLogService.java
plm-server/src/main/java/com/yuewei/plm/service/impl/OperationLogServiceImpl.java
plm-server/src/main/java/com/yuewei/plm/common/constant/OperationActionConstants.java
```

核心逻辑：

```text
1. 统一封装 writeLog
2. 记录 object_type / object_id
3. 记录 action_type
4. 记录 before_snapshot / after_snapshot
5. 记录 operator_user_id / operator_user_name
6. 记录 request_id
7. 记录 action_result 和 action_comment
```

备注：

```text
现有 plm_operation_log 的 action_type 约束没有 timeline_advance、timeline_confirm 等值。
建议第一阶段先复用 create/update/freeze/publish/comment 等已有动作，或新增 V1.4 脚本扩展约束。
```

### 4.4 工作台进行中项目

计划新增或修改：

```text
plm-server/src/main/java/com/yuewei/plm/controller/ProjectController.java
plm-server/src/main/java/com/yuewei/plm/controller/WorkbenchController.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/ProjectQueryDTO.java
plm-server/src/main/java/com/yuewei/plm/service/ProjectService.java
plm-server/src/main/java/com/yuewei/plm/service/impl/ProjectServiceImpl.java
plm-server/src/main/java/com/yuewei/plm/service/vo/ProjectSummaryVO.java
plm-server/src/main/java/com/yuewei/plm/service/vo/ProjectDetailVO.java
plm-server/src/main/java/com/yuewei/plm/repository/ProductRepository.java
plm-server/src/main/java/com/yuewei/plm/repository/entity/Product.java
```

核心逻辑：

```text
1. 第一阶段 projectId 直接使用 productId
2. 查询 plm_product 中 status=developing/reviewing/draft 且未删除的数据
3. product_type 映射新产品线 / 新型号线
4. current_step_no 表示当前时间轴步骤
5. current_stage 根据 product_type + current_step_no 计算
6. active_bom_version 从 BOM 最新版本或 Product 扩展字段取值
7. 返回给工作台和项目管理进行中列表
```

备注：

```text
当前 Product entity 没有 currentStepNo、currentStage、activeBomVersion 字段。
可以通过增量脚本补字段，也可以先由后端根据 status 和关联表计算。
推荐补 current_step_no，active_bom_version 可以优先计算。
```

### 4.5 项目时间轴

计划新增：

```text
plm-server/src/main/java/com/yuewei/plm/controller/TimelineController.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/TimelineActionDTO.java
plm-server/src/main/java/com/yuewei/plm/service/TimelineService.java
plm-server/src/main/java/com/yuewei/plm/service/impl/TimelineServiceImpl.java
plm-server/src/main/java/com/yuewei/plm/service/TimelineDefinitionProvider.java
plm-server/src/main/java/com/yuewei/plm/service/vo/TimelineNodeVO.java
plm-server/src/main/java/com/yuewei/plm/service/vo/TimelineDetailVO.java
plm-server/src/main/java/com/yuewei/plm/common/constant/TimelineNodeConstants.java
```

核心逻辑：

```text
1. 根据 product_type 获取新产品线或新型号线节点定义
2. 根据 current_step_no 标记节点状态
3. 当前步骤之前为 completed
4. 当前步骤为 current
5. 当前步骤之后为 pending
6. confirm 确认当前节点或门禁节点
7. advance 推进到下一节点
8. return 退回修改或退回上一节点
9. 每次动作更新 Product 当前步骤和状态
10. 每次动作写 plm_operation_log
```

节点建议：

```text
新产品线：
1 立项确认
2 设计确认
3 开模试模
4 样品与工艺
5 小批与 MX 验证
6 投产决策

新型号线：
1 扩展确认
2 差异设计
3 模具判断
4 差异验证
5 小批与 MX 验证
6 冻结发布
```

备注：

```text
第一阶段不必新建完整 timeline 表。
如果要做更完整追溯，再新增 plm_project_timeline_node 表。
当前可以用 Product 当前步骤 + operation_log + attachment.timeline_node_key 支撑试运行。
```

### 4.6 BOM 管理

计划新增：

```text
plm-server/src/main/java/com/yuewei/plm/controller/BomController.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/BomCreateDTO.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/BomUpdateDTO.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/BomItemCreateDTO.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/BomItemUpdateDTO.java
plm-server/src/main/java/com/yuewei/plm/repository/entity/ProductBom.java
plm-server/src/main/java/com/yuewei/plm/repository/entity/ProductBomItem.java
plm-server/src/main/java/com/yuewei/plm/repository/ProductBomRepository.java
plm-server/src/main/java/com/yuewei/plm/repository/ProductBomItemRepository.java
plm-server/src/main/java/com/yuewei/plm/service/BomService.java
plm-server/src/main/java/com/yuewei/plm/service/impl/BomServiceImpl.java
plm-server/src/main/java/com/yuewei/plm/service/vo/BomVO.java
plm-server/src/main/java/com/yuewei/plm/service/vo/BomItemVO.java
```

核心逻辑：

```text
1. 按 projectId/productId 查询 BOM 版本
2. 创建 EBOM / MBOM / 包装 BOM
3. 添加 BOM 明细
4. 修改 BOM 明细
5. 标记替代料和损耗率
6. 冻结 BOM 时写 frozen_at/frozen_by/freeze_reason/lock_status
7. 冻结后不允许直接修改
8. BOM 变化写操作日志
9. 项目详情返回 BOM 摘要和 activeBomVersion
```

备注：

```text
BOM 不应只服务 /bom 页面。
接口必须支持 /api/v1/projects/{projectId}/boms，保证项目详情和时间轴节点能直接读取。
```

### 4.7 工艺路线

计划新增：

```text
plm-server/src/main/java/com/yuewei/plm/controller/ProcessRouteController.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/ProcessRouteCreateDTO.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/ProcessRouteUpdateDTO.java
plm-server/src/main/java/com/yuewei/plm/repository/entity/ProcessRoute.java
plm-server/src/main/java/com/yuewei/plm/repository/ProcessRouteRepository.java
plm-server/src/main/java/com/yuewei/plm/service/ProcessRouteService.java
plm-server/src/main/java/com/yuewei/plm/service/impl/ProcessRouteServiceImpl.java
plm-server/src/main/java/com/yuewei/plm/service/vo/ProcessRouteVO.java
plm-server/src/main/java/com/yuewei/plm/service/vo/ProcessOperationVO.java
```

核心逻辑：

```text
1. 复用 plm_process 表承载工艺路线和工序
2. process_type=routing 表示路线
3. process_type=operation 表示工序
4. product_id 关联项目 / Product
5. sequence_no 表示工序顺序
6. process_param_json 保存工艺参数摘要
7. quality_requirement 保存质量要求
8. 冻结工艺路线时写冻结字段和 lock_status
9. SOP / SIP 文件通过 attachment 关联 Process 或 Product + timeline_node_key
10. 工艺变化写操作日志
```

备注：

```text
当前数据库已有 plm_process，不建议第一阶段新增复杂工艺路线表。
如果后续要支持多层路线、工序版本对比，再拆专用表。
```

### 4.8 时间轴节点文件上传

计划新增或修改：

```text
plm-server/src/main/java/com/yuewei/plm/controller/AttachmentController.java
plm-server/src/main/java/com/yuewei/plm/controller/TimelineAttachmentController.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/AttachmentUploadDTO.java
plm-server/src/main/java/com/yuewei/plm/repository/entity/Attachment.java
plm-server/src/main/java/com/yuewei/plm/repository/AttachmentRepository.java
plm-server/src/main/java/com/yuewei/plm/service/AttachmentService.java
plm-server/src/main/java/com/yuewei/plm/service/impl/AttachmentServiceImpl.java
plm-server/src/main/java/com/yuewei/plm/service/vo/AttachmentVO.java
plm-server/src/main/java/com/yuewei/plm/infrastructure/storage/StorageClient.java
plm-server/src/main/java/com/yuewei/plm/infrastructure/storage/LocalStorageClient.java
```

核心逻辑：

```text
1. 前端从项目时间轴节点上传文件
2. 后端接收 MultipartFile
3. LocalStorageClient 保存真实文件到本机目录
4. 生成 storage_key 或 storage_path
5. 写入 plm_attachment
6. owner_object_type 优先使用 Product / Process / Attachment 支持范围内的枚举
7. owner_object_id 写 productId 或 processId
8. timeline_node_key 记录节点
9. file_category 区分 drawing/bom/sop/sip/test_report/customer_confirm/production_doc/other
10. 上传成功后写 operation_log
```

备注：

```text
当前 LocalStorageClient 只返回 /mock-storage/{storageKey}，还没有真实保存文件逻辑。
本次实施必须把它改成真实本地文件写入和读取。
文件真实内容在本机，数据库在远程；换机器运行后端时旧文件可能不可访问，需要在接口错误中明确提示。
```

### 4.9 文件中心归档查询

计划新增：

```text
plm-server/src/main/java/com/yuewei/plm/controller/FileCenterController.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/FileCenterQueryDTO.java
plm-server/src/main/java/com/yuewei/plm/service/FileCenterService.java
plm-server/src/main/java/com/yuewei/plm/service/impl/FileCenterServiceImpl.java
plm-server/src/main/java/com/yuewei/plm/service/vo/FileArchiveVO.java
plm-server/src/main/java/com/yuewei/plm/service/vo/FileGroupVO.java
```

核心逻辑：

```text
1. 文件中心只做归档、检索、查看、下载
2. 主上传入口在项目时间轴节点
3. 文件中心按 projectId/productId 分组
4. 支持按 timeline_node_key 查询
5. 支持按 file_category 查询
6. 支持关键字搜索文件名、项目名、分类
7. 下载时校验本地文件是否存在
8. 文件不存在返回明确错误，不返回空列表伪装成功
```

备注：

```text
前端 FileCenterView 当前仍有“添加文件”的演示入口。
第一阶段后端文档口径建议：文件中心不是主上传入口；如果保留添加按钮，也应调用同一套附件上传服务并要求选择项目和节点。
```

### 4.10 生命周期冻结 / 发布 / 归档

计划新增或修改：

```text
plm-server/src/main/java/com/yuewei/plm/service/LifecycleService.java
plm-server/src/main/java/com/yuewei/plm/service/impl/LifecycleServiceImpl.java
plm-server/src/main/java/com/yuewei/plm/controller/LifecycleController.java
plm-server/src/main/java/com/yuewei/plm/controller/dto/LifecycleActionDTO.java
plm-server/src/main/java/com/yuewei/plm/service/vo/LifecycleActionVO.java
plm-server/src/main/java/com/yuewei/plm/common/constant/ProductStatusConstants.java
```

核心逻辑：

```text
1. freezeProject 校验当前项目资料完整性
2. freezeBom 冻结 BOM
3. freezeProcess 冻结工艺路线
4. publishProject 发布项目
5. archiveProject 归档项目
6. abandonProject 放弃项目
7. 发布前检查时间轴是否到达可发布节点
8. 发布前检查 BOM 至少有冻结版本
9. 发布前检查工艺路线至少有冻结版本
10. 发布前检查必要文件已上传
11. 所有动作写 operation_log
```

备注：

```text
当前 ProductStatusConstants 没有 frozen 状态，只有 draft/developing/reviewing/released/archived。
第一阶段建议继续用 lock_status=frozen 表示冻结，用 status=released 表示发布，不强行新增 frozen 状态。
```

## 5. 计划修改数据库文件

建议新增：

```text
plm-database/postgres/V1.4__project_timeline_attachment_alignment.sql
```

建议内容：

```sql
SET search_path TO plm;

ALTER TABLE plm_product
  ADD COLUMN IF NOT EXISTS current_step_no integer NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS current_stage varchar(128),
  ADD COLUMN IF NOT EXISTS project_progress numeric(5,4) NOT NULL DEFAULT 0;

ALTER TABLE plm_attachment
  ADD COLUMN IF NOT EXISTS project_id bigint REFERENCES plm_product(product_id),
  ADD COLUMN IF NOT EXISTS timeline_node_key varchar(64),
  ADD COLUMN IF NOT EXISTS stage_key varchar(64),
  ADD COLUMN IF NOT EXISTS storage_type varchar(32) NOT NULL DEFAULT 'local',
  ADD COLUMN IF NOT EXISTS original_file_name varchar(255),
  ADD COLUMN IF NOT EXISTS content_type varchar(128);

CREATE INDEX IF NOT EXISTS idx_plm_product_current_step
  ON plm_product(product_type, current_step_no, status);

CREATE INDEX IF NOT EXISTS idx_plm_attachment_project_node
  ON plm_attachment(project_id, timeline_node_key, file_category);
```

备注：

```text
不要修改 V1.0、V1.1、V1.2、V1.3 历史脚本。
远程库只允许执行增量脚本。
执行前需要先确认远程库是否已经存在同名字段。
```

## 6. 计划修改前端文件

后端接口完成后，前端再按顺序切真实接口。

计划修改：

```text
plm-web/src/api/modules/auth.ts
plm-web/src/api/modules/product.ts
plm-web/src/api/modules/foundation.ts
plm-web/src/api/modules/bom.ts
plm-web/src/api/modules/process.ts
plm-web/src/api/modules/attachment.ts
plm-web/src/views/project/ProjectCenterView.vue
plm-web/src/views/file/FileCenterView.vue
```

前端联调逻辑：

```text
1. auth.ts 接 /api/v1/auth/login 和 /api/v1/auth/profile
2. product.ts 或新增 project.ts 接工作台项目列表
3. foundation.ts 中项目详情 mock 逐步替换为 /api/v1/projects/{id}/summary
4. bom.ts 接 /api/v1/projects/{projectId}/boms
5. process.ts 接 /api/v1/projects/{projectId}/process-routes
6. attachment.ts 接时间轴节点上传和下载
7. ProjectCenterView 里的当前节点、项目流程、BOM、工艺、资料区读真实数据
8. FileCenterView 改为归档查询为主
```

备注：

```text
前端时间轴结构已经基本完成，不建议大改 UI。
本次重点是把 mock 数据来源替换为后端真实接口。
```

## 7. 接口清单

### 7.1 登录

```text
POST /api/v1/auth/login
GET  /api/v1/auth/profile
POST /api/v1/auth/logout
```

### 7.2 工作台与项目

```text
GET /api/v1/workbench/projects/in-progress
GET /api/v1/projects
GET /api/v1/projects/{projectId}
GET /api/v1/projects/{projectId}/summary
```

### 7.3 时间轴

```text
GET  /api/v1/projects/{projectId}/timeline
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/confirm
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/advance
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/return
```

### 7.4 BOM

```text
GET    /api/v1/projects/{projectId}/boms
GET    /api/v1/boms/{bomId}
POST   /api/v1/projects/{projectId}/boms
PUT    /api/v1/boms/{bomId}
POST   /api/v1/boms/{bomId}/items
PUT    /api/v1/boms/{bomId}/items/{itemId}
DELETE /api/v1/boms/{bomId}/items/{itemId}
POST   /api/v1/boms/{bomId}/freeze
```

### 7.5 工艺路线

```text
GET  /api/v1/projects/{projectId}/process-routes
GET  /api/v1/process-routes/{processId}
POST /api/v1/projects/{projectId}/process-routes
PUT  /api/v1/process-routes/{processId}
POST /api/v1/process-routes/{processId}/freeze
```

### 7.6 附件与文件中心

```text
POST /api/v1/projects/{projectId}/timeline/{nodeKey}/attachments
GET  /api/v1/projects/{projectId}/timeline/{nodeKey}/attachments
GET  /api/v1/file-center/attachments
GET  /api/v1/attachments/{attachmentId}
GET  /api/v1/attachments/{attachmentId}/download
DELETE /api/v1/attachments/{attachmentId}
```

### 7.7 生命周期

```text
POST /api/v1/projects/{projectId}/freeze
POST /api/v1/projects/{projectId}/publish
POST /api/v1/projects/{projectId}/archive
POST /api/v1/projects/{projectId}/abandon
```

## 8. 实施顺序

```text
1. 确认远程 PostgreSQL 连接配置
2. 新增 V1.4 增量 SQL
3. 实现简单登录和当前用户
4. 实现 OperationLogService
5. 实现工作台进行中项目列表
6. 实现项目详情和时间轴查询
7. 实现时间轴节点 confirm / advance / return
8. 实现 BOM 查询、维护、冻结
9. 实现工艺路线查询、维护、冻结
10. 实现本地文件真实上传
11. 实现时间轴节点附件接口
12. 实现文件中心归档查询和下载
13. 实现项目冻结、发布、归档校验
14. 前端逐步替换 mock 接口
15. 按验收脚本跑完整闭环
```

## 9. 验收逻辑

### 9.1 最小闭环验收

```text
1. 工程部用户登录成功
2. 工作台能看到进行中项目
3. 打开项目详情能看到时间轴
4. 当前节点能确认
5. 当前节点能推进到下一节点
6. 项目能创建 BOM
7. BOM 能添加明细
8. BOM 能冻结
9. 项目能创建工艺路线
10. 工艺路线能冻结
11. 时间轴节点能上传文件
12. 文件中心能查到上传文件
13. 文件能下载
14. 项目能发布
15. 操作日志能查到关键动作
```

### 9.2 错误验收

```text
未登录访问业务接口：返回未登录
跳过当前节点推进：返回状态非法
冻结 BOM 后修改明细：返回已冻结不可修改
冻结工艺后修改：返回已冻结不可修改
发布时缺少 BOM：返回缺失 BOM
发布时缺少工艺路线：返回缺失工艺路线
下载本机不存在的文件：返回文件不存在
数据库写入失败：返回明确错误，不伪装为空结果
```

## 10. 备注

```text
1. 第一阶段不做复杂审批流，审批表现为时间轴节点确认、门禁节点确认和状态推进。
2. 第一阶段不做复杂 RBAC，两位工程部正式用户默认全部权限。
3. 文件上传主入口是项目时间轴节点，文件中心主要负责归档、查询、查看、下载。
4. BOM 和工艺路线不是孤立页面，而是项目时间轴推进中的核心成果物。
5. 当前数据库在远程、文件在本机，必须接受“换机器文件不可访问”的试运行限制。
6. 历史 SQL 不修改，所有结构变化走新增迁移脚本。
7. 前端 UI 已经有进行中项目时间轴，不建议优先重做界面。
8. 后续如果人数增加，再把节点确认升级为正式审批任务。
```

## 11. 本次实施前文档不包含的内容

```text
不直接修改业务代码
不直接执行数据库迁移
不直接联调远程 PostgreSQL
不直接替换前端 mock
不直接启动服务
```

本文档用于实施前对齐范围、文件、技术、逻辑和注意事项。确认后再进入代码实施阶段。
