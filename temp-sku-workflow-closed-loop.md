# SKU 管理场景简要工作流闭环

## 1. 场景目标
围绕 `产品卡片 -> SKU 列表 -> 详情弹窗 -> 新增 / 删除 / 版本确认` 建立一个最小可执行的业务闭环，让用户能在一个连续流程里完成 SKU 的识别、查看、判断和处理。

## 2. 业务入口
1. 用户进入 `SKU 管理` 首页。
2. 首页先按产品卡片展示，而不是直接铺开全部 SKU。
3. 用户先定位到目标产品，再进入该产品下的 SKU 列表。

## 3. 列表识别
进入具体产品后，SKU 列表至少展示以下关键字段：

- `SKU编码`
- `SKU名称`
- `型号`
- `单位`
- `项目来源`
- `颜色`
- `版本`
- `状态`
- `最近活跃`

这样用户先完成“这是谁、从哪来、现在是什么状态”的第一轮识别。

## 4. 快速查看
1. 用户点击列表右侧 `详情`。
2. 系统不跳转新页面，而是在当前页打开 `详情弹窗`。
3. 弹窗中集中展示：
   - 基础信息
   - 成本
   - 当前版本 BOM
   - 简要流程
   - 当前进程

这样用户可以在不离开列表页的前提下，快速完成一次业务判断。

## 5. 业务处理
在 SKU 列表页，用户可继续执行两个主动作：

1. `新增 SKU`
   - 从当前产品下新增型号扩展记录。
   - 补齐名称、型号、单位、项目来源等基础字段。

2. `删除 SKU`
   - 先勾选目标 SKU。
   - 再执行删除。
   - 删除前需要确认，避免误操作。

## 6. 版本确认
用户在详情弹窗中重点确认两件事：

1. 当前版本 BOM 是否正确。
2. 当前流程是否推进到预期节点。

如果 BOM、成本、流程都匹配，就说明该 SKU 当前版本资料是可继续推进的。

## 7. 闭环结果
一个完整的 SKU 管理闭环可以收口为：

```text
进入 SKU 管理
-> 选择产品卡片
-> 查看产品下 SKU 列表
-> 通过字段完成识别
-> 通过详情弹窗完成判断
-> 执行新增或删除
-> 确认当前版本 BOM / 成本 / 流程
-> 完成当前 SKU 的一次管理闭环
```

## 8. 简版总结
这条闭环的核心不是“看见一个 SKU”，而是：

1. 先定位产品。
2. 再识别 SKU。
3. 再查看详情。
4. 再执行处理。
5. 最后确认版本和流程状态。

这样页面就从“单纯列表”变成了一个真正服务业务判断的工作台。
