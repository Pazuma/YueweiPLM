# YUEWEI PLM 系统 — AI 开发认知文档

## 一、系统定位与边界

本项目是一个面向**手机壳制造业研发、样品、开模、工艺与生产资料沉淀**的 PLM 系统。

### 硬边界（不可逾越）

1. **当前只做 PLM**，不承担 ERP、MES、WMS、CRM 的正式执行职责
2. 后续 ERP/MES 集成属于**后续边界设计**，不在当前核心实现范围
3. PLM 始终作为**源模型**，不得为外部系统反向改造核心对象定义
4. 草稿/审批中数据不外发，只对外提供已冻结或已发布版本

---

## 二、行业核心特征与痛点

手机壳制造业研发的六个"高"：

| 特征 | 说明 |
| --- | --- |
| 高 SKU 组合 | 同一产品线 × 机型 × 颜色 × 材质 × 包装 × 客户版本 |
| 高打样频率 | V1、V2、V3 反复迭代 |
| 高模具依赖 | 多数新型号、新结构需开模与试模 |
| 高版本风险 | BOM、图纸、包装稿、客户确认版容易混乱 |
| 高协同要求 | 销售、项目、工程、采购、品质、生产多角色并行 |
| 高自主决策 | 并非所有新型号都来自客户下单——公司可基于市场判断自主开发、先开模、先备样 |
| 高成本不透明 | 无 BOM 统一管理则无法及时获知物料成本、研发投入、打样/工艺成本 |

传统 Excel + 共享盘 + 微信群的组合**无法支撑**：单一事实源、版本冻结、样品与量产一致性、客户确认留痕、内部立项与客户立项并行管理、开模门禁、基于 BOM 的成本可视化、变更闭环追溯。

---

## 三、全系统唯一核心业务对象（7 个，不可新增）

> **最高设计原则**：只能有 7 个一级业务对象。BOM、图纸、报价、模具、变更等全部依附于这 7 个对象，通过扩展表、关联表、附件表承载。

| 核心对象 | 本质定义 | 严禁新建的同类对象 |
| --- | --- | --- |
| **Customer** | 客户需求来源、确认主体、客户版资料归属 — 但并非每个 Product 必须先绑 Customer | Client、Buyer、Account |
| **Product** | 产品名称主线 + 型号/颜色/版本/BOM/图纸/测试资料的统一对象，通过 `parent_product_id` 自关联形成层级 | SKU、PhoneModel、BOM、FileVersion |
| **Order** | 业务触发入口 — 客户需求/样品需求/报价需求/内部立项/机会款储备的统一需求对象。**可以不绑 Customer**（内部立项场景） | SampleRequest、QuoteRequest |
| **ProductionOrder** | 打样/试产/试模/返工/量产准备的统一执行对象 | SampleTask、TrialOrder、WorkOrder |
| **Process** | 工艺路线/工序/参数/确认/ECR/ECN 的统一过程对象 | Routing、Operation、ECR、ECN |
| **Inventory** | 物料/半成品/成品/包材/模具/治具的统一库存对象 | Material、Tooling、Stock |
| **Workstation** | 机台/工位/产线/工作中心的统一位置对象（不是完整 MES 设备模型） | Machine、Line、Station |

### 每个对象的正确理解

- **Customer**：谁提客户需求、谁确认客户版、谁接收客户资料的主体。内部立项场景下可为空。
- **Product**：研发主线核心，不只成品名——承载版本、BOM、图纸、测试资料。通过 `parent_product_id` 实现产品名称→型号→颜色的层级。产品按**产品名称/系列名称**作为主分类入口，不按功能大类建复杂分类树。
- **Order**：业务入口但 ≠ ERP 销售订单。内部立项、样品储备、机会款开发都可通过 Order 表达。`customer_id` 可空。
- **ProductionOrder**：执行载体，打样/试模/试产/返工/量产准备统一承载。
- **Process**：不是单一工序名，而是工艺路线 + 工序 + 参数 + 变更的归口对象。
- **Inventory**：承载物料、包材、模具、治具。模具不是独立根对象。
- **Workstation**：工序执行位置，不是完整的 MES 排产设备模型。

---

## 四、两条核心业务主线（统一归入 Product）

### 4.1 新产品线开发

用于承接新产品名称、产品系列或新结构方向（如"超队 3.0"）。

#### 触发来源（不限于客户下单）

- 客户明确需求
- 市场趋势判断
- 销售机会预判
- 公司内部产品规划
- 管理层指定开发方向

#### 产品开发主流程（从系统认知总结直接转录）

```
产品立项（含立项说明书）
  → 确认立项（含供应商比价、开发方式确认、实际需求成本评估）
  → 画图查看
  → 供应商确认外观图纸
  → 申请开模
  → 制作模具（同时确认其他配件供应商）
  → 测试模具
  → 签样确认 ★ 重要门禁点
  → 加工艺（工艺分类）
  → 敲定工序
  → 确认组件
  → 确认组件成品
  → 最终外观确认样（外观定稿样）
  → 红样进行测试
  → 整理生产资料（SIP/SOP）
  → 黄样
  → 小批量市场测试（确认市场可能性）
  → 运模（选择物流）
  → MX 验收
  → 测试、验证（品质、数量）
  → MX 市场测试
  → 根据情况投产或继续优化
```

#### 关键理解

- 不是"建个 Product 就结束"，而是从立项到生产资料沉淀的**完整开发过程**
- 内部先行开发时，前期可无客户订单、无明确客户主体
- **供应商不只是加工方**，还参与图纸、外观、模具、配件等关键确认环节
- 客户型项目以客户确认为关键门禁；内部机会款以内部评审、市场测试结果为关键门禁
- **红样** = 开发测试样，**黄样** = 生产准备样，是两个重要的阶段资料节点

### 4.2 新型号线扩展

用于已有产品线下扩展具体型号/颜色/包装/客户版本（如"超队 3.0 iPhone18 黑色"）。

#### 触发来源（同样不限于客户下单）

- 客户新增型号需求
- 已知市场热机型判断
- 内部判断该机型有开发价值
- 为标准产品线提前储备型号版本

#### 业务主线

```
已有产品线 Product
  → 新型号/颜色/包装/客户版本需求或内部机会判断
  → Product 子项或子版本建立
  → 图纸与外观差异确认
  → 必要时申请开模或改模
  → 组件与工艺差异确认
  → 样品确认
  → 测试验证
  → 生产资料整理
  → 小批量验证/市场验证
  → 版本冻结 → 正式发布
```

**核心区别**：新型号线不建新根对象，在 Product 体系内通过 `parent_product_id` 和版本管理。

---

## 五、产品分类口径（修正）

> 产品按**产品名称**作为主线分类，不以功能大类（防摔类/磁吸类/商务类）做复杂分类树。

- 以产品名称、系列名称作为主分类入口
- 同一产品名称下扩展型号、颜色、客户版本、包装版本
- 工艺、结构、材质等信息作为产品属性、BOM、图纸、工艺资料的一部分沉淀，不单独作为产品分类主轴
- 系统核心是"以产品名称为主线、以版本和资料为核心"的研发管理方式

**典型产品名称示例**：超队 3.0、亮甲 3.0、某客户专版系列

---

## 六、立项说明书的核心内容

产品立项说明书至少包含：

| 内容项 | 说明 |
| --- | --- |
| 产品名称 | 产品命名 |
| 目标机型范围 | 适配哪些手机型号 |
| 预期工艺 | 主要工艺路线 |
| 预期完成时间 | 开发周期 |
| 预估成本 | 材料+工艺+模具+人工 |
| 预估人员投入 | 工程/项目/品质人力 |
| 预估产能投入 | 产线/机台需求 |
| 开发难点 | 技术风险点 |
| 关键风险点 | 业务风险点（交期/成本/质量） |
| 立项明细表 | 详细条目化内容 |

---

## 七、对象关系的正确理解（五层结构）

对象之间不是单条线性链路，而是五层关系共同作用。

### 7.1 需求线

```
Customer → Order
内部机会 / 内部立项 → Order（customer_id 可为空）
```

- Order 是业务入口，不要求必须有客户
- 内部自主开发时，Order 记录来源为内部机会、销售预判或管理层决策

### 7.2 产品线

```
Product(产品名称主线) → Product(型号/颜色/客户版本/子版本)
```

- 通过 `parent_product_id` 自关联
- 产品正式资料始终归集到对应 Product 版本

### 7.3 执行线

```
Order → ProductionOrder → Process → Workstation
```

- 内部自主开模和自主打样也沿用同一执行链路

### 7.4 资料线

```
Product → BOM / 图纸 / 包装稿 / 测试资料 / 客户确认资料
Product → Inventory（通过 BOM 或用料关系）
Process → SOP / SIP / 工艺参数 / 工艺变更
ProductionOrder → 样品记录 / 试模记录 / 执行记录
```

- **BOM 不只是结构资料，更是成本核算和研发成本可视化的重要基础**
- SIP/SOP 作为生产资料沉淀，属于工艺与附件体系的重要部分

### 7.5 审批与冻结线

```
Order / Product / ProductionOrder / Process
  → 审批流 → 冻结 → 发布 → 变更
```

### 7.6 完整关系图

```
Customer
  ├─> Order
  └─> 客户确认资料

内部机会 / 内部立项
  └─> Order

Order
  ├─> ProductionOrder
  │     ├─> Process
  │     │     └─> Workstation
  │     └─> 样品/试模/试产执行记录
  └─> 报价、样品、内部任务等需求类型

Product
  ├─> 子 Product（型号/颜色/客户版本）
  ├─> BOM（关联 Inventory）→ 成本核算基础
  ├─> 图纸/文件/包装稿/测试报告
  ├─> Process（产品工艺路线）
  ├─> 质量资料
  └─> 冻结版本 / 发布版本 / 变更记录

Inventory
  ├─> 物料
  ├─> 包材
  ├─> 模具
  └─> 治具
```

### 7.7 必须补充的交叉关系

- Customer ↔ Product：客户专版、确认版本、客户包装资料
- Product ↔ ProductionOrder：一个产品版本可能多轮打样与试模
- Product ↔ Process：一个产品版本对应一套正式/候选工艺路线
- Product ↔ Inventory：通过 BOM 建立物料、包材、模具、治具关系
- Process ↔ Inventory：某些工序依赖特定治具、模具或辅料
- Inventory ↔ Workstation：模具/治具可能归属或安装于特定机台
- ProductionOrder ↔ 质量记录：样品测试、试模结果、问题闭环必须回写到执行批次
- Order ↔ Customer：可强关联、可空、可弱关联（内部立项场景）

---

## 八、审批、冻结、发布 —— 三个最容易混淆的概念

| 概念 | 定义 | 口诀 |
| --- | --- | --- |
| **审批** | 业务放行机制 — 是否允许进入下一阶段/开模/发布/变更 | "能不能过" |
| **冻结** | 锁版本快照 — 锁定当前 BOM、图纸、工艺参数、确认文件，不可直接覆盖 | "锁住当前" |
| **发布** | 定正式版本 — 该版本成为正式有效版本，可作 ERP/MES 对接候选 | "定为正式" |
| **变更** | 处理已冻结/已发布版本后续调整的唯一途径 | "改要走流程" |

### 时序关系

基本流程：`审批通过 → 版本冻结 → 正式发布 → 后续变更`

但在实际业务中：
- **客户确认后先冻结**（锁定样品确认版资料），不是立刻发布
- **内部评审确认后先冻结**（锁定机会款版本），不是立刻发布
- **冻结 ≠ 发布**：冻结是锁版本，发布是定正式版本
- **已发布版本不得直接覆盖**，只能发起变更形成新版本

---

## 九、开模申请（含门禁 + 闭环）

### 触发来源（不要求必须先有客户下单）

- 客户明确提出新型号需求
- 销售判断热门机型值得抢先开发
- 公司计划做标准款、展会款、储备款
- 管理层判断某一结构值得提前布局

### 开模前门禁（6 项必须达标）

1. 结构方案明确
2. 目标机型/孔位/尺寸明确
3. 关键 BOM 已明确
4. 关键工艺路径已明确
5. 颜色或外观要求已明确
6. 包装与测试要求已明确

### 完整闭环

```
客户需求或内部机会判断
  → 开模条件确认（门禁检查）
  → 开模申请发起
  → 工程评审
  → 采购评审（供应商/成本/周期）
  → 品质评审（风险与验证要求）
  → 项目经理统筹确认
  → 管理层审批
  → 模具制作
  → 试模
  → 修模/再试模（可循环）
  → 模具验收
  → 进入正式样品或量产准备
```

**关键**：开模申请不是终点，试模任务由 ProductionOrder（`production_order_type=tooling_trial`）承接。模具对象归入 Inventory（`inventory_type=tooling`），不建独立根对象。

---

## 十、变更流程（当前可简化理解）

```
提出变更 → 评估影响 → 审批确认 → 更新资料 → 形成新版本
```

**核心原则**：不能直接覆盖旧版本，必须形成新版本并保留痕迹。当前阶段不需把变更流程写得过重过长。

---

## 十一、典型产品与典型工序

### 工序路线的核心原则

**不是所有产品走同一套工序**。工序由 `Product + Process + version_no` 决定。下面只是典型参考。

### 磁吸手机壳典型工序

```
画图 → 外观图纸确认 → 开模 → 试模 → 注塑成型 → 修边
  → 外观处理 → 磁铁装配 → 组件装配 → 外观确认样 → 测试 → 包装
```

### 贴皮类手机壳典型工序

```
画图 → 图纸确认 → 开模/试模 → 基材成型 → 裁片 → 贴合
  → 热压 → 修边 → 外观确认 → 组装 → 测试 → 包装
```

### 共性测试项目

跌落、耐磨、高低温、酒精、百格、磁吸力、尺寸/孔位匹配、外观全检、**红样测试**、**黄样确认测试**

---

## 十二、技术栈（固定）

| 层级 | 选型 |
| --- | --- |
| 前端 | Vue 3 |
| 后端 | Java Spring Boot |
| 数据库 | PostgreSQL（推荐）/ MySQL 8+ |
| 文件存储 | MinIO / S3 |
| 缓存 | Redis |
| 部署 | Docker |
| 接口 | REST API + JSON，统一前缀 `/api/v1/...` |

---

## 十三、强制命名与字段规范

### 字段命名

| 类型 | 强制命名 | 示例 |
| --- | --- | --- |
| 主键 | `xxx_id` | `product_id` |
| 外键 | `xxx_id` | `parent_product_id` |
| 状态 | `status` | `draft`、`confirmed` |
| 数量 | `quantity` | BOM 用量 |
| 时间 | `created_at` / `updated_at` | timestamp |
| 编码 | `xxx_code` | `product_code` |
| 名称 | `xxx_name` | `product_name` |
| 版本 | `version_no` | `V1`、`V2` |

### 编码规则

| 对象 | 格式 | 示例 |
| --- | --- | --- |
| Customer | `CUS-地区-流水` | `CUS-MX-0001` |
| Product | `PRD-产品名-型号-颜色-版本` | `PRD-CD30-IP18-BLK-A` |
| Order | `ORD-类型-年月日-流水` | `ORD-SAMPLE-20260603-001` |
| ProductionOrder | `PO-类型-年月日-流水` | `PO-SAMPLE-20260603-001` |
| Process | `PROC-产品短码-工序-序号` | `PROC-CD30-SPRAY-020` |
| Inventory | `INV-类型-流水` | `INV-MATERIAL-0001` |
| Workstation | `WS-类型-流水` | `WS-INJECTION-001` |

### 数据库规范

- 所有业务表必含：`created_at`、`created_by`、`updated_at`、`updated_by`、`deleted_flag`
- 版本资料必含：`version_no`、`status`
- 正式版本建议含：`effective_date`、`released_at`、`released_by`
- 金额：`decimal(18,4)` + 币种字段
- 比率：`decimal(10,4)`，按小数保存
- 表字段统一 `snake_case`

---

## 十四、状态机标准

| 对象 | 状态流转 |
| --- | --- |
| Customer | `draft → active → inactive` |
| Product | `draft → developing → reviewing → released → archived` |
| **Order** ★ | `draft → confirmed → in_production → completed → closed` |
| ProductionOrder | `draft → scheduled → in_progress → completed → closed` |
| Process | `draft → confirmed → locked → changed → archived` |
| Inventory | `draft → available → reserved → consumed → closed` |
| Workstation | `draft → available → in_use → maintenance → inactive` |

★ Order 状态机为全系统强制标准。

---

## 十五、角色与权限模型（RBAC + 数据权限 + 状态权限）

### 核心原则

- **发起与确认分离**：同一流程的发起人和确认人不得为同一人（系统强制校验）
- **全员可见，专人推进**：所有角色默认可查看项目状态与进展，流程节点推进需特定权限
- **超级管理员兜底**：可强制推进或禁止任意项目任意环节

| 角色 | 核心职责 | 定位 |
| --- | --- | --- |
| 销售 | Customer、Order、客户需求/反馈、报价查看 | 需求输入方 |
| 项目经理 | Product 立项**发起**、Order 创建、ProductionOrder 创建、开模申请**发起**、阶段跟踪 | **发起方**（可发起，不可自行确认） |
| 工程 | Product 技术字段、Process、BOM、SOP/SIP、开模资料、变更 | 技术主责方 |
| 采购 | Inventory、供应商、模具治具、材料/包装成本 | 成本与供应方 |
| 品质 | 测试、检验标准、合规资料、客户确认质量门禁 | 质量门禁方 |
| 生产 | ProductionOrder、Process、Workstation 查询与执行反馈 | 执行反馈方 |
| 财务 | 成本确认、利润率、报价审核 | 财务审核方 |
| 管理层 | 立项**确认**★、开模**确认**★、关键审批、风险控制、阶段放行、报表查看 | **确认方**（不可自行发起） |
| 管理员 | 用户、角色、字典、流程、系统配置 | 系统配置方 |
| **超级管理员** ★ | **强制推进/禁止任意项目任意环节、紧急解锁/解冻、绕过审批流直接放行或阻断** | **兜底控制方** |

### 发起与确认分离（强制）

| 流程 | 发起人 | 确认人 |
| --- | --- | --- |
| 产品立项 | 项目经理 | 管理层 |
| 开模申请 | 项目经理 | 管理层 |
| Product 发布 | 工程 | 管理层 |
| 报价审批 | 销售/项目经理 | 管理层 |

> 同一流程 `发起人.user_id ≠ 确认人.user_id`，系统强制校验。

### 关键权限原则

- **项目状态与进展**：全员可查看（所有角色的用户默认拥有只读查看权限）
- **成本字段**：默认只对采购、财务、管理层、授权项目经理、超级管理员开放
- **Product 发布**：必须工程主责 + 相关会签 + 管理层审批
- **已冻结版本**：不得直接编辑（超级管理员可紧急解锁，但触发全量审计）
- **已发布版本**：不得直接覆盖，只能发起变更
- **文件下载**：必须权限校验并留痕
- **超级管理员**：所有特权操作强制记录全量操作日志，自动通知管理层，每月生成操作月报

---

## 十六、12 个业务模块归口

| 编号 | 模块 | 核心归属 | 要记住的事 |
| --- | --- | --- | --- |
| 01 | 产品主数据与 SKU | Product | SKU 是型号/颜色/客户版本的组合视图，不是根对象 |
| 02 | 机型库管理 | Product | 机型是 Product 的属性与适配维度 |
| 03 | 样品开发管理 | Order + ProductionOrder + Product | 支持客户样品和内部样品两种场景 |
| 04 | BOM 管理 | Product + Inventory | BOM 归 Product，物料归 Inventory，BOM 也是成本核算基础 |
| 05 | 图纸与文件版本管理 | Attachment + Product/Process | 文件不单独升为根对象 |
| 06 | 工艺路线管理 | Process + Workstation | 工序/路线/参数统一在 Process 下 |
| 07 | 模具治具管理 | Inventory + Process + Workstation | 模具归 Inventory，支持客户项目和内部机会款开模 |
| 08 | 成本与报价管理 | Order + Product + Inventory + Process | 报价依附需求，成本来自物料与工艺 |
| 09 | ECR/ECN 工程变更 | Process + Product | 变更围绕工艺、版本、资料展开 |
| 10 | 质量与合规资料 | Product + ProductionOrder + Attachment | 既有产品归档也有执行批次记录 |
| 11 | 权限/配置/基础报表 | 支撑模块 | RBAC、审批流、日志与报表 |
| 12 | ERP 接口与数据导入 | 集成预留 | 当前只做预留，不做正式深度联调 |

---

## 十七、开发阶段规划（22 周）

| 阶段 | 周次 | 范围 |
| --- | --- | --- |
| 1 | 1-3 周 | 需求调研、流程梳理、业务蓝图 |
| 2 | 4-5 周 | 原型设计、权限设计、数据结构设计 |
| 3 | 6-13 周 | 核心模块：产品、样品、BOM、工艺、文件 |
| 4 | 14-18 周 | 扩展模块：模具治具、成本报价、变更、质量 |
| 5 | 19-20 周 | 联调测试、数据导入、集成预留验证 |
| 6 | 21 周 | 用户培训、试运行 |
| 7 | 22 周 | 正式上线、验收交付 |

---

## 十八、本期明确不包含

- CRM 全流程
- ERP 全流程及正式深度联调
- MES/WMS 深度集成
- 客户门户、供应商门户
- 完整双语 UI、多工厂集团权限
- 复杂 CAD 在线预览
- 高级模具寿命分析
- 复杂阶梯价引擎

---

## 十九、ERP/MES 集成预留规则

| PLM 对象 | → ERP | → MES |
| --- | --- | --- |
| Customer | 客户主数据 | 客户需求标识 |
| Product | 产品/物料/BOM | 产品型号/工艺版本 |
| Order | 需求来源/订单来源 | 生产需求来源 |
| ProductionOrder | 工单/委外单候选 | 工单/试产任务 |
| Process | 工艺路线/工序清单 | 工序步骤 |
| Inventory | 物料/包材/模具治具 | 在制物料/工装 |
| Workstation | 工作中心/设备 | 机台/工位/产线 |

**集成铁律**：草稿不外发、审批中不外发、只对外提供已冻结或已发布版本、外部系统不得覆盖 PLM 内部编码与版本定义。

---

## 二十、AI 开发强制规范

### 开发前必须先引用

1. `README.md`
2. 总纲与架构文档
3. 对应模块文档
4. 涉及审批/数据模型/验收/接口时的专项文档

### 收到任务后必须先输出（9 步，完成前不写代码）

1. 任务理解
2. 所属 PLM 业务链路
3. 涉及前端页面
4. 涉及后端模块
5. 涉及数据对象 / 状态 / API
6. 不确定点
7. 实施方案
8. 风险点
9. 交付文档结构

### 严禁事项

1. 不读文档直接开发
2. 新建第 8 个同类根对象
3. 绕过审批、冻结、发布、版本、日志规则
4. 未更新文档就新增字段、状态、接口
5. 无说明的大范围重构
6. 直接把方案落库到原业务代码（除非明确要求）

---

## 二十一、系统认知核心结论（19 条，每次开发前过一遍）

1. 当前系统是 **PLM 系统**，不是 ERP、MES 的替代品
2. 全系统只有 **7 个核心对象**，其他能力全部依附其上
3. 新产品线与新型号线都统一归入 **Product**
4. 业务入口**不只有客户下单**，还包括内部立项、市场机会判断、机会款储备开发
5. 公司内部可因看好某个型号或结构，**先发起新型号开发和开模准备**，不必等客户下单
6. 产品按**产品名称和系列名称**管理，不以复杂功能分类作为本期主线
7. 产品开发流程应围绕**立项、图纸、外观、开模、签样、工艺、组件、红样、黄样、市场测试、MX 验收**等关键节点理解
8. 供应商不只是加工方，还参与**图纸、外观、模具、配件**等关键确认环节
9. 对象关系是**五层结构**（需求线 + 产品线 + 执行线 + 资料线 + 审批线），不是简单线性流转
10. **Customer 不是每个 Product 的前置条件**，内部立项场景下 Order 的 customer_id 可为空
11. **冻结 ≠ 发布**：冻结是锁版本快照，发布是定正式版本
12. 客户确认后**先冻结**，内部评审确认后**先冻结**，满足条件后再正式发布
13. 已发布版本**不得直接覆盖**，只能发起变更形成新版本
14. **BOM 不只是结构资料**，更是成本核算和研发成本可视化的重要基础
15. 开模申请必须含**开模前门禁**（6 项条件）和**开模后试模闭环**
16. 模具归入 **Inventory**，试模任务由 **ProductionOrder** 承接
17. 工艺路线由 **Product + Process + version_no** 决定，不同产品不同工序
18. 变更流程当前可简化为：**提出变更 → 评估影响 → 审批确认 → 更新资料 → 形成新版本**
19. 所有开发、评审、验收以 **Markdown 文档体系**作为统一依据
