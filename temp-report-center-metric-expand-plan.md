# 38-报表中心指标点击展开对应项目列表交互方案

## 1. 本次需求理解

当前报表中心页面里，像“立项中”“模具阶段”“半成品阶段”这类数据板，只能看到数量和提示语，不能直接承接下一步查看。

你希望改成：

- 点击“立项中”，下方区域立即出现“立项中的项目列表”
- 点击“模具阶段”，下方区域切换成“模具阶段项目列表”
- 点击“半成品阶段”，下方区域切换成“半成品阶段项目列表”
- 其他报表模块同理

也就是说，报表页不只是“概览页”，而是要具备“点击指标 -> 在当前页展开该状态对应明细列表”的能力。

本次只做文档沉淀，不修改项目代码。

---

## 2. 当前页面问题

结合现有实现 [ReportCenterView.vue](d:/Yuewei/git/YUEWEI/plm-web/src/views/report/ReportCenterView.vue) 可以看到：

- 顶部报表卡片负责切换报表类型
- 中间 `metrics` 区域虽然已经做成可点击按钮
- 但点击后当前逻辑是直接跳路由 `targetPath`
- 下方内容只有：
  - 异常列表
  - 分布结构
- 缺少一个“当前指标对应的项目明细列表区”

所以现在的问题不是“不能点”，而是“点了以后没有在本页承接详细内容”。

---

## 3. 目标交互

### 3.1 开发进度报表

点击上方任一指标卡片：

- 点击“立项中”
  - 下方出现“立项中的项目列表”
- 点击“模具阶段”
  - 下方出现“模具阶段项目列表”
- 点击“半成品阶段”
  - 下方出现“半成品阶段项目列表”

列表中的每一行继续支持点击，进入对应产品详情或项目详情。

### 3.2 其他报表同理

#### 模具状态报表

- 点击“开模中” -> 下方显示“开模中模具/项目列表”
- 点击“试模中” -> 下方显示“试模中列表”
- 点击“已验收” -> 下方显示“已验收列表”

#### 成本分析报表

这里不一定按“项目阶段”展开，而是按当前卡片代表的业务集合展开：

- 点击“超队 3.0” -> 下方显示该产品相关成本明细列表
- 点击“亮甲 3.0” -> 下方显示该产品相关成本明细列表
- 点击“单品均摊” -> 下方显示参与均摊的对象列表或结构项列表

结论是：

- 交互模式统一
- 列表内容因报表类型不同而不同

---

## 4. 建议的页面结构调整

### 4.1 保留现有三层结构

报表中心仍保持：

1. 报表入口卡片区
2. 当前报表摘要区
3. 下方详细内容区

### 4.2 把“当前报表摘要区”改成四段

建议当前报表详情区改成下面四段：

1. 标题与导出操作
2. 指标卡片区
3. 指标对应明细列表区
4. 异常区 + 分布区

新的信息顺序应为：

- 先点一个指标
- 立刻在下面看到该指标的项目列表
- 再结合右侧异常与分布做判断

这样更符合用户操作习惯。

---

## 5. 明细列表区的展示方式

### 5.1 推荐做法

在 `metrics` 下方新增一个独立区块：

- 标题：当前选中指标名称
- 副标题：当前状态说明
- 列表内容：对应项目明细

例如点击“立项中”后：

- 标题：立项中项目
- 副标题：当前停留在立项确认或立项审批阶段的项目
- 列表项字段建议：
  - 产品名称
  - 产品线
  - 当前节点
  - 负责人
  - 停留时长
  - 风险提示
  - 查看按钮

### 5.2 列表区不要做成弹窗

不建议弹窗，原因：

- 报表页面本来就是分析页
- 弹窗会打断横向比较
- 用户可能会连续点多个指标切换查看

所以最合适的是“页内切换显示”。

---

## 6. 统一交互规则

为了让三个报表模块行为一致，建议统一下面的规则：

### 6.1 指标卡片的点击结果

- 点击后卡片进入选中态
- 下方列表区切换到对应数据
- 再次点击同一张卡片时，不收起，保持选中

### 6.2 默认选中逻辑

进入某个报表详情后：

- 默认自动选中第一张指标卡片
- 下方自动展示第一组明细列表

这样页面一打开就不是空的。

### 6.3 跨报表切换逻辑

当用户从“开发进度报表”切到“模具状态报表”时：

- 当前选中指标应重置
- 默认选中新报表的第一张指标卡片
- 下方列表同步刷新成新报表的第一组明细

---

## 7. 对现有前端结构的理解

当前涉及文件主要有三处：

### 7.1 页面文件

[ReportCenterView.vue](d:/Yuewei/git/YUEWEI/plm-web/src/views/report/ReportCenterView.vue)

当前职责：

- 渲染报表入口卡片
- 根据 `currentDetail` 展示指标、异常、分布

后续若实施，本文件需要新增：

- 当前选中的指标 key
- 指标点击后的列表联动逻辑
- 明细列表区模板

### 7.2 类型文件

[foundation.ts](d:/Yuewei/git/YUEWEI/plm-web/src/types/foundation.ts)

当前 `ReportDetailSection` 里只有：

- `metrics`
- `alerts`
- `distribution`

缺少：

- 每个指标对应的明细列表数据结构

后续若实施，需要为报表指标增加一个“展开明细”的类型定义。

### 7.3 Mock 数据

[foundation.ts](d:/Yuewei/git/YUEWEI/plm-web/src/mock/foundation.ts)

当前 `reportCenterSnapshot.details[].metrics[]` 里只有：

- `label`
- `value`
- `hint`
- `targetPath`

缺少：

- 指标唯一 key
- 指标列表标题
- 指标对应的明细列表项

所以如果后面开发，要在 mock 数据层先补足。

---

## 8. 建议的数据结构改造

以下是后续实施时建议新增的结构，不是本次要改代码。

### 8.1 指标项增加明细承载能力

建议把 `metrics` 的每一项从“纯数字卡片”扩展为“数字卡片 + 明细列表入口”。

建议结构概念如下：

```ts
interface ReportMetricDetailItem {
  itemId: string
  title: string
  subtitle: string
  owner: string
  currentNode: string
  durationText: string
  riskText?: string
  targetPath: string
}

interface ReportMetricItem {
  key: string
  label: string
  value: string
  hint: string
  targetPath?: string
  detailTitle: string
  detailSummary: string
  detailItems: ReportMetricDetailItem[]
}
```

这样就能支撑：

- 卡片本身显示数字
- 卡片选中后展示自己的列表

### 8.2 不同报表可复用同一结构

开发进度报表、模具状态报表、成本分析报表都可以共用上面这套结构。

只是 `detailItems` 的字段文案略有不同：

- 开发进度：更偏项目阶段
- 模具状态：更偏模具/治具状态
- 成本分析：更偏成本项与版本

如果不想一开始做得太抽象，也可以先统一字段展示，再逐步细化。

---

## 9. 页面效果建议

### 9.1 指标卡片选中态

点击后的指标卡片建议增加：

- 深一点的边框
- 淡蓝底色
- 标题高亮
- 当前选中角标

目的很直接：让用户知道“下面列表正在对应哪一张卡片”。

### 9.2 明细列表样式

列表建议做成纵向卡片或表格都可以，但就当前系统风格，更推荐“紧凑卡片列表”：

- 一行一条项目
- 左边显示产品与状态
- 右边保留查看按钮

原因：

- 和当前异常列表风格更统一
- 改造成本更低
- 不需要额外引入复杂表格交互

---

## 10. 推荐的页面布局顺序

以后真正开发时，建议调整为：

```text
报表入口卡片
  ↓
当前报表标题 / 导出
  ↓
指标卡片（可点击）
  ↓
当前指标对应项目列表
  ↓
异常列表 + 分布结构
```

这个顺序的好处是：

- 用户先看“有多少”
- 再看“具体是谁”
- 最后看“哪里异常、整体怎么分布”

比现在直接跳到异常和分布更顺手。

---

## 11. 后续实施时预计改动文件

如果后面你确认要开发，预计主要会改这几个文件：

### 11.1 页面文件

- [ReportCenterView.vue](d:/Yuewei/git/YUEWEI/plm-web/src/views/report/ReportCenterView.vue)

改动方向：

- 新增 `activeMetricKey`
- 新增 `activeMetricDetail`
- 调整指标卡片点击逻辑
- 新增明细列表区

### 11.2 类型文件

- [foundation.ts](d:/Yuewei/git/YUEWEI/plm-web/src/types/foundation.ts)

改动方向：

- 为 `ReportDetailSection.metrics` 补充明细列表结构

### 11.3 Mock 数据文件

- [foundation.ts](d:/Yuewei/git/YUEWEI/plm-web/src/mock/foundation.ts)

改动方向：

- 为每个报表的每张指标卡补充对应列表数据

---

## 12. 实施后的效果

如果按这个方案落地，最终效果会变成：

- 点击“立项中”，下方立即展开“立项中的项目列表”
- 点击“模具阶段”，下方立即切成“模具阶段列表”
- 点击“半成品阶段”，下方立即切成“半成品阶段列表”
- 模具状态报表、成本分析报表也完全按同样方式工作
- 用户不需要离开报表页，就能先完成一轮筛选和定位

这会让报表中心从“只会展示数字的入口页”，变成“能承接分析动作的工作页”。

---

## 13. 本次结论

本次需求本质上不是新增一个模块，而是把报表中心的指标卡从“静态数字展示”升级成“可展开明细的交互入口”。

我建议采用：

- 页内联动展开
- 每个指标卡承接自己的明细列表
- 不弹窗
- 不跳页优先
- 三个报表模块统一交互规则

这样既贴合你现在的使用方式，也和系统前面已经做过的“分层展示、先选再看明细”的方向一致。
