# PLM 数据库初始化结果记录

## 1. 初始化目标

将当前 PLM PostgreSQL 数据库脚本初始化到远程数据库：

- 地址：`<DB_HOST>`
- 端口：`5432`
- 数据库：`plm_data_management`
- 用户：`postgres`

本次执行脚本：

- `V1.0__init_schema.sql`
- `V1.1__init_seed_data.sql`
- `V1.2__erp_item_bom_alignment.sql`
- `V1.3__workflow_locking_alignment.sql`
- `V1.3__workflow_locking_seed_data.sql`

## 2. 执行方式

本次通过 Docker 容器内的 `psql` 执行远程初始化。

本地运行口径统一约定为：

- Docker 运行时使用 Docker Desktop
- 本地数据库验证通过 `plm-database/postgres/docker-compose.database.yml`
- 本地默认映射端口使用 `5433`

执行口径：

- 先做只读探测，确认可连通
- 确认远程库中不存在 `plm` schema
- 按 `V1.0 -> V1.1 -> V1.2` 顺序执行
- 后续本地增强按 `V1.3` 增量补齐审批、冻结、锁定、导入/集成状态机
- 执行后验证表、字段、字典、权限、审批定义

## 3. 初始化结果

执行结果：成功

验证结果如下：

| 检查项 | 结果 |
| --- | --- |
| `plm` schema 是否存在 | 是 |
| `plm` schema 表数量 | `41` |
| `plm_inventory` 字段数量 | `51` |
| `sys_dict_item` 初始化数量 | `136` |
| `sys_permission` 初始化数量 | `33` |
| `plm_approval_definition` 初始化数量 | `4` |

关键表检查通过：

- `plm_inventory`
- `plm_inventory_tax`
- `plm_product_bom`
- `plm_product_bom_item`
- `plm_attachment`
- `plm_integration_mapping`
- `plm_inventory_workstation_binding`

本地 Docker Desktop 增量验证结果：

| 检查项 | 结果 |
| --- | --- |
| `plm_approval_instance.initiator_user_id` | 已存在 |
| `plm_approval_instance.super_admin_action_type` | 已存在 |
| `plm_product.frozen_at` | 已存在 |
| `plm_product.lock_status` | 已存在 |
| `plm_inventory_workstation_binding` | 已建表 |
| `approval_status.completed/super_admin_*` | 已入字典 |
| `approval:force_*` 权限 | 已入权限表 |

## 4. 本次落库包含的能力

- 七个核心对象主表
- 产品扩展、成本、质量、报价、变更扩展表
- BOM 头、BOM 明细、BOM 工序、BOM 展开明细
- Inventory 条码、单位换算、默认项、供应商物料、补货规则、税模板预留
- 文件附件与下载日志
- 用户、角色、权限、审批、字典、编码规则、系统配置
- 导入日志与后续 ERP/MES 集成映射预留

## 5. 风险与说明

- 当前数据库已初始化完成，后续重复执行 `V1.0` 和 `V1.1` 不适合作为日常升级方式。
- 以后如有结构变更，建议继续新增 `V1.3`、`V1.4` 这类增量迁移脚本。
- 当前 `V1.3` 仍为增量迁移写法，适合作为后续 Flyway 目录继续演进的基础。
- 当前库名是 `plm_data_management`，但 schema 名仍然是 `plm`，这符合当前脚本设计。
- 本次只完成数据库结构和初始化数据落库，不代表后端服务、对象存储、Redis、审批流程引擎已联调。

## 6. 后续建议

- 下一步建议建立一份真实连接配置文档，约定 Spring Boot 使用的 JDBC URL、用户名、密码来源和环境变量名。
- 如果后续要让后端直接接这个库，建议补一份 `application-dev.yml / application-prod.yml` 的数据库配置模板。
- 如果准备继续开发后端，下一步最适合先落 Flyway 迁移目录和基础实体模型。
