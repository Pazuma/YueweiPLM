# 60-产品详情页乱码修复与文案统一前端优化方案

## 1. 任务理解
本次只做方案沉淀，不修改 `plm-web` 项目代码。

这轮的重点不是新增功能，而是先把 `ProductDetail.vue` 里已经出现的中文乱码系统修复，并统一页面文案口径。

本次目标是：

1. 修复用户可见的乱码中文。
2. 保持当前页面结构与业务链路不变。
3. 统一标题、按钮、标签、提示文案、弹窗说明和表格列名。
4. 只涉及前端展示层，不涉及后端接口、数据库和状态机。

## 2. 所属业务链路
本次仍属于 `Product` 详情工作台的前端治理，承接的是：

- 新产品线详情查看
- 新型号线详情查看
- 时间轴推进判断
- BOM 版本资料查看
- 商务区 / 质量区信息查看

如果页面标题、节点说明、BOM 标签和弹窗提示都是乱码，用户就无法准确判断当前区域、当前节点、当前版本资料和可执行动作。

## 3. 涉及前端页面
核心页面为：

- `plm-web/src/views/product/ProductDetail.vue`

乱码主要集中在：

1. `PageContainer` 默认标题与描述。
2. 分区标题和说明文案。
3. 基础信息区字段标签。
4. 时间轴区按钮、节点说明和状态提示。
5. 资料区中的 BOM、图纸文件、模具治具文案。
6. 商务区、质量区说明文案。
7. 节点推进 / 驳回 / 强制推进弹窗文案。
8. BOM 详情弹窗标题、表格列名和差异标识文案。

## 4. 涉及后端模块
本次不涉及后端模块改造，不新增也不修改：

- Spring Boot Controller
- Service
- Repository / Mapper
- 数据库表结构
- 正式 REST API

当前仍沿用现有前端 mock 数据：

- `plm-web/src/api/modules/foundation.ts`
- `plm-web/src/mock/foundation.ts`
- `plm-web/src/types/foundation.ts`

## 5. 涉及对象 / 状态 / API
本次涉及的核心对象仍是：

- `Product`
- `Process`
- `Inventory`

本轮不新增状态机，不调整流转，只修复“状态表达的可读性”。

仍使用：

```ts
getFoundationProducts()
getProductPresentation(productId)
```

## 6. 当前问题判断
当前页面存在 4 个关键问题：

1. 中文乱码已经影响主业务阅读。
2. 乱码分布很散，后续维护风险高。
3. 时间轴推进、驳回、强制推进等动作语义被破坏。
4. BOM 详情和节点弹窗最容易被误读。

## 7. 实施方案
### 7.1 修复范围
建议明确收口到 `ProductDetail.vue` 中所有用户可见中文，包括：

- 页头默认标题与描述
- 一级分区切换文案
- 二级资料区切换文案
- 基础信息字段名
- 时间轴动作按钮文本
- 节点详情字段名
- 弹窗标题、placeholder、确认语
- BOM 详情弹窗字段和表格列名
- 差异状态标签文案

### 7.2 不做的事
本轮不做：

- 不重构布局
- 不调整时间轴逻辑
- 不新增字段或接口
- 不拆更多子组件
- 不扩大到其他无关模块

### 7.3 文案统一原则
建议统一成以下口径：

- 页面名称统一使用 `产品详情`
- 区域统一使用 `项目时间轴`、`资料区`、`商务区`、`质量区`
- 基础字段统一使用 `产品线`、`型号 + 颜色`、`客户来源`、`当前阶段`
- 动作按钮统一使用 `查看节点`、`驳回`、`确认推进`、`强制推进`
- BOM 弹窗统一使用 `所属产品`、`版本状态`、`总成本`、`成本变化`、`物料编码`、`物料名称`、`用量`、`单位`、`供应商`、`单价`、`差异标识`

### 7.4 文案收口方式
建议把复杂长文案收口到脚本层的 `computed` 或 helper 函数里，避免模板里堆长三元表达式。

## 8. 计划修改的代码结构
后续正式落地时，建议优先修改：

- `plm-web/src/views/product/ProductDetail.vue`

如需联动排查，再补看：

- `plm-web/src/mock/foundation.ts`

## 9. 前端代码草稿
### 9.1 页面标题与描述
```ts
const pageTitle = computed(() => presentation.value?.title || '产品详情')
const pageDescription = computed(() => presentation.value?.summary || '聚焦项目推进、版本资料、模具治具、成本与质量记录。')
```

### 9.2 弹窗文案收口
```ts
const dialogChecklistTitle = computed(() => {
  if (dialogState.mode === 'force') return '越权前须知'
  if (dialogState.mode === 'reject') return '驳回前检查项'
  return '推进前检查项'
})
```

### 9.3 BOM 差异标识
```ts
function getBomChangeTypeLabel(changeType: ProductBomItemRow['changeType']) {
  if (changeType === 'new') return '新增'
  if (changeType === 'replace') return '替换'
  if (changeType === 'remove') return '删除'
  return '沿用'
}
```

## 10. 预期效果
落地后，页面会恢复为可读、可判断、可操作的产品详情工作台：

1. 所有关键可见中文恢复正常可读。
2. 用户能看懂每个区块的用途。
3. 时间轴推进、驳回、强制推进等动作具备清晰语义。
4. BOM 详情弹窗可以正常阅读，不再影响版本和成本判断。

## 11. 使用的技术与逻辑
### 11.1 技术
- Vue 3 `script setup`
- TypeScript
- `computed`
- Element Plus `el-dialog`、`el-table`、`el-tag`、`el-input`、`el-checkbox`

### 11.2 逻辑
核心是“前端展示层文本治理”：先替换乱码，再把复杂文案收口到脚本层，保证后续维护不再散落。

## 12. 风险点
1. 本轮只做方案文档，不修改项目代码。
2. 如果乱码来源是文件编码，正式落地时需要统一 UTF-8 保存。
3. 建议优先修复 `ProductDetail.vue`，不要顺手扩大范围。
4. 若 `mock/foundation.ts` 同页展示文案也受影响，再补修。

## 13. 本轮结论
本次最合理的处理方式，不是重做产品详情页，而是先把乱码系统性修复，并统一关键文案口径，让 `ProductDetail.vue` 重新成为可读、可判断、可操作的业务工作台。

本次只完成方案沉淀，不修改项目代码。
