# PLM 后端项目架构设计规范

> 版本：v1.0  
> 日期：2026-07-02  
> 适用范围：`plm-server` 后端工程  
> 文档性质：实施前架构与工程约束文档  
> 明确边界：本文档只定义后端语言、框架、架构、目录、规范和扩展方式，不编写业务代码。

## 1. 文档目标

本文档用于固定 PLM 后端第一阶段的项目架构。后续新增登录、操作日志、工作台进行中项目、项目时间轴、BOM、工艺路线、时间轴文件上传、文件中心归档等模块时，必须按本文档约束放置文件、设计接口、处理异常、记录日志、连接数据库和接入权限校验。

第一阶段后端目标不是一次性做成复杂平台，而是先打通工程部两个人在工作台推动项目的核心闭环：

```text
登录
-> 工作台查看进行中项目
-> 打开项目时间轴
-> 推进 / 确认 / 退回节点
-> 维护 BOM
-> 维护工艺路线
-> 在时间轴节点上传资料
-> 文件自动归档到文件中心
-> 冻结 / 发布 / 归档项目
-> 关键动作全部留痕
```

## 2. 已确认后端语言和框架

### 2.1 技术栈

| 类型 | 已确认方案 | 当前工程依据 |
| --- | --- | --- |
| 后端语言 | Java 17 | `pom.xml` 中 `java.version=17` |
| 主框架 | Spring Boot 3.5.0 | `spring-boot-starter-parent=3.5.0` |
| 构建工具 | Maven | `plm-server/pom.xml` |
| Web 框架 | Spring MVC | `spring-boot-starter-web` |
| 参数校验 | Spring Validation / Jakarta Validation | `spring-boot-starter-validation` |
| 权限框架 | Spring Security | `spring-boot-starter-security` |
| ORM / 数据访问 | MyBatis-Plus 3.5.7 | `mybatis-plus-spring-boot3-starter` |
| 数据库 | PostgreSQL | `postgresql=42.7.11` |
| API 文档 | SpringDoc OpenAPI 2.8.9 | `springdoc-openapi-starter-webmvc-ui` |
| 日志 | SLF4J + Logback | Spring Boot 默认日志体系 |
| AOP | Spring AOP | `spring-boot-starter-aop` |
| Redis 预留 | Spring Data Redis | `spring-boot-starter-data-redis` |
| 文件存储 | 本地服务器目录 | 第一阶段内网试运行方案 |

### 2.2 总体架构风格

后端采用 **模块化单体架构**。

```text
一个 Spring Boot 应用
一个 PostgreSQL 数据库
一套统一响应格式
一套统一异常处理
一套统一权限入口
按业务模块组织代码
通过 Service 层聚合业务能力
```

第一阶段不拆微服务，不引入独立工作流引擎，不做复杂 RBAC。核心聚合围绕：

```text
Project / Product Timeline Aggregate
```

即项目、产品、时间轴、BOM、工艺路线、附件、生命周期动作都围绕“工作台进行中项目时间轴”发生。

## 3. 为什么选择 Java + Spring Boot

### 3.1 选择原因

1. PLM 属于企业内部管理系统，业务规则、状态流转、权限、审计、数据库事务较多，Java + Spring Boot 对这类系统成熟稳定。
2. Spring Boot 配套生态完整，Web、校验、安全、事务、AOP、配置分环境、OpenAPI、日志体系都能统一管理。
3. 当前工程已经使用 Java 17 + Spring Boot 3.5.0，继续沿用可以减少迁移成本。
4. MyBatis-Plus 适合在已有 PostgreSQL 表结构上开发，便于保留现有表和 SQL 可控性。
5. 后续如果要接 ERP、MES、文件服务、消息队列，Spring 生态扩展路径清晰。
6. 项目第一阶段是本机后端 + 远程 PostgreSQL，Spring Boot 单体应用启动、部署、排查都比较直接。

### 3.2 不选其他方案的原因

| 备选方案 | 暂不选择原因 |
| --- | --- |
| Node.js / NestJS | 开发体验轻，但 PLM 后续事务、审计、状态流转、数据库一致性要求较强；当前工程也已是 Spring Boot，切换成本高。 |
| Python / FastAPI | 接口开发快，但企业级长期维护、强类型约束、事务边界、团队 Java 生态复用方面不如当前方案贴合。 |
| Go / Gin | 性能好，但业务管理系统更需要成熟企业框架、ORM 配套、安全和文档生态；当前阶段性能不是主要瓶颈。 |
| .NET | 企业能力强，但当前工程和已沉淀规范均基于 Java Spring Boot，切换会造成文档、代码、工具链全部重做。 |
| 微服务 | 当前正式使用约两人，业务闭环尚未跑通，拆微服务会引入服务治理、部署、链路追踪和数据一致性复杂度。 |
| 独立工作流引擎 | 第一阶段审批被简化为时间轴节点确认和状态推进，引入流程引擎会过重。 |
| JPA 优先 | 当前要求沿用已有 PostgreSQL 表结构，MyBatis-Plus 对表结构和 SQL 控制更直接。 |
| MinIO / S3 优先 | 第一阶段内网试运行，本地文件目录 + 数据库元数据更简单；正式部署或多机访问时再迁移对象存储。 |
| 完整 RBAC 优先 | 当前正式使用人员约两人且都有全部权限，先做简单登录和固定角色权限更适合。 |

## 4. 必须遵守的语言工程规范

### 4.1 Java 基础规范

1. 统一使用 Java 17 语法和 JDK 17 编译目标。
2. 包名统一使用 `com.yuewei.plm` 下级包。
3. 类名使用大驼峰，方法名和字段名使用小驼峰。
4. 数据库字段为 `snake_case`，Java 字段为 `camelCase`。
5. 不允许在 Controller 中写业务规则。
6. 不允许在 Repository / Mapper 中写业务状态判断。
7. 不允许直接拼接 SQL 参数，必须使用 MyBatis 参数绑定或 MyBatis-Plus 条件构造器。
8. 不允许把数据库密码、token 密钥、文件根目录等生产敏感配置写死在代码中。
9. DTO、VO、Entity 必须分离，不能直接把 Entity 暴露给前端。
10. 枚举、状态、错误码、动作类型必须集中定义，不能散落硬编码。

### 4.2 分层职责

```text
Controller：接收请求、参数校验、调用 Service、返回 ResponseVO
Service：业务规则、状态流转、权限校验、事务边界、操作日志
Repository / Mapper：数据读写，不承载业务判断
Entity：数据库表映射
DTO：请求入参
VO：接口出参
Validator：复杂业务校验
Infrastructure：文件、消息、外部系统等技术能力
Common：通用配置、响应、异常、常量、工具、拦截器
```

### 4.3 事务规范

1. 事务边界必须放在 Service 层。
2. 涉及新增、修改、删除、状态推进、文件元数据写入、操作日志写入的业务方法必须明确事务策略。
3. 单个业务动作涉及多张表时，要么整体成功，要么整体回滚。
4. 文件上传要区分“文件保存成功但数据库失败”和“数据库成功但文件失败”的补偿策略，不能把失败伪装成成功。
5. 只读查询原则上不加写事务，必要时使用只读事务。

## 5. 必须遵守的 Spring Boot 最佳实践

### 5.1 框架使用原则

1. 优先使用 Spring Boot 自动配置，不重复造基础设施。
2. 参数校验使用 `@Valid`、`@Validated` 和 Jakarta Validation 注解。
3. 全局异常统一进入 `@RestControllerAdvice`。
4. 权限入口统一进入 Spring Security Filter Chain。
5. 请求日志、requestId、当前用户上下文使用 Filter 或 Interceptor 统一处理。
6. 操作日志使用 Service 或 AOP 统一沉淀，不在每个 Controller 中手写重复逻辑。
7. API 文档使用 SpringDoc OpenAPI 生成，接口命名和 DTO/VO 要清晰。
8. 环境差异通过 `application-dev.yml`、`application-prod.yml` 和环境变量处理。

### 5.2 框架最大化利用原则

后续开发必须优先利用框架已有能力：

| 场景 | 应优先使用 |
| --- | --- |
| REST 接口 | Spring MVC Controller |
| 参数校验 | Spring Validation |
| 登录和权限入口 | Spring Security |
| 事务 | Spring `@Transactional` |
| 异常收口 | `@RestControllerAdvice` |
| 分页 | MyBatis-Plus 分页插件 |
| CRUD 基础能力 | MyBatis-Plus Mapper / Service 能力 |
| 操作留痕 | Spring AOP 或统一 OperationLogService |
| 配置隔离 | Spring Profiles |
| API 文档 | SpringDoc OpenAPI |
| 日志 | SLF4J + Logback |
| 文件上传 | Spring `MultipartFile` |

不要在框架已经提供成熟能力的地方手写一套平行机制。

## 6. 目录约束

### 6.1 当前工程兼容结构

当前 `plm-server` 已有结构如下：

```text
src/main/java/com/yuewei/plm/
├─ PlmApplication.java
├─ common/
│  ├─ config/
│  ├─ constant/
│  ├─ exception/
│  ├─ interceptor/
│  ├─ util/
│  └─ vo/
├─ controller/
│  └─ dto/
├─ service/
│  ├─ impl/
│  └─ vo/
├─ repository/
│  └─ entity/
└─ infrastructure/
   ├─ mq/
   ├─ sse/
   └─ storage/
```

第一阶段允许当前平铺结构继续存在，但新增模块必须逐步向模块化目录收敛。

### 6.2 新增模块目标结构

后续新增模块统一放到：

```text
src/main/java/com/yuewei/plm/module/<moduleName>/
```

标准目录如下：

```text
module/<moduleName>/
├─ controller/
├─ service/
│  └─ impl/
├─ repository/
├─ entity/
├─ dto/
├─ vo/
├─ validator/
└─ constant/
```

模块内部文件放置规则：

| 文件类型 | 放置目录 | 命名示例 |
| --- | --- | --- |
| Controller | `controller/` | `ProjectController.java` |
| Service 接口 | `service/` | `ProjectService.java` |
| Service 实现 | `service/impl/` | `ProjectServiceImpl.java` |
| Repository / Mapper | `repository/` | `ProjectRepository.java` |
| Entity | `entity/` | `Project.java` |
| 创建请求 DTO | `dto/` | `ProjectCreateDTO.java` |
| 更新请求 DTO | `dto/` | `ProjectUpdateDTO.java` |
| 查询请求 DTO | `dto/` | `ProjectQueryDTO.java` |
| 返回对象 VO | `vo/` | `ProjectVO.java` |
| 复杂校验器 | `validator/` | `ProjectValidator.java` |
| 模块常量 | `constant/` | `ProjectStatusConstants.java` |

### 6.3 推荐模块划分

```text
module/auth              登录、当前用户、简单 token
module/operationlog      操作日志
module/workbench         工作台聚合查询
module/project           项目承载对象
module/timeline          项目时间轴
module/product           产品主数据
module/bom               BOM 管理
module/process           工艺路线
module/attachment        时间轴附件上传
module/filecenter        文件中心归档查询与下载
module/lifecycle         冻结、发布、归档、废弃
module/system            用户、角色、字典等系统配置
infrastructure/storage   本地文件存储、后续对象存储适配
infrastructure/mq        Redis Stream 或消息发布预留
infrastructure/integration 后续 ERP / MES 集成预留
```

### 6.4 新增模块怎么放文件

以后新增一个模块时，按下面顺序建文件：

```text
1. module/<module>/dto        先定义请求参数
2. module/<module>/vo         再定义返回对象
3. module/<module>/entity     如需要映射数据库表则建立实体
4. module/<module>/repository 数据访问
5. module/<module>/service    业务接口
6. module/<module>/service/impl 业务实现和事务
7. module/<module>/validator  复杂校验
8. module/<module>/controller 最后暴露接口
9. module/<module>/constant   状态、动作、错误码等集中沉淀
```

跨模块调用原则：

```text
Controller 不跨模块调用 Repository
Controller 只调用本模块 Service 或明确的聚合 Service
Service 可以调用其他模块 Service
Repository 只服务本模块数据访问
Infrastructure 不反向依赖业务模块
Common 不依赖具体业务模块
```

## 7. 接口设计和响应规则

### 7.1 URL 规则

1. 所有业务接口统一使用 `/api/v1` 前缀。
2. 资源名使用复数名词，例如 `/products`、`/projects`、`/attachments`。
3. 动作类接口使用明确动作后缀，例如 `/publish`、`/freeze`、`/advance`。
4. 工作台聚合接口可以使用 `/workbench` 表达页面聚合语义。
5. 文件下载接口必须单独校验权限和文件存在性。

### 7.2 统一响应结构

所有 JSON 接口必须使用 `ResponseVO<T>`：

```text
code       业务码，成功为 0
message    响应消息
data       响应数据
requestId  请求 ID，用于排查日志
timestamp  响应时间
```

成功响应：

```text
code=0
message=success 或 created
data=实际数据，可以为空
```

分页响应必须使用统一分页对象，字段建议保持：

```text
content
page
size
totalElements
totalPages
```

### 7.3 空结果和错误的区别

必须区分：

```text
成功但无结果：code=0，data 为空列表或空分页
失败：code 非 0，message 写明失败原因
```

禁止用空数组、空字符串、空对象掩盖真实错误。例如：

```text
数据库连接失败不能返回空列表
文件不存在不能返回空文件
权限不足不能返回空数据
参数错误不能返回默认值
```

## 8. 错误处理规则

### 8.1 统一错误入口

后端必须通过 `GlobalExceptionHandler` 统一处理异常。当前工程已有：

```text
common/exception/GlobalExceptionHandler.java
common/exception/BusinessException.java
common/constant/ErrorCodeConstants.java
```

后续新增异常不要在 Controller 中零散 try-catch 返回。

### 8.2 错误码规则

当前错误码应继续沿用：

| 错误码 | 含义 |
| --- | --- |
| `0` | 成功 |
| `40001` | 参数校验失败 |
| `40002` | 状态流转非法 |
| `40101` | 未登录 |
| `40103` | 无权限 |
| `40301` | 版本已冻结 |
| `40302` | 版本已发布 |
| `40401` | 资源不存在 |
| `40901` | 编码或数据冲突 |
| `50001` | 服务端内部错误 |

### 8.3 异常处理约束

1. 参数格式、必填、长度、枚举错误归入参数校验失败。
2. 状态不允许推进、节点不允许跳过，归入业务异常。
3. 查询单个资源不存在，必须明确返回资源不存在。
4. 数据唯一性冲突，必须明确返回冲突错误。
5. 未登录和无权限必须分开。
6. 未知异常只对前端返回通用错误，对后端日志记录完整堆栈。
7. 文件不存在、文件读取失败、上传失败必须返回明确错误，不允许伪装成空结果。

## 9. 日志规则

### 9.1 应用日志

应用运行日志使用 SLF4J + Logback。

必须记录：

```text
请求方法
请求路径
requestId
当前用户
关键业务对象 ID
异常堆栈
耗时信息
```

禁止记录：

```text
明文密码
token 明文
数据库密码
敏感成本明细
过大的文件内容
```

### 9.2 请求日志连接方式

请求进入系统后必须形成同一条排查链路：

```text
HTTP Request
-> RequestLoggingInterceptor / Filter 生成或读取 requestId
-> 写入请求上下文
-> Controller / Service / Repository 日志带上 requestId
-> ResponseVO 返回 requestId
-> 异常日志带上 requestId
```

当前工程已有 `RequestLoggingInterceptor` 和 `RequestIdUtil`，后续应在此基础上增强，不另起一套请求日志机制。

### 9.3 操作日志

业务操作日志必须写入数据库表，建议表为：

```text
plm_operation_log
```

必须记录的动作：

```text
登录
项目节点推进
项目节点确认
项目节点退回
BOM 新增 / 修改 / 冻结
工艺路线新增 / 修改 / 冻结
时间轴附件上传
文件删除
项目冻结
项目发布
项目归档
项目废弃
```

操作日志建议字段：

```text
business_type
business_id
action_type
old_status
new_status
before_snapshot
after_snapshot
operator_user_id
operator_user_name
request_id
remark
created_at
```

第一阶段可以同步写入数据库。后续如果操作日志量增大，再通过 Redis Stream 异步写入。

### 9.4 日志连接方式

日志连接方式分三层：

```text
第一层：应用日志
  Spring Boot -> SLF4J -> Logback -> 控制台 / 日志文件

第二层：请求链路
  requestId -> Interceptor / Filter -> MDC 或请求上下文 -> ResponseVO

第三层：业务审计
  Service / AOP -> OperationLogService -> PostgreSQL: plm_operation_log
```

后续可扩展：

```text
Service / AOP
-> Redis Stream: plm.operation.log
-> LogConsumer
-> PostgreSQL: plm_operation_log
```

第一阶段不强制异步日志，优先保证动作可查、失败可排查。

## 10. 数据库规则

### 10.1 数据库选择

数据库统一使用 PostgreSQL。第一阶段运行方式：

```text
本机 Spring Boot 后端
-> JDBC
-> 远程 PostgreSQL
```

本地开发配置位于：

```text
src/main/resources/application-dev.yml
```

正式环境密码、地址、文件目录等必须通过环境变量或安全配置注入，不能写死在代码中。

### 10.2 数据访问规则

1. 优先沿用现有 PostgreSQL 表结构。
2. 不允许随意重跑全量初始化脚本。
3. 结构变化只能走增量迁移脚本。
4. 表字段命名使用 `snake_case`。
5. Java 字段命名使用 `camelCase`。
6. 查询默认过滤逻辑删除数据。
7. 删除优先软删除，确需物理删除必须在文档和代码中说明原因。
8. 业务状态字段必须有明确枚举范围。
9. 创建人、创建时间、更新人、更新时间、删除标记等审计字段要统一。

### 10.3 第一阶段重点表

后端第一阶段优先围绕这些表开发或适配：

```text
sys_user
plm_product
plm_product_bom
plm_product_bom_item
plm_process
plm_attachment
plm_operation_log
```

如附件表缺少时间轴归档字段，优先通过增量脚本补充：

```text
project_id
product_id
timeline_node_key
stage_key
business_type
business_id
file_category
```

## 11. 权限校验入口

### 11.1 第一阶段权限模型

当前实际使用人员约两人，且两人都有全部权限。因此第一阶段采用：

```text
简单登录
固定角色
工程部正式用户默认 allPermissions=true
```

不做复杂菜单权限、字段权限、审批角色矩阵。

### 11.2 统一权限入口

权限入口必须统一经过 Spring Security：

```text
HTTP Request
-> SecurityFilterChain
-> Token 解析过滤器
-> CurrentUserContext
-> Controller
-> Service 业务权限校验
```

当前 `SecurityConfig` 暂时允许所有请求通过，这是开发阶段状态。实现登录后必须调整为：

```text
放行：
  /api/v1/auth/login
  /swagger-ui/**
  /v3/api-docs/**

需要登录：
  /api/v1/**
```

### 11.3 权限校验分层

```text
Security 层：判断是否登录，解析当前用户
Service 层：判断用户是否允许操作该业务对象
VO 组装层：后续如有敏感字段，再按权限过滤返回字段
```

第一阶段 Service 层主要校验：

```text
当前用户是否存在
当前用户是否启用
当前用户是否工程部全权限用户
项目是否允许被当前状态操作
文件是否属于可访问项目
```

## 12. 文件上传与文件中心架构规则

第一阶段文件上传采用：

```text
Spring MultipartFile
-> LocalStorageClient
-> 服务器本地目录
-> plm_attachment 元数据
-> 文件中心查询 / 下载
```

必须遵守：

1. 文件上传入口优先放在项目时间轴节点下。
2. 文件中心是归档、检索、查看、下载入口，不作为第一阶段主上传入口。
3. 数据库只保存元数据和存储路径，不保存文件二进制内容。
4. 存储路径必须基于配置根目录生成。
5. 数据库保存相对路径或可迁移路径，不直接依赖开发者个人绝对路径。
6. 文件名必须生成安全文件名，原始文件名只作为元数据保存。
7. 下载时必须先查元数据，再校验权限，再校验本地文件存在。
8. 本地文件不存在时返回明确错误。

## 13. 模块新增示例

### 13.1 新增工作台模块

```text
module/workbench/controller/WorkbenchController.java
module/workbench/service/WorkbenchService.java
module/workbench/service/impl/WorkbenchServiceImpl.java
module/workbench/dto/WorkbenchProjectQueryDTO.java
module/workbench/vo/InProgressProjectVO.java
module/workbench/vo/WorkbenchSummaryVO.java
```

### 13.2 新增时间轴模块

```text
module/timeline/controller/TimelineController.java
module/timeline/service/TimelineService.java
module/timeline/service/impl/TimelineServiceImpl.java
module/timeline/dto/TimelineActionDTO.java
module/timeline/vo/TimelineNodeVO.java
module/timeline/vo/TimelineDetailVO.java
module/timeline/constant/TimelineActionConstants.java
module/timeline/validator/TimelineActionValidator.java
```

### 13.3 新增 BOM 模块

```text
module/bom/controller/BomController.java
module/bom/service/BomService.java
module/bom/service/impl/BomServiceImpl.java
module/bom/repository/BomRepository.java
module/bom/repository/BomItemRepository.java
module/bom/entity/ProductBom.java
module/bom/entity/ProductBomItem.java
module/bom/dto/BomCreateDTO.java
module/bom/dto/BomItemUpdateDTO.java
module/bom/vo/BomVO.java
module/bom/vo/BomItemVO.java
module/bom/validator/BomValidator.java
```

### 13.4 新增工艺路线模块

```text
module/process/controller/ProcessRouteController.java
module/process/service/ProcessRouteService.java
module/process/service/impl/ProcessRouteServiceImpl.java
module/process/repository/ProcessRouteRepository.java
module/process/entity/ProcessRoute.java
module/process/dto/ProcessRouteCreateDTO.java
module/process/vo/ProcessRouteVO.java
module/process/validator/ProcessRouteValidator.java
```

### 13.5 新增附件和文件中心模块

```text
module/attachment/controller/TimelineAttachmentController.java
module/attachment/service/AttachmentService.java
module/attachment/service/impl/AttachmentServiceImpl.java
module/attachment/repository/AttachmentRepository.java
module/attachment/entity/Attachment.java
module/attachment/dto/AttachmentUploadDTO.java
module/attachment/vo/AttachmentVO.java

module/filecenter/controller/FileCenterController.java
module/filecenter/service/FileCenterService.java
module/filecenter/service/impl/FileCenterServiceImpl.java
module/filecenter/dto/FileCenterQueryDTO.java
module/filecenter/vo/FileArchiveVO.java
```

## 14. API 文档规则

1. 所有对外接口必须能出现在 OpenAPI 文档中。
2. Controller 方法命名要能表达业务动作。
3. DTO 字段必须有清晰命名和校验规则。
4. 接口返回不要使用裸 `Map` 作为长期结构，临时结构必须尽快沉淀为 VO。
5. 前端已经实现的页面字段，要优先在 VO 中明确表达。
6. API 路径变更要同步修改前端 API 模块和文档。

## 15. 测试和验证规则

后端每完成一个模块，至少按顺序验证：

```text
1. Maven 编译和测试
2. Spring Boot 启动
3. OpenAPI 文档可访问
4. GET 查询接口
5. POST 创建或动作接口
6. 参数校验失败场景
7. 状态非法场景
8. 权限未登录场景
9. 数据库记录是否正确
10. 操作日志是否正确
11. 前端联调
```

当前本机 Maven 可使用：

```text
D:\Yuewei\IntelliJ IDEA 2026.1.2\plugins\maven\lib\maven3\bin\mvn.cmd
```

## 16. 第一阶段不做的内容

第一阶段暂不做：

```text
微服务拆分
完整 RBAC
复杂审批流引擎
独立流程设计器
MinIO / S3 强制接入
ERP / MES 正式集成
复杂质量闭环
复杂成本核算
大批量异步导入
多租户
分布式事务
```

这些能力后续可以扩展，但不能阻塞第一阶段核心闭环。

## 17. 架构执行红线

1. 不允许绕过 `ResponseVO` 直接返回杂乱结构。
2. 不允许绕过 `GlobalExceptionHandler` 分散处理异常。
3. 不允许绕过 Spring Security 自己在 Controller 里解析登录。
4. 不允许业务代码直接依赖开发者本机绝对文件路径。
5. 不允许把失败伪装为空结果。
6. 不允许新增模块继续无约束平铺在根目录。
7. 不允许在 Controller 中写状态流转规则。
8. 不允许在无操作日志的情况下完成关键业务动作。
9. 不允许重跑远程库全量初始化脚本。
10. 不允许把临时 mock 数据混入真实业务接口作为正式逻辑。

## 18. 当前代码差距备注

当前工程已经具备后端骨架：

```text
Spring Boot 3.5.0
Java 17
MyBatis-Plus
PostgreSQL 驱动
ResponseVO
GlobalExceptionHandler
ErrorCodeConstants
SecurityConfig
RequestLoggingInterceptor
OpenAPI 配置
本地文件存储接口雏形
```

仍需后续补齐：

```text
模块化目录迁移
简单登录与 token 解析
CurrentUserContext
业务操作日志落库
工作台进行中项目接口
时间轴查询和节点动作接口
BOM 模块接入时间轴
工艺路线模块接入时间轴
时间轴附件上传
文件中心归档查询和下载
生命周期冻结 / 发布 / 归档
```

## 19. 最终建议

后端从现在开始应按“模块化单体 + 时间轴主线”推进。先把工程部真实使用链路跑通，再逐步补充审批中心、质量、成本、ERP/MES 集成等外围能力。

所有新增代码都必须回答三个问题：

```text
它服务哪个模块？
它是否接入项目时间轴主线？
它是否符合统一响应、异常、日志、权限和数据库规则？
```

只有这三点清楚，后端结构才不会随着页面增多而失控。
